# PR10 — max_parallel = 1 closure: the working record

The record of the tenth slice of upstroke v0.2, kept on the branch so that a successor session
inherits what was decided and why. Written before the work and updated as each piece lands, so
that at any commit on this branch the record is true. It is **not** a design document:
`DESIGN.md` and the packet stay the authority, and a sentence here that disagrees with either is a
defect in this file.

**Branch:** `feature/pr10-max-parallel-one-closure`, cut from master at `61ec7587` — the merge
commit of pull request #275, the tree after G4 (`reviews/2026-09-10-gate-G4.md`, verdict at
`7e0110a1`) plus the two P1 repairs that followed it.

**Where this file lives, and why.** As PR9's record (`reviews/2026-09-08-pr9-record.md`): the
owner ruled on 2026-09-08 that a slice's working record does not belong at the repository root,
and `docs/internals/` holds only module notes (`validate-internals-notes.sh` N2). So it sits
beside the gate reports in `reviews/`, dated.

**Two sessions wrote this slice.** A Fable session on the `cameron` account collected the
context, planned the slice and landed the first three commits (`a7e497a1`, `e262fc75`,
`47c7556a`); the owner stopped it and started a fresh Fable session on `camwork`
(`claude-fable-5-1`, `max`), which reconciled the brief against the tree and wrote everything
from §0 row 4 on. Nothing on disk at the handover was redone.

## 0. Status

| Step | State |
|---|---|
| A read the packet (`decisions.pr_sequence[11]`, `run_end_policy`, `resource_accounting`, `tests_acceptance`, `bounded_census`, `sequential_substrate`, the five fault rows, INV-13/15/22/23, G5), the errata, the G4 report, `CLAUDE.md`, `MAINTAINING.md`, the findings README and PROCESS, `standards/SWEEP.md`, the internals README, `design/04`, `/14`, `/26`, the PR9 record and the three notes files | **done** |
| B Phase 0: O1 closed (`a7e497a1`, `e262fc75`); M22/M23 re-run and killed by the new assertions | **done** — §6 |
| C Phase 1: closure (T-FINISH) — the first cut (`47c7556a`), the five re-pinned tests, the census entries, the ST-17 tests | in progress — §3, §4, §7 |
| D Phase 2: finalization (T-FINALIZE) and the ST-18 tests | pending |
| E Phase 3: the report, projection equivalence, ST-20 naming | pending |
| F Phase 4: the census classifier, fault-row reachability, the summary | pending |
| G Phase 5: the per-resource ledger and the outcome equations | pending |
| H Phase 6: the ST-07 registry document, the observation export, the merge check | pending |
| I Phase 7: the acceptance subset at 1, the committed live-equals-replay helper | pending |
| J Phase 8: notes, this record, findings, the PR body, the ten gates, the Windows guest | pending |

## 1. What PR10 is, from the packet

`decisions.pr_sequence[11]`, `.id == "PR10"`, title *max_parallel = 1 closure*,
`production_effect: none`, `cumulative_gate_after: G5`, non-goals *production activation* and
*concurrency*.

Scope, verbatim: registry projections (incl. the run's runner identity with image id and digest),
integration ledger section, derived total run outcome wired to the loop (structural
admissibility; ceiling check appending budget_exceeded before ending; backoff branch; closure
step (5b) for deferred items), closure procedure (incl. halting conversion during a budget
drain), run_finished, terminal finalization (live and on resume for Complete/Halted; answer files
and private records never touched; report names the runner), cleanup per run_end_policy,
complete typed site bijection registry for the sequential topology incl. Event points in both
modes, IdUnread points, residue classes, and the fast no-execution record, bounded reachability
census with totality and the merge_prepared relations, per-resource ledgers per
decisions.resource_accounting incl. R27/R28, acceptance subset at 1; production writer and reader
still 3.

Gating: site bijection complete for the range; ST-14; ST-09 durable rows; ST-17; ST-18; ST-20
report/status naming. Invariants introduced: INV-13 (registry projections, integration ledger),
INV-15 (run-end closure, terminal finalization, cleanup ordering incl. execution root,
repair-source protection rule, answer-file retention), INV-22 (durable rows R1, R2, R5–R12,
R14–R16, R18, R21, R23–R25, R27, outcome equations, derived outcome, closure, finalization).
Preserved: INV-01..INV-17 (earlier portions), INV-19, INV-20 (fold), INV-23 at max_parallel = 1.

The errata (`2026-08-25-g2-pass-errata.md`) win over the packet where they conflict; none of
E1–E6 touches closure, finalization, the report, the census or the ledger.

## 2. What the tree already had at `61ec7587`, measured by reading

The fold's whole run-end vocabulary landed in PR3 and was certified at G1: `RunFinished4`,
`DerivedOutcome`, `RunState::derived_outcome` with the packet's precedence (`fold/outcome.rs`),
`check_run_finished` (`fold/check_end.rs`: refused unless the outcome equals the derived one and
`halted_at` agrees), `check_started_run`'s `RunIsOver` guard (a Complete or Halted run admits
nothing; a Parked or BudgetExceeded run admits `run_resumed` alone), `apply_resumed` clearing
`finished` and the epoch's budget stop and waking deferred items, and
`GenerationCloseReason::RunEnding { outcome }`. The census skeleton (`topology/census.rs`, ST-14
at G1) explores the two-task fixture and asserts totality, the deferred way-out, the publication
relations and replay equality. The typed site inventory names the finalization sites —
`Ref.DeleteCandidatesRef`, `Ref.DeletePreparedPin` and `Ref.DeleteCandidatePin` under
`T-FINALIZE`, `Worktree.RemoveExecutionRoot` (R18, `T-FINALIZE`), `RunDir.WriteReport` and
`Report.Write` (R21, after `run_finished`, `T-FINALIZE`), `Lock.Release` — and the funnels behind
them exist (`WorkspaceManager::delete_ref_expected_old`, `remove_execution_root`,
`rundir::write_report`).

What was missing was the production path through them: `select::checkpoint` refused
`Step::Closure` outright (`checkpoint_refusals`: "PR7: integration and run end beyond refusal"),
`LoopBranch::Closure` was `RefusedByCheckpoint`, `Progress::Blocked` reported a hard block nobody
answered, and recovery step (b) was `refuse_if_finished` — the refusal without the finalization.
No production emitter of `run_finished` existed (G4 report §2). `settle::close_generation` and
`close_retained` existed and were reachable only from recovery step (e) and the live retry's
`Close` arm.

## 3. Readings taken on packet ambiguities

Each is a place where the packet, the errata and the shipped vocabulary together do not determine
the answer. The reading, the alternative, and why. Readings 1–9 are the handover brief's; each
was re-derived here from the packet and the code before being kept, and the ones marked
*(changed)* differ from the brief.

### R1 — closure steps (2)–(4) at max_parallel = 1 are refusals naming PR11

`closure_procedure` steps (2) (in-flight cancellation or drain), (3) (complete promotions) and (4)
(complete authorized publications) describe work the synchronous loop never leaves open when it
reaches closure: an attempt is settled inside the step that started it, a promotion completes
inside `promote_candidate`, and a transaction reaches its terminal inside `integrate`. A fresh
process's recovery steps (d)–(f) settle, promote and complete them before the loop runs.
**Reading:** `closure::refuse_unclosable` refuses those three shapes before any append, naming
`tokio_boundary` ("closure under concurrency (halt cancellation, budget drain with halting
conversion, in-flight cancellation on an append error)" — PR11's). Halting conversion during a
budget drain has no in-flight settlement to convert at this width; `closure::ending_outcome`
reads halt before budget, which is the same precedence. The refusal is a checkpoint refusal of
the kind `checkpoint_refusals` licenses; it is reachable only by a log no sequential process
writes, and a test constructs one to hold the refusal.

### R2 — the hard block falls through to closure

`loop`: "else apply the hard-block rules (attached-terminal prompt or wait_on_block for open
questions) -> else run-end closure". Every `AnswerSource` that yields `Unanswered` from `resolve`
(unattended, an exhausted `wait_on_block`, an empty terminal line) means nobody answered.
**Reading:** `hard_block` calls `close_run`, which derives Parked (no admissible work, no backoff,
questions open). `Progress::Blocked` is gone; `Progress::Finished` replaces it. A parked run
resumes through `run_resumed` alone, which the fold admits (`check_started_run`), and the next
incarnation's first step ingests an answer left in the run directory (`T-ANSWER`).

### R3 — the ending outcome is computed before the open generations are closed

`closure::ending_outcome`: halt > budget > the fold's derived outcome. Under halt or budget it is
computed *before* closing the open generations, because `derived_outcome` is NotEnding while an
OpenNoAttempt or RetainedIdle generation blocks `common`. This is `PR7-R4-LOOP-004`'s first
shape: closure closes first and re-derives (`closure::confirm_derived`), and a residual
NotEnding refusal names every blocker (`closure::blockers`) — the diagnostic that finding owed.
`GenerationCloseReason::RunEnding { outcome }` carries the outcome the closure is ending with.
Parked and Complete have no open generation by construction: the selector would have continued
or retried it, or the lineage's question would have been refused while it was open
(`check_question_can_park_lineage`).

### R4 — `Step::NotStarted` and `Step::Finished`

The selector no longer offers a closure for an unstarted fold (there is nothing to close and no
outcome to derive), and a fold whose `finished()` is set selects `Step::Finished(outcome)`. Both
are refused at the checkpoint before any append: "the run has not started" and "already finished
as … continuation of a finished run is refused after finalization". A parked or budget-stopped
run continues only through a resume, which the fold admits by `run_resumed` alone. `Admitted`
carries `Closure` and neither of the two; `LoopBranch::of` maps both to no branch.

### R5 — `run_finished.merged` and `.parked` are derived and not validated

`RunFinished4 { merged, parked }` are derived from the fold (tasks `Merged`; tasks
`AwaitingInput`) by `report::merged_and_parked`, and are **not** validated by
`check_run_finished`, whose pass rule reads `outcome` and `halted_at` only. Validating them would
be a Class B change to `src/topology/fold/check_end.rs`. Live and replay agree because both read
the fold. Recorded as a reading, not a gap: the derived-outcome guard is what INV-15 names, and
the counts are the report's convenience (`PR31-RUN-FINISHED-COUNTS-NEVER-CHECKED` stands as
filed).

### R6 — the report lists retained candidates refs for every outcome but Complete

`terminal_finalization` (v): candidates refs are pruned at Complete only and "retained and listed
at Halted". The report is written after `run_finished` and before the cleanup steps, so at
Complete it already describes the pruned state (no retained refs listed), and at Halted, Parked
and BudgetExceeded it lists every candidate's ref with its commit. `report.json` is "regenerated,
never read back": the only read is the digest comparison that decides whether to rewrite it
(`TopologyReport::is_fresh_against`), and a report that does not parse is stale.

### R7 — a resume recreates a pruned execution root

R18 is "pruned unless a later resume recreates it". `run_recovery_order` recreates it through
`Worktree.CreateExecutionRoot` when absent, before `reclaim_stale_residue`, with a warning. That
site's declared adjacency is `Before(run_started)` and its fault row `T-RUNSTART`; executing it
inside `T-RESUME` is this reading, checked against the registry entries §7 builds: the entry for
the resume-time execution is keyed by the same site and carries the site's own row, and the
resume-time test is named beside the fresh-run one.

### R8 — the live retry path's Close arm scrubs the closed generation

`G4B-O3-LIVE-WORKTREE-MISSING-CLOSE-KEEPS-THE-INTENT`: after the `generation_closed` append
succeeds, `retry_ready`'s `Close` arm removes the closed generation's worktree (when residue, not
absence, failed the verification) and its durable intent through the workspace manager, as
`reclaim_closed_generations` does on resume. The test carries the intents read `G4G` made.

### R9 — the production reader stays at schema 3

`report::topology_status` and `TopologyReport::render` are test-reachable; `status::load` is
untouched. The ST-20 status half ("report.json and status name the run's kind, policy, image
reference, id, and digest") is asserted through them, and `contract_mappings.rs`'s `defer:PR10`
row maps to that test.

### R10 — closure at BudgetExceeded scrubs the closed generations' worktrees

`terminal_finalization`: "Parked and BudgetExceeded runs finalize only terminal items ((ii),
(iii), (iv)) and keep everything resumably_open"; R9's lifecycle: "OpenNoAttempt … closed at run
end", "candidate created or Closed: pruned (forced); intent removed". **Reading:** the worktree
of a generation *closure closes* is a Closed generation's and is pruned with the close, at
BudgetExceeded as at Halted, by `close_run` and not by the finalization step (i) — which at
BudgetExceeded and Parked does not run. Nothing resumably_open is touched: a resume dispatches a
fresh generation at the then-current head (ST-11), never the closed one.

### R11 — the ST-18 kills are injected error returns at each finalization site, plus one real kill

"kills after run_finished, after report write, and between every cleanup effect converge to the
outcome equations". An injected `Injection::Error` at a site's hook ends the command at that
point, and the durable state it leaves is the same state a kill there leaves — no in-memory
state survives either to the next resume. The matrix is driven that way at every finalization
site in turn; one real subprocess kill inside finalization is run beside it as executed evidence
of the same convergence.
