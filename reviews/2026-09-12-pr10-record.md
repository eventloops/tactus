# PR10 — max_parallel = 1 closure: the working record

The record of the tenth slice of upstroke v0.2, kept on the branch so that a successor session
inherits what was decided and why. Written before the work and updated as each piece lands, so
that at any commit on this branch the record is true. It is **not** a design document:
`DESIGN.md` and the packet stay the authority, and a sentence here that disagrees with either is a
defect in this file.

**Branch:** `feature/pr10-max-parallel-one-closure`, cut from master at `61ec7587` — the merge
commit of pull request #275, the tree after G4 (`reviews/2026-09-10-gate-G4.md`, verdict at
`7e0110a1`) plus the two P1 repairs that followed it.

**Where this file lives, and why.** As PR9's record (`reviews/2026-09-08-pr9-record.md`): the
owner ruled on 2026-09-08 that a slice's working record does not belong at the repository root,
and `docs/internals/` holds only module notes (`validate-internals-notes.sh` N2). So it sits
beside the gate reports in `reviews/`, dated.

**Two sessions wrote this slice.** A Fable session on the `cameron` account collected the
context, planned the slice and landed the first three commits (`a7e497a1`, `e262fc75`,
`47c7556a`); the owner stopped it and started a fresh Fable session on `camwork`
(`claude-fable-5-1`, `max`), which reconciled the brief against the tree and wrote everything
from §0 row 4 on. Nothing on disk at the handover was redone.

**Two more sessions wrote the repair round** (§13), after the round-1 review of `aaebf8df`: a
Fable session on `camwork` landed `8dd82409` through `895544fe` and the work-in-progress commit
`3923d2d5`, and was stopped by a capacity handover; a fresh Fable session on `cameron`
(`claude-fable-5-1`, `max`) reconciled its handover against the tree, merged master, finished the
census, the notes, this record and the body, and ran the gates and the guest. Nothing in the
work-in-progress commit was discarded without being read; what it got wrong is said in §13.

## 0. Status

| Step | State |
|---|---|
| A read the packet (`decisions.pr_sequence[11]`, `run_end_policy`, `resource_accounting`, `tests_acceptance`, `bounded_census`, `sequential_substrate`, the five fault rows, INV-13/15/22/23, G5), the errata, the G4 report, `CLAUDE.md`, `MAINTAINING.md`, the findings README and PROCESS, `standards/SWEEP.md`, the internals README, `design/04`, `/14`, `/26`, the PR9 record and the three notes files | **done** |
| B Phase 0: O1 closed (`a7e497a1`, `e262fc75`); M22/M23 re-run and killed by the new assertions | **done** — §6 |
| C Phase 1: closure (T-FINISH) — the first cut (`47c7556a`), the five re-pinned tests, the census entries, the ST-17 tests | **done** — `303c0997` (closure crosses the checkpoint, step (b) finalizes then refuses, this record), `09905d43` (ST-17 at the loop: closure per outcome, the closure witnesses, a real kill inside the end); §3, §4, §7 |
| D Phase 2: finalization (T-FINALIZE) and the ST-18 tests | **done** — `816aa8a0`; §7 |
| E Phase 3: the report, projection equivalence, ST-20 naming | **done** — `b02ad84c` (the acceptance subset at 1, projection equivalence at every prefix, `finalized_report_names_runner_identity`); §3 R12, §7 |
| F Phase 4: the census classifier, fault-row reachability, the summary | **done** — `76c7b930`; §3 R16, §8 |
| G Phase 5: the per-resource ledger and the outcome equations | **done** — `3c411b21`, which also fixes what the ledger found (§3 R13, §6); §8 |
| H Phase 6: the ST-07 registry document, the observation export, the merge check | **done** — `1b612c9a`; §3 R14, §8 |
| I Phase 7: the acceptance subset at 1, the committed live-equals-replay helper | **done** — in `b02ad84c` (the G4 report's `drive_keep`/`live_vs_replay` request: `live_vs_replay` and `assert_live_equals_replay` are committed and run after every step of the chain and of the projection test) |
| J Phase 8: notes, this record, findings, the PR body, the ten gates, the Windows guest | **done** — `7dfbf8b0` (notes, findings), then this record; §9, §10 |
| K Repair round 1 (the round-1 review of `aaebf8df`, 2026-09-13): the seven P1s, the four P2s, the P3 sentences, every copy of each | **done** — `8dd82409`, `94eaf4f4`, `64fea82b`, `2b019324`, `895544fe`, `3923d2d5`, `a75478c7`; §13 |
| L Master merged into the branch after the review (`origin/master` at `231c1aad`: #276 moved the finding ledger to `findings/`, #278), the branch's finding files relocated with it | **done** — `6fb5052e`; §6, §13 |
| M The repair round's prose moved into the notes (§13 of the standards), the re-deferred findings' lines, this record and the body at the round's head | **done** — `16fe48fe`, `36447851`, then this record; §5, §6, §8, §13 |

## 1. What PR10 is, from the packet

`decisions.pr_sequence[11]`, `.id == "PR10"`, title *max_parallel = 1 closure*,
`production_effect: none`, `cumulative_gate_after: G5`, non-goals *production activation* and
*concurrency*.

Scope, verbatim: registry projections (incl. the run's runner identity with image id and digest),
integration ledger section, derived total run outcome wired to the loop (structural
admissibility; ceiling check appending budget_exceeded before ending; backoff branch; closure
step (5b) for deferred items), closure procedure (incl. halting conversion during a budget
drain), run_finished, terminal finalization (live and on resume for Complete/Halted; answer files
and private records never touched; report names the runner), cleanup per run_end_policy,
complete typed site bijection registry for the sequential topology incl. Event points in both
modes, IdUnread points, residue classes, and the fast no-execution record, bounded reachability
census with totality and the merge_prepared relations, per-resource ledgers per
decisions.resource_accounting incl. R27/R28, acceptance subset at 1; production writer and reader
still 3.

Gating: site bijection complete for the range; ST-14; ST-09 durable rows; ST-17; ST-18; ST-20
report/status naming. Invariants introduced: INV-13 (registry projections, integration ledger),
INV-15 (run-end closure, terminal finalization, cleanup ordering incl. execution root,
repair-source protection rule, answer-file retention), INV-22 (durable rows R1, R2, R5–R12,
R14–R16, R18, R21, R23–R25, R27, outcome equations, derived outcome, closure, finalization).
Preserved: INV-01..INV-17 (earlier portions), INV-19, INV-20 (fold), INV-23 at max_parallel = 1.

The errata (`2026-08-25-g2-pass-errata.md`) win over the packet where they conflict; none of
E1–E6 touches closure, finalization, the report, the census or the ledger.

## 2. What the tree already had at `61ec7587`, measured by reading

The fold's whole run-end vocabulary landed in PR3 and was certified at G1: `RunFinished4`,
`DerivedOutcome`, `RunState::derived_outcome` with the packet's precedence (`fold/outcome.rs`),
`check_run_finished` (`fold/check_end.rs`: refused unless the outcome equals the derived one and
`halted_at` agrees), `check_started_run`'s `RunIsOver` guard (a Complete or Halted run admits
nothing; a Parked or BudgetExceeded run admits `run_resumed` alone), `apply_resumed` clearing
`finished` and the epoch's budget stop and waking deferred items, and
`GenerationCloseReason::RunEnding { outcome }`. The census skeleton (`topology/census.rs`, ST-14
at G1) explores the two-task fixture and asserts totality, the deferred way-out, the publication
relations and replay equality. The typed site inventory names the finalization sites —
`Ref.DeleteCandidatesRef`, `Ref.DeletePreparedPin` and `Ref.DeleteCandidatePin` under
`T-FINALIZE`, `Worktree.RemoveExecutionRoot` (R18, `T-FINALIZE`), `RunDir.WriteReport` and
`Report.Write` (R21, after `run_finished`, `T-FINALIZE`), `Lock.Release` — and the funnels behind
them exist (`WorkspaceManager::delete_ref_expected_old`, `remove_execution_root`,
`rundir::write_report`).

What was missing was the production path through them: `select::checkpoint` refused
`Step::Closure` outright (`checkpoint_refusals`: "PR7: integration and run end beyond refusal"),
`LoopBranch::Closure` was `RefusedByCheckpoint`, `Progress::Blocked` reported a hard block nobody
answered, and recovery step (b) was `refuse_if_finished` — the refusal without the finalization.
No production emitter of `run_finished` existed (G4 report §2). `settle::close_generation` and
`close_retained` existed and were reachable only from recovery step (e) and the live retry's
`Close` arm.

## 3. Readings taken on packet ambiguities

Each is a place where the packet, the errata and the shipped vocabulary together do not determine
the answer. The reading, the alternative, and why. Readings 1–9 are the handover brief's; each
was re-derived here from the packet and the code before being kept, and the ones marked
*(changed)* differ from the brief.

### R1 — closure steps (2)–(4) at max_parallel = 1 are refusals naming PR11

`closure_procedure` steps (2) (in-flight cancellation or drain), (3) (complete promotions) and (4)
(complete authorized publications) describe work the synchronous loop never leaves open when it
reaches closure: an attempt is settled inside the step that started it, a promotion completes
inside `promote_candidate`, and a transaction reaches its terminal inside `integrate`. A fresh
process's recovery steps (d)–(f) settle, promote and complete them before the loop runs.
**Reading:** `closure::refuse_unclosable` refuses those three shapes before any append, naming
`tokio_boundary` ("closure under concurrency (halt cancellation, budget drain with halting
conversion, in-flight cancellation on an append error)" — PR11's). Halting conversion during a
budget drain has no in-flight settlement to convert at this width; `closure::ending_outcome`
reads halt before budget, which is the same precedence. The refusal is a checkpoint refusal of
the kind `checkpoint_refusals` licenses; it is reachable only by a log no sequential process
writes, and a test constructs one to hold the refusal.

### R2 — the hard block falls through to closure

`loop`: "else apply the hard-block rules (attached-terminal prompt or wait_on_block for open
questions) -> else run-end closure". Every `AnswerSource` that yields `Unanswered` from `resolve`
(unattended, an exhausted `wait_on_block`, an empty terminal line) means nobody answered.
**Reading:** `hard_block` calls `close_run`, which derives Parked (no admissible work, no backoff,
questions open). `Progress::Blocked` is gone; `Progress::Finished` replaces it. A parked run
resumes through `run_resumed` alone, which the fold admits (`check_started_run`), and the next
incarnation's first step ingests an answer left in the run directory (`T-ANSWER`).

### R3 — the ending outcome is computed before the open generations are closed

`closure::ending_outcome`: halt > budget > the fold's derived outcome. Under halt or budget it is
computed *before* closing the open generations, because `derived_outcome` is NotEnding while an
OpenNoAttempt or RetainedIdle generation blocks `common`. This is `PR7-R4-LOOP-004`'s first
shape: closure closes first and re-derives (`closure::confirm_derived`), and a residual
NotEnding refusal names every blocker (`closure::blockers`) — the diagnostic that finding owed.
`GenerationCloseReason::RunEnding { outcome }` carries the outcome the closure is ending with.
Parked and Complete have no open generation by construction: the selector would have continued
or retried it, or the lineage's question would have been refused while it was open
(`check_question_can_park_lineage`).

### R4 — `Step::NotStarted` and `Step::Finished`

The selector no longer offers a closure for an unstarted fold (there is nothing to close and no
outcome to derive), and a fold whose `finished()` is set selects `Step::Finished(outcome)`. Both
are refused at the checkpoint before any append: "the run has not started" and "already finished
as … continuation of a finished run is refused after finalization". A parked or budget-stopped
run continues only through a resume, which the fold admits by `run_resumed` alone. `Admitted`
carries `Closure` and neither of the two; `LoopBranch::of` maps both to no branch.

### R5 — `run_finished.merged` and `.parked` are derived and not validated

`RunFinished4 { merged, parked }` are derived from the fold (tasks `Merged`; tasks
`AwaitingInput`) by `report::merged_and_parked`, and are **not** validated by
`check_run_finished`, whose pass rule reads `outcome` and `halted_at` only. Validating them would
be a Class B change to `src/topology/fold/check_end.rs`. Live and replay agree because both read
the fold. Recorded as a reading, not a gap: the derived-outcome guard is what INV-15 names, and
the counts are the report's convenience (`PR31-RUN-FINISHED-COUNTS-NEVER-CHECKED` stands as
filed).

### R6 — the report lists retained candidates refs for every outcome but Complete

`terminal_finalization` (v): candidates refs are pruned at Complete only and "retained and listed
at Halted". The report is written after `run_finished` and before the cleanup steps, so at
Complete it already describes the pruned state (no retained refs listed), and at Halted, Parked
and BudgetExceeded it lists every candidate's ref with its commit. `report.json` is "regenerated,
never read back": the only read is the digest comparison that decides whether to rewrite it
(`TopologyReport::is_fresh_against`), and a report that does not parse is stale.

### R7 — a resume recreates a pruned execution root

R18 is "pruned unless a later resume recreates it". `run_recovery_order` recreates it through
`Worktree.CreateExecutionRoot` when absent, before `reclaim_stale_residue`, with a warning. That
site's declared adjacency is `Before(run_started)` and its fault row `T-RUNSTART`; executing it
inside `T-RESUME` is this reading. The pinned registry (`effects/sequential-registry.json`)
carries the site's two hook-phase entries, keyed by site, phase and order and carrying the site's
own row, each witnessed by
`a_budget_stopped_run_with_a_retained_generation_is_closed_run_ending_and_ends` — the
resume-time execution, which is the one the sequential suites perform. The registry keys an
entry by its coordinate, not by the prefix that executed it, so there is no separate
"resume-time entry" beside a fresh-run one; the reviewed record said there was, when the
registry then carried no entry for the site at all, and that sentence is withdrawn (§13).

### R8 — the live retry path's Close arm scrubs the closed generation

`G4B-O3-LIVE-WORKTREE-MISSING-CLOSE-KEEPS-THE-INTENT`: after the `generation_closed` append
succeeds, `retry_ready`'s `Close` arm removes the closed generation's worktree (when residue, not
absence, failed the verification) and its durable intent through the workspace manager, as
`reclaim_closed_generations` does on resume. The test carries the intents read `G4G` made.

### R9 — the production reader stays at schema 3

`report::topology_status` and `TopologyReport::render` are test-reachable; `status::load` is
untouched. The ST-20 status half ("report.json and status name the run's kind, policy, image
reference, id, and digest") is asserted through them, and `contract_mappings.rs`'s `defer:PR10`
row maps to that test.

### R10 — closure at BudgetExceeded scrubs the closed generations' worktrees

`terminal_finalization`: "Parked and BudgetExceeded runs finalize only terminal items ((ii),
(iii), (iv)) and keep everything resumably_open"; R9's lifecycle: "OpenNoAttempt … closed at run
end", "candidate created or Closed: pruned (forced); intent removed". **Reading:** the worktree
of a generation *closure closes* is a Closed generation's and is pruned with the close, at
BudgetExceeded as at Halted, by `close_run` and not by the finalization step (i) — which at
BudgetExceeded and Parked does not run. Nothing resumably_open is touched: a resume dispatches a
fresh generation at the then-current head (ST-11), never the closed one.

### R11 — the ST-18 matrix is every finalization cell with an injected error return, and one real kill inside finalization

"kills after run_finished, after report write, and between every cleanup effect converge to the
outcome equations". An injected `Injection::Error` at a site's hook ends the command at that
point, and the durable state it leaves is the same state a kill there leaves — no in-memory
state survives either to the next resume. The matrix (`kill_after_report_before_each_cleanup_step`)
is derived from the ordered list of what finalization does to a run planted with every kind of
residue (`finalization_effects`: the report, every cleanup step's effects site by site, then the
run lock's release) and drives both hook phases of every effect's site — **24 cells at Complete,
22 at Halted, asserted as exact counts** — and at every cell `assert_finalization_order` reads
what has and has not disappeared at the fault: every effect before the faulted site done, the
faulted site's own effect done only when the fault came after it, nothing later. `Lock.Release`
is a cell because `finalize_if_finished` releases the run lock through the hooked funnel rather
than leaving it to the guard's drop, which no hook sees; a fault at the release is absorbed by
`RunLock::release`, so a resume faulted there still reaches the refusal. Beside the matrix, one
real process death inside finalization:
`a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume`
kills its child (`finalization_kill_child`) at `Worktree.RemoveExecutionRoot`'s after phase —
after the last cleanup step's effect, before the lock's release and the guards' drop — and the
next resume finds the report current, nothing left to prune, releases the lock through the funnel
and refuses. The reviewed record claimed a real kill inside finalization while the only kill
child died at `Event.Append/Written` before finalization began, and "every finalization site and
phase" while two Before cells and both `Lock.Release` phases were missing and the matrix was
closed by `cells >= 18`; both claims are withdrawn and replaced by what is measured (§13).

### R12 — `topology_status` takes a `StablePrefix`, not bytes

`events::log::tests::the_stable_prefix_barrier_is_the_only_way_a_log_becomes_a_topology_fold`
pins every production region that names `TopologyFold`, and the first cut's `topology_status`
built a fold from the log's bytes for a status reader — a second route from bytes to a fold
beside the barrier. **Reading:** a reader takes the barrier's output. `topology_status(run_id,
&StablePrefix)` derives the report from `prefix.fold()` and `prefix.events()`, so the only way
a log becomes a fold is still `establish_stable_prefix`; the three new modules (`closure`,
`finalize`, `report`) are in the census's control list as readers, none of which constructs a
fold.

### R13 — every `Closed` settlement scrubs its slot at the loop

Found by the ledger at Parked (§6): the live park settlement closed beta's generation in the
fold and left its worktree and intent, and finalization reported the execution root not
removed. R9's lifecycle — "candidate created or Closed: pruned (forced); intent removed" — is
not conditional on which transition closed the generation, and the packet's Parked column says
"pruned". **Reading:** `TopologyRun::settle` scrubs the slot after the `attempt_finished` append
for every `Closed` transition (retry, escalation, deferral, park, terminal failure), in the
order `G4B-O3` fixed for the retry path's `Close` arm: the durable close first, the scrub after,
so a kill between the two leaves a closed generation the sweep reclaims and never a scrubbed
slot the fold still holds open. Promotion was already scrubbing after `task_candidate_created`;
retained settlements keep their worktree by design (R9: "RetainedIdle: resumably_open during a
live run") and closure or the next recovery scrubs them.

### R14 — ST-07 is claimed over the full sequential inventory

"Site bijection complete for the range." The reviewed record read the range as the twelve
run-end sites and left `Lock.Release`, every Object site, `Event.OpenLog`, `Event.AppendFirst`,
`Event.AppendInformational`, `Event.ProvePrefixStable` and the residue and fast no-execution
entries out of the check; the review refused that reading, and it is withdrawn. **The inventory
is every Topology- and Shared-scoped site the enums generate — 68 sites** (`coverage::inventory`) —
and the merge check (`st07_the_sequential_range_is_a_bijection_over_the_exported_observations`)
holds the pinned document to a fresh export over the **checked inventory of 66**: three
coordinates are declared unobservable with a reason held to the code by
`every_declared_unobservable_coordinate_has_its_reason_in_the_code` — `Report.Write` as a whole
(no funnel names it: `report.json` is written through `RunDir.WriteReport`, `PR3-REPORT-DOUBLE-NAME`),
`Process.Spawn`'s two hook phases (the process funnel consults `SpawnHooks::point` at its
parent-side points only), and `Process.Terminate` as a whole (`kill_tree` consults no hook). The
check excuses only those coordinates, requires that none of them was observed, holds the export's
fast sequences to the no-execution record, every claim this host requires to the test it names,
and every recovery-proven entry's N to the declarations. The document has 174 entries: both hook
phases of every checked site, every point of the four schema-4 event sites (`Event.Append`'s
five, `Event.OpenLog`'s `SyncPrefix` and torn-tail points among them) in every mode each supports
and the `IdUnread` points, `Process.Spawn`'s parent-side points, nine recovery-proven residue
entries (one per residue class of every site that registers one, with the synthetic half from
`effects/residue-synthetic.json` and the sampling half from `effects/residue-histogram.json` and
`effects/residue-histogram-sequential.json` — the five sites PR5's sampler never ran are
kill-sampled by `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered`),
and three fast no-execution entries (`Worktree.AddStaging`, `Ref.PinPrepared`,
`Object.ProposalCherryPick`, cited to
`fast_path_publishes_exact_candidate_without_staging_or_proposal_object`). Every harness adapter
exports its observations (`src/observations.rs`, on drop and just before a kill is handed back), so
the kill children of the recover, settle, candidate, create, prelock and coverage suites are
handed `UPSTROKE_HOOK_OBSERVATIONS` and their kills are claimed — `settle::tests::settlement_kill_child`
for `Event.Append`'s `Synced` point in kill mode among them. The coordinates no suite executed
under the production adapters have witnesses of their own in `coverage/tests.rs` (§7).

### R15 — R20 is observed by classification

The credential volume is operator-owned: "never created or pruned by a run; exempt from
creation-site mapping by classification". **Reading:** the ledger observes it as two facts
rather than as a runtime probe — no site in `EffectSiteId::all()` names a volume, and the
volume map `run_started` recorded is the one the fold ends with — because the fixtures drive
the loop through the host-shaped recording runner and no container runtime persists across a
test to be asked.

### R16 — the census runs at the packet's bounds, and is truncated at a state ceiling

`bounded_census.bounds` names three originals, two repairs, two lineages, two generations per
task, two attempts per generation, four integration sequences, two verification defers, two open
questions, one review pass, two resumes, and the chain, fan-out and diamond shapes. The reviewed
record kept PR3's two-original fixture and said so; the review refused that, and the fixture is
raised: three originals in three plan shapes (`PlanShape`: the chain, the fan-out the shared
census explores, and the join — a task after two independent ones, which is the diamond's join
with three originals), the generator offering every event class under the bounds (rejections
registering the repair the production repair module derives, a hand spawn, questions, deferrals,
resumes where a run resumes, the run-ending close on the closure's own precedence), the arm
enumeration read from the production dispatch's source
(`every_plan_transition_arm_is_executed_by_the_census`), and every declared bound held by
equality (`every_declared_dimension_is_reached_at_its_bound`). **Reading:** the space under these
bounds is orders of magnitude larger than any ceiling a test suite can explore, so the shared
census is a breadth-first prefix truncated at 20,000 states and says so (`Census::truncated`,
set at the state ceiling and at the trace ceiling alike), and the bounds the prefix cannot reach —
the fourth integration sequence, the second repair — are reached by a deep census seeded from two
merged originals, which closes under its ceilings. Every assertion over the shared census is over
its explored set, and nothing here reads as closure of the bounded space (§11).

### R17 — the NoRunFinished witness is a command ended by the append-error protocol

"A command ended by the append-error protocol leaves exactly this shape with the surviving
prefix as the fold." **Reading:** the NoRunFinished ledger is driven, not planted: beta's third
append (the first after `attempt_started` is durable) errors at `Written`, the fold is
poisoned, the command ends, and the surviving prefix holds an in-flight generation with its
worktree and intent and the execution root. The next incarnation settles the attempt
interrupted and completes the run, and the ledger balances at Complete there too. The
NoRunFinished ledger compares two observations rather than one with itself: the live one
(`observe_live`, taken while the process still holds the run, its lock and its fold) and the
after-drop one (`observe_after_drop`, from the bytes and the OS once it has let go), so each
`Retained` requirement reads a before and an after.

## 4. The refusals PR9 left, and what replaced each

| PR9 | PR10 |
|---|---|
| `LoopBranch::Closure => Disposition::RefusedByCheckpoint` — the checkpoint refused `Step::Closure` and `Admitted` had no closure variant | `Admitted::Closure(DerivedOutcome)` crosses; `TopologyRun::close_run` performs the closure procedure (`closure.rs`), appends `run_finished` and finalizes (`finalize.rs`). `Step::NotStarted` and `Step::Finished(outcome)` are the two `Step` variants the checkpoint now refuses beside `Poisoned` (eleven, eight, three) |
| `Progress::Blocked { questions }` when nobody answered the hard block | The hard block falls through to closure (R2): `Progress::Finished { outcome: Parked, .. }` |
| `refuse_if_finished` — recovery step (b) refused a Complete or Halted run and wrote no report | `finalize_if_finished` finalizes (report regenerated when missing or stale — its stored digest not the digest of its own bytes, or its outcome or runner not the derived report's; the cleanup steps in the packet's fixed order; the run lock released through its funnel) and then refuses, with what it did in the message |
| no `report.json` for schema 4; `contract_mappings.rs` carried `defer:PR10` for ST-20's report/status naming | `TopologyReport` (INV-13's projections, the runner identity in full, the retained candidates refs, the integration ledger section) and `topology_status`; the mapping row names `finalized_report_names_runner_identity` |
| the five pins of the refusal listed in the brief's §4 | re-pinned in `303c0997`: `every_branch_states_what_this_build_does_with_it` (no branch refused), `an_unstarted_run_selects_nothing` (`Step::NotStarted`), `a_breach_appends_budget_exceeded_and_integration_and_run_end_cross_the_checkpoint` (its tail admits the closure), `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` (the second step ends the run), `resume_finalizes_halted_then_refuses` (finalize-then-refuse assertions) |

Steps (2)–(4) of the closure procedure — in-flight cancellation, promotion and publication
completion inside closure — remain refusals, naming PR11 (R1).

## 5. Class B and Class C

**No Class B fold change and no Class C wire change.** `src/topology/**` was touched only in
Class A: `fold/predicates.rs` gains the read-only reader `TopologyFold::task_count`;
`census.rs` gains tests, its summary export writes through the fixture's `write_file`, and in
the repair round the census itself gains a per-state index of its transitions (`Census::outgoing`)
and interns its labels and refusal reasons (`Arc<str>`) — the explorer's own bookkeeping, read by
no production module. The new classifier lives in the engine (`engine::topology::reachability`)
and is called from the census tests.

**Two instruments are touched, and they are declared here for the delegation rule.** Both are
CI-contract tests under `src/effects/`, which `CLAUDE.md` names among the known instruments.
`src/effects/tests/contract_mappings.rs` has one row changed: the `defer:PR10` marker that stood
for ST-20's report/status naming is replaced by the name of the test that now exists,
`finalized_report_names_runner_identity`, which the mapping census requires to be defined
exactly once under `src/`. `src/effects/tests/cfg.rs` has two entries added to
`WHOLE_FILE_TEST_MODULES`, `engine/topology/coverage/tests.rs` and
`engine/topology/ledger/tests.rs`, the list the effects census resolves whole-file test modules
from (a census skipping only the ones named `tests.rs` by file name would leave the rest inside
its domain; the test that holds the list to the declarations failed until they were named). Each
change narrows what a census accepts and cannot make a check report success without doing its
work; under the first limb the files nonetheless govern what everyone may land, so the
classification is stated rather than assumed, and the merge — under the standing delegation of
2026-09-12 — is not this session's (§12).

**Two more governed lists are touched by the repair round, declared on the same terms.**
`FOLD_MENTIONS` in `src/events/log/tests.rs` — the census that pins every production region
naming `TopologyFold`, so the stable-prefix barrier stays the only route from bytes to a fold —
gains `src/engine/topology/seams.rs`: the hooks bundle's `folded` hook takes the fold the emitter
has just applied so the live projection can be recorded at every append (§13, record P1-2); the
bundle constructs no fold. `EXCLUDED` in
`src/runner/container/resolve/tests.rs::no_module_outside_the_container_runner_writes_a_container_intent`
— the census that no module outside the container runner names a container intent — gains
`src/engine/topology/coverage/tests.rs`, the suite that drives the container launch funnels
through the production adapter for ST-07, beside the other test suites it already excludes;
the census still holds every production module. Neither list is under `src/effects/`; each
governs what everyone may land in the same way, so the classification is the same as above.

## 6. Findings

**Closed by this slice (the files deleted; the ledger rows are in the pull request body).**
The five were deleted at `reviews/findings/`; master moved the ledger to `findings/` after the
round-1 review (#276), and the merge commit `6fb5052e` deletes them at `findings/` — including
`findings/P2_correctness_202608310806_before-phase-names-only-the-sites-own-artifact.md`, which
master had also edited (its `location` filled in) and which this slice closes, so master's edit is
deleted with it (§13).

- `PR7-R4-LOOP-004` — "closure derives NotEnding for a budget-stopped run". `closure::
  ending_outcome` reads the halt and the budget stop before the derivation (R3), closure closes
  the retained generation `RunEnding { BudgetExceeded }`, and the run ends. Witness:
  `a_budget_stopped_run_with_a_retained_generation_is_closed_run_ending_and_ends`, whose
  diagnostic names the retained generation. Deleted in `09905d43`.
- `G4B-O3-LIVE-WORKTREE-MISSING-CLOSE-KEEPS-THE-INTENT` — the live `Close` arm now scrubs the
  closed generation's worktree and intent after the `generation_closed` append (R8). Witness:
  `a_live_worktree_missing_close_reclaims_the_generations_worktree_and_intent`. Deleted in
  `09905d43`.
- `G4-O1-REJECTION-KEEPS-CANDIDATES-REF-UNWITNESSED` — closed by the first session
  (`a7e497a1`, `e262fc75`): the terminal shape table reads the rejected candidate's candidates
  ref and object on both rejection arms. **M22 and M23 re-run in this session for the record:**
  each mutation — a `Ref.DeleteCandidatesRef` deletion added after the `merge_rejected` append
  of the Conflict arm (M22) and of the CodeRejected arm (M23) in `integrate.rs` — fails
  `terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` at the ref
  read ("after `merge_rejected` the rejected candidate's candidates ref still names the
  candidate", the arm named). The reviewed record cited `rc=101` from logs that capture no exit
  code; both mutations are now recipes of the mutation runner (`m22-conflict-deletes-candidates-ref`,
  `m23-code-rejection-deletes-candidates-ref`, §8), whose summary records the process exit
  code beside the assertion — `~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/mutations/summary.tsv`, the two
  logs beside it — and the source is restored from a `cp` backup and compared afterwards.
- `SWEEP-BIJECTION-005` — "sampling N is the registry's own number". The frozen N now has a
  home outside the entry that cites it: `effects/residue-classes.json`, the declarations half
  the artifacts test pins, read by `coverage::frozen_sampling_n`; `coverage::check_frozen_n`
  compares every recovery-proven entry's `n` against it, the merge check runs it over the
  document, and `a_recovery_proven_entrys_n_is_held_to_the_declarations` refuses an entry
  citing 1 where the declarations freeze 8. `check_evidence` itself (frozen) is unchanged; the
  finding's own text places the freeze check in "the gate that reads registry.json across
  runs", which is where it is. Deleted in `7dfbf8b0`.
- `PR3-BEFORE-PHASE-SCOPE` — "Worktree.Add/Before is empty although R9 already holds the
  intent". Closed on the tree rather than by this slice: the residue authority's
  `BeforeState::PrecursorDurable` gives `Worktree.Add`'s and `Worktree.AddStaging`'s before
  phase the rows of `Present` (R9, R10) with the precursor's own words, asserted by
  `effects::tests::the_self_test_fixture_varies_every_field_it_reads`; PR10's registry document
  enters its sites through the same authority. Deleted in `7dfbf8b0`.

**Found by this slice and fixed in it** (`3c411b21`; no finding file was opened, since the
fix and its regression test landed in the commit that found it, and the pull request body
carries the row): a live `Closed` settlement — deferral, park, escalation, retry, terminal
failure — left the generation's worktree and intent (R9) for the next resume's sweep, and the
ledger at Parked reported `Progress::Finished { execution_root_removed: false }` with
`tasks/k1-g0` and its intent still in the root. Same family as `G4B-O3`. `TopologyRun::settle`
scrubs the slot after the `attempt_finished` append for every `Closed` settlement (R13);
regression test `a_closed_settlement_scrubs_the_generations_worktree_and_intent`, and the
Parked ledger now reports the root removed.

**Re-checked and re-deferred with a new guard** (the files edited, each with the re-check
appended): `PR3-REG-001-CONDITIONAL` and `PR4-REG-001-STILL-EQUIVALENT` — still conditional,
`EffectSiteId::observable_orders` returns one order or none by construction and the sequential
registry keys its entries the same way; the guard passes from the PR4–PR10 implementer to the
change that first gives a site two orders. `PR3-FRAMEWORK-SILENT-1` to `-5` — the residue
authority still states each derivation as code and as a test and the packet still does not;
what would close them is a design sentence, and `DESIGN.md` is the owner's, so the guard passes
to the owner. Each of the seven files carries `reviewed_sha: 7dfbf8b0cb1203671300a097ca9be1c8107732a3`
(the notes-and-findings head the re-check was made at) and a `location` naming the line of the
rule it is about at that SHA — `src/topology/effects.rs:338` (`observable_orders`) for the two
registry-equivalence findings; `src/topology/effects/residue_authority.rs:299` (`Removed`), `:288`
(`NoEffect`), `:1053` (`ContainerSite::after_effect`), `:505` (`NextOpenConverges`) and `:510`
(`AmbientHandleTerminates`) for `PR3-FRAMEWORK-SILENT-1` to `-5` — and the body's ledger carries a
row per finding with the same facts, which the reviewed body lacked.

**Not touched:** `PR8-R2-SPEND-REPLAY`, `PR8-CRASH-002`, as the brief directs.

## 7. The packet's tests, and where each is in the tree

Every name below is a test in `src/engine/topology/recover/tests.rs` unless a module is given.

| Packet | Tree |
|---|---|
| T-FINISH / ST-17: `kill_inside_closure_recovers` | same name; its child `closure_kill_child` is killed at `Event.Append`'s `Written` point in both durable shapes (torn, complete-unsynced) through `TopologyRun::step`, and the parent's resume completes the closure |
| `append_error_inside_closure_ends_command_and_resume_completes_closure` | same name |
| `run_finished_complete_refused_with_queued_candidate`, `run_finished_parked_refused_with_admissible_work`, `run_finished_parked_or_complete_refused_while_deferred_items_exist`, `run_finished_halted_and_budget_exceeded_accepted_with_deferred_items`, `run_finished_budget_exceeded_refused_after_halting_drain_settlement`, `run_finished_halted_accepted_after_declined_verification_park`, `over_budget_prefix_without_budget_exceeded_is_not_ending`, `replayed_conflicting_outcome_refused` | same names, each at the fold and at the loop where the loop can reach the shape |
| (the two findings' witnesses) | `a_budget_stopped_run_with_a_retained_generation_is_closed_run_ending_and_ends`, `a_live_worktree_missing_close_reclaims_the_generations_worktree_and_intent` |
| T-FINISH's closure prefix with a fault inside it (crash P2-5) | `a_fault_between_the_closure_close_and_its_scrub_is_reclaimed_by_the_next_resume`: the retained generation's `generation_closed` durable, the scrub refused at `Worktree.Remove`'s before phase, the next resume reclaiming the worktree and intent, keeping every candidates ref, clearing the epoch's stop, and meeting the ceiling again |
| the two ST-17 witnesses that plant after the resume (crash P1-3) | `run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` and `run_finished_budget_exceeded_refused_after_halting_drain_settlement` plant their triggering state through `plant_live` — the production emitter on the resumed handle — so the closure meets the deferred task and the budget stop rather than what `run_resumed` clears |
| T-FINALIZE / ST-18: `kill_after_run_finished_before_report`, `kill_after_report_before_each_cleanup_step` | same names; the matrix is every cell of `finalization_sites` — both hook phases of every effect's site, `Lock.Release` included, 24 at Complete and 22 at Halted as exact counts — each driven with an injected error return and `assert_finalization_order` reading what is and is not gone at the fault (R11); plus `a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume`, whose child `finalization_kill_child` dies inside finalization |
| `resume_finalizes_then_refuses` | `resume_finalizes_halted_then_refuses` (the name the tree has carried since PR7, rewritten from the refusal into finalize-then-refuse assertions) |
| `halted_report_lists_candidate_refs`, `answer_files_untouched_by_finalization`, `private_records_untouched_by_finalization`, `finalized_report_names_runner_identity` | same names |
| T-ANSWER: `late_answer_before_halting_settlement_is_inert_and_retained`, `late_answer_after_finalization_is_inert_and_reported_not_live` | same names; the second publishes through the funnels `upstroke answer` delegates to and reads liveness by the probe the command reports (`src/answer.rs`'s own test is the command's) |
| ST-20 report/status naming | `finalized_report_names_runner_identity`; `contract_mappings.rs` names it |
| acceptance subset [0] | `max_parallel_one_completes_a_two_task_chain_with_one_linear_commit_per_task_and_the_checkout_unchanged` |
| projection equivalence (Q1) | `projections_are_equal_between_live_and_replay_at_every_prefix`: the report derived from the live fold at every successful append (the hooks bundle's `folded` hook, recorded by `HarnessTopologyHooks::live_projections`) equals the report derived from a replay of that prefix, and every durable prefix this process appended had such a live comparison; `live_vs_replay` and `assert_live_equals_replay` run after every step beside it |
| ST-09 / ST-10 (durable rows; the process-local rows as the loop reports them) | `the_ledger_balances_at_complete`, `the_ledger_balances_at_parked`, `the_ledger_balances_at_halted`, `the_ledger_balances_at_budget_exceeded`, `the_ledger_is_resumably_open_when_no_run_finished_and_balances_after_the_resume` (a live and an after-drop observation); `a_closed_settlement_scrubs_the_generations_worktree_and_intent` (the settlement's append durable before the scrub's first effect, read from one timeline across the Event and effect families, `OrderedHooks`); `ledger::tests::every_outcome_equation_names_every_row_once`, `ledger::tests::requirements_read_before_and_after_as_named`, `ledger::tests::a_rows_named_facts_are_each_consumed_by_its_verdict` |
| ST-14 (PR10's part) | `topology::census::tests::the_census_runs_at_the_packets_bounds_and_says_where_it_stopped`, `every_declared_dimension_is_reached_at_its_bound`, `every_plan_transition_arm_is_executed_by_the_census`, `every_plan_shape_is_explored`, `every_explored_state_classifies_and_the_classification_is_the_same_live_and_on_replay`, `every_fault_rows_durable_prefix_is_a_reachable_state_classified_as_its_resume_action` (row membership from the fold alone, `reachability::rows_reached`), `a_rejection_and_its_repairs_dispatch_are_reachable_prefixes_classified_as_tabled`, `a_retained_generation_and_its_retry_are_reachable_prefixes_classified_as_tabled`, `run_resumed_is_accepted_with_an_identical_runner_and_refused_with_any_different_field`, `the_census_summary_names_every_fault_row_and_serializes` |
| ST-07 over the inventory | `engine::topology::coverage::tests::the_inventory_is_claimed_at_every_required_phase_on_both_hosts_or_declared`, `every_claim_names_a_test_defined_exactly_once_under_src`, `the_sequential_registry_is_pinned`, `every_declared_unobservable_coordinate_has_its_reason_in_the_code`, `a_recovery_proven_entrys_n_is_held_to_the_declarations`, `an_observation_record_merges_by_the_larger_count_and_round_trips`; the witnesses for the coordinates no other suite executes under the production adapters: `the_question_and_answer_funnels_execute_both_phases_under_the_production_adapter`, `every_error_return_point_of_the_event_funnels_fires_under_the_production_adapter`, `every_kill_point_of_the_event_funnels_kills_the_child_under_the_production_adapter` (child `event_kill_child`), `every_kill_point_of_the_process_funnel_kills_the_child_on_this_host` (child `spawn_kill_child`), `the_ambient_job_join_error_return_point_fires_under_the_process_funnel` (Windows), `the_container_launch_funnels_execute_both_phases_under_the_production_adapter`, `sampled_git_child_kills_of_the_remaining_residue_sites_are_classified_and_recovered`; ignored: `st07_the_sequential_range_is_a_bijection_over_the_exported_observations` (the merge check), `write_the_sequential_registry` (regenerates the pinned document) |

## 8. Measurements

Every artifact this section cites is under `~/pr10-evidence/` on this box, beside the gate logs
of §9. The repair round's are under `~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/` — the round's code head,
the last commit before this record's and the body's — and `~/pr10-evidence/r1/`; the scratch
copies the sessions wrote are not cited. The measurements of the reviewed head
(`~/pr10-evidence/7dfbf8b0/`, `2df22b78/`) stand as history and are superseded below.

**The ledger, observed at every outcome** (`~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/ledger/`, six rendered
tables written through `UPSTROKE_LEDGER_EXPORT` by the five ledger tests in the full-suite run
`full-suite-export.log`; each row's before and after facts, the expectation and the evidence,
part by part). Every row's verdict consumes every fact its `resource_accounting` cell names:
R14 one monotone requirement per consumed counter (the merge sequence, task keys, display ids,
generations, attempts, lineage indexes, repair budget units, verification defers, override
slots); R21 one per persistent output (the integration ref, `events.jsonl`, the normalized plan,
the report — `Any` only mid-run — the question, answer and `.partial` files, the marker, the run
and cleanup lock files, the owner and commit records, the private artifacts); R27 one per object
accounting (no released object missing, the whole store monotone and nothing of it missing, the
`fsck` unreachable count monotone), read from an already-unreachable object the fixture plants
(`plant_unreachable_object`) as well as from the released ones. `ledger::tests::a_rows_named_facts_are_each_consumed_by_its_verdict`
holds `check` to reporting a disagreement on the named part, and the two mutation witnesses of
§13 (`ledger-report-skipped`, `ledger-prunes-unreachable`) show the Complete ledger failing when
the report is not written and when finalization prunes Git's own residue. The facts the tables
show:

| Outcome | What the ledger shows |
|---|---|
| Complete (`ledger-complete.md`) | R11 `Present(2)` before finalization, `Absent` after; the two released commits and every object the store held before still in it, the planted unreachable object included (R27 balanced); R21's report, plan, records and lock files present; R6, R15 zero; R18 absent; R17 absent after the drop |
| Parked (`ledger-parked.md`) | R15 `Held(1)` (beta's question); R11 `Present(1)` retained (alpha's ref at its commit); R6 zero (alpha's position consumed by `task_merged`); R9, R18 absent |
| Halted (`ledger-halted.md`) | R11 `Present(2)` retained (the parked candidate's ref and the published beta's); R6, R15 zero (the declined park consumed both); R10, R12 absent before and after — the resume pruned the planted terminal's proposal pin and staging before the loop ran |
| BudgetExceeded (`ledger-budget-exceeded.md`) | R6 `Held(1)`, R7 `Held(1)`, R11 `Present(1)` (beta's queued candidate, resumably open); R23 absent (the candidate-prepared pin pruned); R5 zero; R18 absent |
| NoRunFinished (`ledger-no-run-finished.md`) | the live observation against the after-drop one: R1 `Held(1)` (beta in flight), R9 `Present(1)`, R18 present, R11 `Present(1)`, R17 held live and absent after the drop (the lock went with the process), the invocation ledger balanced and no entitlement held; R23 `Present(1)` is the planted candidate-prepared pin of alpha, which the resume that follows prunes at Complete |
| Complete after that resume (`ledger-no-run-finished-then-complete.md`) | R1, R9, R11 zero or absent; R14's every counter monotone across the incarnation |

**ST-07 over the full inventory** (`~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/st07-summary.json`,
`observations/`, `st07-merge-check.log`): 320 observation records from the
full-suite run with `UPSTROKE_HOOK_OBSERVATIONS` set (314 funnel executions; the
kill children of every suite included, since every harness adapter exports on drop and before a
kill); the merge check over the checked inventory of 66 sites at host `unix`:
4 findings, every one a declared-unobservable coordinate and excused, none of
them observed; 174 entries, 157 claims each witnessed by the test
it names (28 distinct tests), the fast sequences `s0` and `exact-base-fast` the ones the
no-execution record names, every recovery-proven N the declarations' eight. The pinned document
is `effects/sequential-registry.json`; `the_sequential_registry_is_pinned` holds it to the
generator with the machine-varying sampling histograms set aside; the two histograms it embeds
are tracked files each sampler rewrites on every run — which class a sample lands in is a race
between the kill and Git, as PR5's sampler already records — so a full-suite run leaves
`effects/residue-histogram-sequential.json` modified and the pin compares everything but those
counts.

**The census** (`~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/census-summary.json`, written by
`the_census_summary_names_every_fault_row_and_serializes` in the same full-suite run):
20,000 states, 5,222,686 offers of which 93,222 accepted
and 5,129,464 refused, **truncated** at the state ceiling (§3 R16), the classification
equal live and on replay at every state; outcomes reached: budget exceeded 4,437, complete 819, halted 1,943, parked 942, not ending 11,859; 21
distinct resume actions, `finalize then refuse` for every Complete (306) and
Halted (650) state; every one of the nineteen fault rows inside the fold
reached from the shared census on its own (`T-ANSWER` 2,327, `T-ATTEMPT` 1,040, `T-CAND-OBJ` 1,040, `T-CAND-REF` 519, `T-DISPATCH` 1,675, `T-FAILED` 6,436, `T-FAST` 476, `T-FINALIZE` 956, `T-FINISH` 6,539, `T-PREPARED` 578, `T-PROPOSAL` 525, `T-REJECT` 8,632, `T-REPAIR-DISPATCH` 900, `T-RESUME` 1,920, `T-RETAINED` 1,295, `T-RETRY` 281, `T-RUNSTART` 6, `T-SCRUB` 3,572, `T-VERIFY` 1,050), the two outside the fold
(`T-APPEND`, `T-CONTAINER`) said so. The bounds the summary reports are the packet's
(`CensusBounds::default()`: 3, 2, 2, 2, 4, 2, 2, 1, 2; `max_trace` 48, `max_states` 20,000). The
deep census reaches the fourth sequence and the second repair and closes under its ceilings
(`every_declared_dimension_is_reached_at_its_bound`). The census suite runs in 26 s
(`~/pr10-evidence/r1/census/suite-run-3.log`); it ran 549 s before `Census::outgoing` read a
per-state index (`suite-run-2.log`), the transition-table and deferred-state tests scanning
every transition once per state.

**Mutation witnesses** (`~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/mutations/`: `summary.tsv` with the
process exit code, one log and one diff per recipe; the runner is `~/pr10-evidence/r1/mutations.sh`,
which patches a `cp` backup of the file, runs the named test through the wrapper, restores the
backup and compares). Every recipe the review gave as "reasoned", executed:

| Recipe | Mutation | Test | Exit | Result |
|---|---|---|---|---|
| `ledger-report-skipped` | `if false { rundir::write_report(…) }` at the finalization's report write (crash P1-1's recipe) | `the_ledger_balances_at_complete` | 101 | **killed** (expected fail); `ledger-report-skipped.log` |
| `ledger-prunes-unreachable` | `git prune --expire now` run by finalization before it reports (an already-unreachable object deleted, R27) | `the_ledger_balances_at_complete` | 101 | **killed** (expected fail); `ledger-prunes-unreachable.log` |
| `projection-no-live-snapshot` | the emitter never calls the `folded` hook (no live projection recorded) | `projections_are_equal_between_live_and_replay_at_every_prefix` | 101 | **killed** (expected fail); `projection-no-live-snapshot.log` |
| `projection-stale-prefix` | the emitter reports the live projection from the prefix before the applied event | `projections_are_equal_between_live_and_replay_at_every_prefix` | 101 | **killed** (expected fail); `projection-stale-prefix.log` |
| `st18-order-swapped` | `CleanupStep::ORDER` with snapshots ahead of task worktrees (crash P1-2's recipe) | `kill_after_report_before_each_cleanup_step` | 101 | **killed** (expected fail); `st18-order-swapped.log` |
| `st17-halted-with-backoff-refused` | `refuse_unclosable` refuses a halted run with a pending backoff (crash P1-3's recipe at `closure.rs:38`) | `run_finished_halted_and_budget_exceeded_accepted_with_deferred_items` | 101 | **killed** (expected fail); `st17-halted-with-backoff-refused.log` |
| `st17-budget-outranks-halt` | `ending_outcome` reads the halt only with no budget stop (crash P1-3's recipe at `closure.rs:17`) | `run_finished_budget_exceeded_refused_after_halting_drain_settlement` | 101 | **killed** (expected fail); `st17-budget-outranks-halt.log` |
| `classifier-drops-beta` | `view` pushes every open generation but beta's (record P1-4's recipe at `reachability.rs:119`) | `every_fault_rows_durable_prefix_is_a_reachable_state_classified_as_its_resume_action` | 101 | **killed** (expected fail); `classifier-drops-beta.log` |
| `settle-scrub-removed` | the scrub after a `Closed` settlement removed (the pre-repair shape, §6) | `the_ledger_balances_at_parked` | 101 | **killed** (expected fail); `settle-scrub-removed.log` |
| `settle-scrub-before-close` | the scrub moved ahead of the `attempt_finished` append (record P2-5's recipe) | `a_closed_settlement_scrubs_the_generations_worktree_and_intent` | 101 | **killed** (expected fail); `settle-scrub-before-close.log` |
| `m22-conflict-deletes-candidates-ref` | a `Ref.DeleteCandidatesRef` deletion after the Conflict arm's `merge_rejected` (G4 M22) | `terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` | 101 | **killed** (expected fail); `m22-conflict-deletes-candidates-ref.log` |
| `m23-code-rejection-deletes-candidates-ref` | the same deletion after the CodeRejected arm's `merge_rejected` (G4 M23) | `terminal_shape_coverage_table_drives_every_shape_and_each_converges_on_replay` | 101 | **killed** (expected fail); `m23-code-rejection-deletes-candidates-ref.log` |
| `census-bound-not-reached` | the generator stops one integration sequence short of the declared bound (contract P1-2's equality recipe) | `every_declared_dimension_is_reached_at_its_bound` | 101 | **killed** (expected fail); `census-bound-not-reached.log` |
| `census-trace-ceiling-silent` | the trace ceiling stops expansion without setting `truncated` (contract P1-2's `shallow.truncated()` recipe) | `a_census_that_hits_its_ceiling_says_so` | 101 | **killed** (expected fail); `census-trace-ceiling-silent.log` |

**The closed-settlement finding, before and after** (§6): the pre-repair shape is what the
`settle-scrub-removed` recipe reproduces — with the scrub after a `Closed` settlement removed,
`the_ledger_balances_at_parked` fails at `finished(Parked, 0, true, false)` with the execution
root still holding `intents/tasks.k1-g0.intent` and `tasks/k1-g0`
(`~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/mutations/settle-scrub-removed.log`); with it, the test reports
`finished(Parked, 0, true, true)` and the root gone.

**Counts.** The full-suite run at `36447851efe2ea390b7c68cfe2a2fa601ddd4a08` (`full-suite-export.log`, the test leg's
command through the wrapper): 2,542 library tests passed, 1 failed, 72 ignored (of 2,615 in the library binary); the binary's tests did not run because the library leg failed. The one failure is `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` (a PR8 sampler, not a PR10 site), refused at its resume because the run's cleanup lease was held — "still has a process of its own alive in worktree /tmp/upstroke-pr7e-3700982-sample-1-177/repo -- an agent-cleanup reaper…" — the inherited-descriptor window under the parallel suite that §9 describes; it passes alone on the same tree (`cherry-pick-sampler-alone.log`), and the gate run at the head the body records is the body's to report. The gate's test leg at `2df22b78`: 2,524
library tests passed, 0 failed, 65 ignored (`~/eight-logs/2df22b7/03-test.log`). At `61ec7587`
the same leg reported 2,473 passed, 10 failed, 62 ignored (`~/eight-logs/61ec758/03-test.log`);
the reviewed record attributed 2,443 to that head, which is G4's figure at `7e0110a1`
(`~/eight-logs/7e0110a/03-test.log`: 2,443 passed, 0 failed, 44 ignored). The Windows guest: §10.

## 9. The ten gates on this box

Every run through `~/bin/w1-eight-iso /srv/worktrees/pr10` (nine commands on the worktree's
private target, serialized by the box-wide lock) plus `bash .github/scripts/test-pr-ready-audit.sh`.

| Head | Result | Logs |
|---|---|---|
| `7dfbf8b0` (notes and findings) | **FAIL** at 03 test: five tests (below); the other eight and the tenth (`test-pr-ready-audit: ok`) pass | `~/eight-logs/7dfbf8b-failed-20260913T000035Z/` |
| `2df22b78` (the two censuses named, the bounded wait) | **ALL 9 PASS** (fmt 1 s, clippy 21 s, test 44 s, msrv 15 s, release-record, pr-policy 9 s, pr-ledger, docs-consistency, internals-notes 1 s); `test-pr-ready-audit: ok` | `~/eight-logs/2df22b7/` (copied to `~/pr10-evidence/2df22b78/eight-logs/`) |
| `a0808bf3` (this record) | first run **FAIL** at 03 test on one test, `runner::container::tests::real_docker_returns_both_streams_of_a_container_separately` ("still running after 200 observations", the box's known Docker flake at `src/runner/container/tests.rs:3226`, `PR274-DOCKER-*`; the diff from `2df22b78` is this file and `docs/internals/engine/topology/recover/tests.md`), 2,523 passed; the tenth `ok`; rerun: the body | `~/eight-logs/a0808bf-failed-20260913T001025Z/` |
| `aaebf8df` (the round-1 reviewed head) | **ALL 9 PASS** on the second run; the first failed the two pre-filed flakes | `~/eight-logs/aaebf8d/`, copied to `~/pr10-evidence/aaebf8df/eight-logs/` |
| `36447851efe2ea390b7c68cfe2a2fa601ddd4a08` (the repair round's code head) | the test leg's command, run for the evidence exports rather than through the gate runner: 2,542 library tests passed, 1 failed, 72 ignored (of 2,615 in the library binary); the binary's tests did not run because the library leg failed. The one failure is `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered` (a PR8 sampler, not a PR10 site), refused at its resume because the run's cleanup lease was held — "still has a process of its own alive in worktree /tmp/upstroke-pr7e-3700982-sample-1-177/repo -- an agent-cleanup reaper…" — the inherited-descriptor window under the parallel suite that §9 describes; it passes alone on the same tree (`cherry-pick-sampler-alone.log`), and the gate run at the head the body records is the body's to report. | `~/pr10-evidence/36447851efe2ea390b7c68cfe2a2fa601ddd4a08/full-suite-export.log` |
| the head the pull request body records | the body | the body |

The five at `7dfbf8b0`, each read from its panic:

- `events::log::tests::the_stable_prefix_barrier_is_the_only_way_a_log_becomes_a_topology_fold`
  — the control list of production regions naming `TopologyFold` lacked `ledger.rs` and
  `reachability.rs`. Named; both are readers and neither calls `replay` or `parse_log`.
- `effects::tests::the_whole_file_test_modules_are_resolved_from_the_declarations_not_the_file_names`
  — `WHOLE_FILE_TEST_MODULES` lacked `coverage/tests.rs` and `ledger/tests.rs`. Named (§5).
- `engine::topology::recover::tests::the_ledger_balances_at_complete` — R17 `Present(1)` after
  the drop: `rundir::is_running` found the run's cleanup lease held. `cleanup::is_held` is a
  non-blocking `flock` on `cleanup.lock`; a `git` child of the ref funnel holds that lease while
  it lives through a descriptor made inheritable for it (`hold_cleanup_lease_for_child`, PR
  #275), and under a parallel suite a child that another test thread forks in the window between
  the descriptor's clearing of `CLOEXEC` and the exec inherits it and holds the lease until it
  exits — the same shape the box already knows from the reaper lease test. The observation now
  waits, bounded at twenty seconds, for the lease to be free (`wait_for_cleanup_hold_release`);
  a hold that never clears still fails.
- `engine::topology::recover::tests::kill_after_report_before_each_cleanup_step` — the same
  hold, met by the resume that followed a finalization: "still has a process of its own alive in
  worktree … and that process holds the run's cleanup lease; refusing overlapping engine
  ownership" (R28 behaving as designed). The test waits through the same helper before each of
  its resumes.
- `runner::container::tests::real_docker_kill_on_an_already_exited_container_is_tolerated` —
  the box's known Docker flake (`PR274-DOCKER-*`, `src/runner/container/tests.rs:3226`), not a
  PR10 site; passed alone at the same head, and in the `2df22b78` run.

## 10. The Windows guest

The persistent guest (`windowsguest`: Windows Server 2025 build 26100.1742, git
2.50.1.windows.1, global `core.autocrlf=true`) was shut off when the session reached it and was
started with `virsh start winguest`. Its `C:\upstroke` checkout carried an uncommitted change to
`src/workspace_manager.rs` from an earlier session, which was left untouched: the branch was
fetched from `origin` (the only repository this session pushes to) into a separate worktree,
`C:\upstroke-pr10`, with its own target directory, at `7dfbf8b0`.

`cargo test --lib -- engine::topology::recover::tests engine::topology::coverage
engine::topology::ledger engine::topology::run engine::topology::select engine::topology::emit
topology::census`: **254 passed, 0 failed, 5 ignored** in 98.5 s
(`~/pr10-evidence/7dfbf8b0/guest-windows-7dfbf8b0.log`). Ten of the 254 are not in that
selection: `cmd.exe` does not separate commands at `;`, so the `type` that was meant to print
the log afterwards reached cargo as an eighth filter and matched ten unrelated tests whose
names contain the word; they passed too. The same selection, run again at `a0808bf3` with the
command separated correctly: **244 passed, 0 failed, 5 ignored** in 94.1 s
(`~/pr10-evidence/2df22b78/guest-windows-a0808bf3.log`). That is every path-shaped thing the
slice adds — the finalization matrix's ref and worktree deletions, the report path, the ledger's
inventories, the coverage census's source scan with backslashes normalized, the closure kill
child — on NTFS with CRLF checkout. `kill_after_report_before_each_cleanup_step` ran over sixty
seconds there and passed both times. The commits after `a0808bf3` are this record's.

After the round-1 review the whole suite was run on the guest at `aaebf8df`
(`~/pr10-evidence/aaebf8df/guest-fulltest-warm-aaebf8df.log`: 2,427 library tests passed, 0
failed, 50 ignored, and the 12 binary tests). The full suite at the head the body records — the
repair round's tree, carried to the guest as a bundle and checked out in `C:\upstroke-pr10` —
is the body's, labelled as the full suite.

## 11. What is not claimed

- The shared census is a breadth-first prefix of the space under the packet's bounds, truncated
  at 20,000 states and saying so. Every declared bound is reached — the fourth sequence and the
  second repair by the deep census, the rest by the shared one — and every fault row inside the
  fold is reached from the shared census, but the space is not closed: an assertion over the
  shared census is over its explored set, and a boundary reached is not a space exhausted (R16).
- ST-07's merge check ran at host `unix`. The `windows`-only points (`ambient_job_joined`, the
  Windows job-object points of `Process.Spawn`) are claimed by the same tests for that host and
  witnessed by the guest's run of the suite, not by a merge check on the guest, which was not run.
  Three coordinates are declared unobservable with reasons held to the code (R14):
  `Report.Write`, `Process.Spawn`'s hook phases, `Process.Terminate`.
- The process-local rows R3, R4, R13, R17, R22 and R28 are observed as the loop reports them
  and certified at G6 (ST-09's "PR11 (process-local rows)"); R19 and R26 are observed absent
  because the ledger's fixtures drive the loop through the recording runner and create no
  container.
- The bounded wait for a transient cleanup hold (§9) is a test's accommodation of a
  process-wide descriptor-inheritance window under parallel spawning; it is not a change to the
  lease, whose observation refused exactly as R28 tables.
- macOS is CI's to confirm.

## 12. The merge

Under the standing delegation of 2026-09-12 (`CLAUDE.md`, `MAINTAINING.md` step 7) the agent
doing the work merges once the bar is met. This session opens the draft and stops: no review, no
ready, no enqueue, no merge. The classification for whoever merges is §5's: two CI-contract test
files under `src/effects/` are touched, so the standing form does not clear the pull request on
its own.

## 13. The repair round

The round-1 review of `aaebf8df` (four `gpt-6-astra` lenses, 2026-09-13; the combined verdict is
on the pull request) found seven P1s, four P2s that conflict with the slice goal, and nine P3
sentences. Each is closed below with the commit that closed it and the executed witness; the
recipes the lenses gave as "reasoned" were all executed through the mutation runner (§8), each
failing its test as the lens predicted and passing with the fix.

| Finding | Closed by | Witness |
|---|---|---|
| contract P1-1 — ST-07 checked `coverage::range()`'s twelve sites, not the inventory | `8dd82409`: the inventory is `coverage::inventory` (R14); every adapter exports (`src/observations.rs`); witnesses in `coverage/tests.rs` for the coordinates no suite executed; residue evidence for all nine classes; the fast no-execution record; the kill children handed the variable | the merge check over the checked inventory, exit 0 (`st07-merge-check.log`, §8); `the_inventory_is_claimed_at_every_required_phase_on_both_hosts_or_declared` |
| contract P1-2 — the census below the packet's bounds | `3923d2d5`, `a75478c7`: three originals in three shapes, the full generator, `truncated` at the trace ceiling, the arm enumeration from `fold/start.rs`, the deep census, equality at every bound (R16) | `census-bound-not-reached` (the generator stopping one sequence short fails `every_declared_dimension_is_reached_at_its_bound`), `census-trace-ceiling-silent` (an unset `truncated` at the trace ceiling fails `a_census_that_hits_its_ceiling_says_so`), `every_plan_transition_arm_is_executed_by_the_census` |
| crash P1-1 / contract P2-3 / record P1-3 — the ledger's verdicts consumed less than they observed; NoRunFinished compared a snapshot with itself | `94eaf4f4`: R14, R21, R27 carry one requirement per named fact (`Expectation::parts`); the NoRunFinished ledger is a live and an after-drop observation (R17); the recover fixture writes the normalized plan | `ledger-report-skipped`, `ledger-prunes-unreachable`; `a_rows_named_facts_are_each_consumed_by_its_verdict` |
| crash P1-2 / record P1-1 — ST-18 neither enforced the order nor covered every cell; no real kill inside finalization | `64fea82b`: the matrix from the ordered effect list, exact cell counts (24, 22), `assert_finalization_order` at every cell, `Lock.Release` hooked on the finalize path, `finalization_kill_child` (R11) | `st18-order-swapped`; `a_kill_inside_finalization_after_the_execution_root_is_removed_converges_on_the_next_resume` |
| crash P1-3 — two ST-17 witnesses erased their state by resuming before the loop | `2b019324`: `plant_live` appends through the production emitter on the resumed handle | `st17-halted-with-backoff-refused`, `st17-budget-outranks-halt` |
| record P1-2 — "projection equivalence at every prefix" compared replay with itself | `8dd82409`: the hooks bundle's `folded` hook records the live projection at every applied delta (`HarnessTopologyHooks::live_projections`), compared with the replay of that prefix, every durable prefix compared | `projection-no-live-snapshot`, `projection-stale-prefix` |
| record P1-4 — the fault-row audit let the classifier select what it checked | `895544fe`: `reachability::rows_reached` from the fold alone, `matches_row` item by item | `classifier-drops-beta` |
| crash P2-4 / contract P2-4 — a stale report with the stored digest was fresh | `3923d2d5`: `is_fresh_against` verifies the content digest, the outcome and the runner | `finalized_report_names_runner_identity` tampers the stored report both ways |
| contract P2-5 — the projection dropped origin and lineage | `3923d2d5`: `TaskProjection.origin`, `TaskProjection.lineage` | asserted in `finalized_report_names_runner_identity` and the fresh-incarnation repair test |
| crash P2-5 — no fault between a `generation_closed` and its scrub | `3923d2d5`, `a75478c7`: `a_fault_between_the_closure_close_and_its_scrub_is_reclaimed_by_the_next_resume` | the test |
| record P2-5 — the settlement guard asserted no order | `3923d2d5`: `OrderedHooks`, one timeline across the families | `settle-scrub-before-close` |

**What the work-in-progress commit got wrong, and this session corrected** (`a75478c7`): the
census generator offered no attempt for a repair (a lineage member's attempt records its
materialization and its candidate widens its lineage, which the fold requires and the generator
did not send), so no repair ever ran and the deep census stopped at three sequences; the
run-ending close was offered on `run_is_ending` alone with an original's lease disposition, so a
budget-stopped state with a retained repair generation had no way out; and `Census::outgoing`
scanned every transition per state. Two P2 tests expected what the fold does not do (a first
repair at lineage index 1; budget stops in epochs 0 and 1 where the live run is itself a resume)
and now derive their expectations from the log.

**The P3 sentences**, each fixed in every copy: the baseline count (§8: 2,473/10/62 at
`61ec7587`, 2,443 at `7e0110a1`); "this file alone" (§9) and the body's "all under
`src/agent/proc.rs`" (the merge also changed `CHANGELOG.md` and `effects/wrappers.toml`); the
claimed `Worktree.CreateExecutionRoot` registry entry (R7); the settle-suite kill children "not
handed the variable" (R14, §11); the stale test name (§4, `…cross_the_checkpoint`); the census
notes' `the_fixture_region_is_the_one_the_fold_derives`, the closure notes' totality-diagnostic
sentence and the select-test notes' "seven cross and two do not" and refused-versus-admitted
description (`3923d2d5`, `16fe48fe`); M22/M23's `rc=101` now in the runner's summary and the
pre-repair `finished(Parked, 0, true, false)` in the `settle-scrub-removed` log (§8); the seven
re-deferred findings' `reviewed_sha`, `location` and body rows (§6); the classifier's nine comment
lines moved to its notes (`895544fe`).

**The searches run for the class rule.** For every false sentence the copies were found by
`grep -rn` over `reviews/2026-09-12-pr10-record.md`, the body, `docs/internals/`, `findings/`
and `src/` for: `2,443`, `this file alone`, `all under`, `not handed the variable`, `are not
claimed`, `a_breach_appends_budget_exceeded_and_integration_and_run_end_are_refused`, `rc=101`,
`finished(Parked, 0, true, false)`, `the entry for the resume-time execution`, `twelve`, `two
originals`, `two-original`, `injected error return`, `error return at every`, `at every prefix`,
`all twenty-eight`, `stale by digest`, `by digest`, `the_fixture_region_is_the_one_the_fold_derives`,
`Seven cross`, `replay of the same prefix`, and `reviews/findings`. The two classes the brief
names — a reduced measurement described with a stronger word, and a named test claimed to do
more than its code does — were swept by reading every sentence of this record and the body that
names a test or a count against the test's code and the cited file, and §3 R11, R14, R16 and §11
are the sentences that changed.

**Master, merged; the ledger, relocated.** `origin/master` at `231c1aad` (#276, which moved
`reviews/findings/` to `findings/`, and #278) was merged as its own commit, `6fb5052e`, never a
rebase. The twelve conflicts were all finding files: the five this slice closes were deleted at
their `findings/` path (`git rm` of master's rename), among them
`findings/P2_correctness_202608310806_before-phase-names-only-the-sites-own-artifact.md`, which
master had also edited and which is closed here, so master's edit is deleted with it; the seven
this slice edits carry the branch's bytes at their `findings/` path, compared byte for byte
against the branch side after the merge. No conflict touched `src/`. After the merge
`git merge-tree --write-tree origin/master HEAD` succeeded cleanly (exit 0) and
`git ls-tree -r --name-only HEAD | grep -c '^reviews/findings/'` printed 0; the sweep
`grep -rn 'reviews/findings' --exclude-dir=target --exclude-dir=.git .` found only master's own
text (the two agent guides' trap paragraph, the gate scripts' fixtures and comments,
`findings/README.md`'s history note, one master finding's body) and `reviews/FINDINGS.md`, which
is closed history and stays.

**The prose, moved.** The comment blocks the round added to modules that carry notes files —
the census, the recover tests, coverage, the ledger, recovery, the report, the observation export
and the classifier — were moved into their notes (`16fe48fe`), the notes' sections that named
code the census rewrite removed were dropped or re-anchored, and the freshness sentence was
corrected in the finalize, recover and report notes. The container-intent census gained the
coverage suite in its exclusions and the fold-mentions census gained the hooks bundle (§5).

