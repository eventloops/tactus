# G4 — cumulative review gate: repair execution, retries, lineages

**Gate:** G4 — *Repair execution, retries, lineages*
**Frozen input range:** merged PR9 on top of G3 — `74da2cbbd24c55f7aed7f3593162981a11720f79`
(`origin/master` at the time of the run; the merge commit of PR #249, merged 2026-09-09T13:22:32Z).
**Blocks:** PR10 and later may not merge until G4 passes.
**Run started:** 2026-09-09T13:23Z. **Reviewer:** Claude Fable 5.1 session `g4`, worktree
`/srv/worktrees/g4`, branch `gate/g4`.

> **Where this file belongs is the owner's call.** The packet's convention is
> `reviews/<date>-gate-<id>.md`, which is why it is here beside the G3 report, and the shape of
> `reviews/` is under reconsideration. This path is the convention, not a claim about the long-term home.

---

## 0. Status of this report

This section is the plan and the progress ledger, written before the work and updated as each piece
landed, so that at any commit the record is true.

### Reading of the remit

G4 is **not a diff review**. The packet's process clause is binding: *"the integrated tree at the
frozen input range is reviewed as a whole … a gate never reviews only the latest diff."* The unit of
review is the tree at `74da2cbb`; PR9's diff is evidence about that tree, not the subject.

The frozen range is *"merged PR9 on top of G3"*. Measured rather than assumed: `git log --first-parent
9cfaf0c8..74da2cbb` is four merge commits — #250 (`docs/.nojekyll`, the G3 O5 repair), #253 (the
three G3 observation findings and one PR8-plan finding, as files under `reviews/findings/`), #252 (the
G3 report itself) and #249 (PR9). **Only #249 touches `src/`** (29 files, +9399/−360) and it is the
only merge that changes any `effects/` file (twelve effect-free rows appended to `effects/wrappers.toml`).
`Cargo.toml`, `Cargo.lock` (64 packages), `clippy.toml`, `.github/` and `scripts/` are unchanged across
the whole range. So the code under review is G3's certified tree plus PR9, and nothing else.

A gate *"may claim only resources and invariant portions introduced in its frozen range, naming the
portion and the inventory rows."* Every claim below names the portion and the row. G4's portions are
the packet's `integrated_invariants` for this gate — INV-11 (execution), INV-16 (the PR9 portion: the
complete lease lifecycle including terminal failure), INV-19 (overrides), INV-06 (Retained repair
generations including same-generation retry) — and the eight rows of its ownership matrix: R8
(lineage lease), R6 (lineage-candidate queue position), R9 (repair generation worktree + intent, incl.
materialization objects and administrative residue), R27 (materialization objects once unreferenced),
R11 (the source candidate ref, protected), R15 (the admission question), R16 (the binding override),
R14 (the repair budget unit).

### Planned order, and why

| # | Work | Why here |
|---|---|---|
| A | Baseline: the ten-command gate suite at the frozen sha | Long-running; started first so it overlaps the reading. It is the packet's *"cumulative test suite up to that gate runs on that exact range"*. |
| B | Test inventory: the packet's named tests for T-REPAIR-DISPATCH, T-ANSWER, T-RETAINED, T-RETRY and PR9's `proof_tests` mapped onto the tree | Cheap and read-only; it is the skeleton of the fault evidence table and says what is *claimed* before anything is *verified*. |
| C | The twelve adversarial fault tests, executed; five of them constructed here because no committed test drives their exact shape | The core of the pass rule. Each verified by running, and where the claim is that a test *detects*, by mutating the code and observing the named test fail. |
| D | Artifacts #2–#6: the lineage/lease trace outputs, the fault evidence table, the limit and admission outputs including ST-12, the payload freezing check, the repair-source protection evidence | The concrete proofs the required-artifacts list names by name. |
| E | Q1–Q6 with evidence, scoped to G4's portion | The bulk. Ordered Q6, Q4, Q3, Q2, Q5, Q1: the lease/ordering questions first because their vocabulary carries the rest; Q1 last because most of it belongs to earlier gates. |
| F | Verdict against the pass rule, with reasoning | Last, so it is a conclusion and not a premise. |

### Progress

| Step | State |
|---|---|
| A baseline suite | **done** — ten gates green at `74da2cbb`, 2433 lib tests passed, 43 ignored, 0 failed, plus 10 in the binary; Linux (§1) |
| B test inventory | **done** — 19 packet slugs mapped: 11 verbatim, 6 under prose names, 2 absent for a stated reason (§2) |
| C detection by mutation | **done** — 36 mutations, 33 caught (29 by the detection set, 4 more only by the full suite), 3 survivors, each a missing witness rather than a broken guarantee (§3) |
| C twelve adversarial tests | **done** — all twelve tabled, each marked run or reasoned, with its detection witness (§9) |
| D artifacts #2–#6 | **done** — §4 (trace outputs), §5 (fault evidence table), §6 (limit and admission outputs incl. ST-12), §7 (payload freezing), §8 (source protection) |
| E Q1–Q6 | **done, with the scope of each answer named** (§10) |
| F verdict | **done** — §12 |
| — defects found | **reported, not repaired** — two P3 findings filed, one merged-record sentence corrected by addendum (§11) |

**Everything below this line is finished work.**

### Known deferrals carried into this gate (not re-opened here)

Eight standing findings bear on this range and are deferred by owner ruling or as beyond reach:
`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` (P1, G3's range, owner-deferred), `PR249-ANSWER-TEXT-NOT-CARRIED`
(P2), `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` (P2), `PR249-MANIFEST-NORMALIZATION-ALIAS` (P3),
`PR249-REFUSED-MANIFEST-HANDOFF` (P3), `PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE` (P2), and
`RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` (P2, filed on pull request #254, **not in this tree**). None
is reopened, repaired or narrowed. §11 says how each bears on the pass rule, and §12 says which the
verdict rests on.

### Method notes that bind the evidence below

- **Nothing was taken on trust from a prior record.** PR9's record (`reviews/2026-09-08-pr9-record.md`)
  carries six repair rounds of mutation tables and witness runs; none was reused. The 36 mutations in
  §3 were designed from this reviewer's own reading of the fold (`src/topology/fold/apply.rs`,
  `src/topology/fold/check_attempt.rs`, `src/topology/fold/check_end.rs`,
  `src/topology/fold/check_integration.rs`, `src/topology/fold/outcome.rs`), the queue and lease
  tables (`src/topology/queue.rs`, `src/topology/leases.rs`), and the engine
  (`src/engine/topology/repair.rs`, `src/engine/topology/dispatch.rs`, `src/engine/topology/select.rs`,
  `src/engine/topology/run.rs`, `src/engine/topology/recover.rs`, `src/engine/topology/settle.rs`,
  `src/engine/topology/integrate.rs`, `src/workspace_manager.rs`), and each was applied and measured
  here.
- **Mutation is the detection test.** A green suite proves the tests pass, not that they still detect.
  Every "test X guards Y" claim below is backed by a mutation of Y observed to kill X, or is marked as
  a positive-behaviour claim with no refusal to rot.
- **Where a claim was constructed rather than found, the construction is temporary and uncommitted.**
  Fifteen measurement tests were appended to `src/engine/topology/recover/tests.rs` for the run, and
  two `println!` lines to the two kill samplers; the files were restored with `git checkout` and their
  hashes verified before the report head was built (§1, re-run). Their source is kept in the session
  scratchpad, and their verbatim output is quoted in §4, §6, §7 and §8 with the tag it printed under
  (`G4A` … `G4H`, `G4I`).
- **Every mutation in this report is a Linux mutation.** All 36 were applied and measured on this box
  (`x86_64-unknown-linux-gnu`, kernel 6.8.0). None was re-run on the Windows guest or on macOS, and no
  claim below should be read as a three-platform mutation result. The **unmutated** tree is green on
  all three: at the frozen sha the commit's own check runs report `test (ubuntu-latest)`,
  `test (macos-latest)` and `test (winguest)` success, `lint` and `msrv` green on ubuntu, macos and
  windows (queried through the GitHub API at 13:31Z; the newest run per check name). What the
  mutations bound is *detection*, and the mechanisms they probe — the fold's refusals, the queue's
  eligibility rule, the lease table — are platform-independent by construction. The one place platform
  matters in this range, the manifest's case handling and the kill samplers' child selection, is PR9's
  and was established on the guest by PR9 (its record §14–§16); this gate re-ran neither there.
- **Verify by hash, not by line count**, and diff against a known-good state before trusting a tree a
  mutation touched. The mutation driver asserts each anchor occurs exactly once, that the file's hash
  changed, and that it was restored to the original hash before the next mutation.

---

## 1. Step A — the cumulative suite on the exact frozen range

The packet requires *"the cumulative test suite up to that gate runs on that exact range."*

**Run:** `w1-eight-iso /srv/worktrees/g4` at `74da2cbb` (clean tree; `git status --porcelain` empty),
2026-09-09T13:27:42Z, plus the tenth gate run separately. Logs `~/eight-logs/74da2cb/`.

| # | Command | Result | Time |
|---|---|---|---|
| 01 | `cargo fmt --check` | PASS | 1s |
| 02 | `cargo clippy --all-targets --all-features -- -D warnings` | PASS | 22s |
| 03 | `cargo test --all-targets --all-features` | PASS | 65s |
| 04 | `cargo +1.85.0 check --locked --all-targets --all-features` | PASS | 19s |
| 05 | `test-release-record.sh` | PASS | 0s |
| 06 | `test-pr-policy.sh` | PASS | 0s |
| 07 | `test-pr-ledger-evidence.sh` | PASS | 0s |
| 08 | `test-docs-consistency.sh` | PASS | 0s |
| 09 | `test-internals-notes.sh` | PASS | 1s |
| 10 | `test-pr-ready-audit.sh` | PASS | 1s |

**Suite scope, measured not assumed.** `2476` tests declared in the lib binary; `2433` passed, `43`
ignored, `0` failed; `+10` in the `main.rs` binary and `0` in the `probe` example — `2443` passed in
total. G3 measured `2419` declared at its sha; the range adds `57` lib tests, all PR9's. The 43 ignored
are the same subprocess-helper entry points G3 counted (`two_crash_kill_child`, the reaper helpers,
the worktree-lock children, the kill children), invoked as children by their own tests. **Platform:
Linux** (`x86_64-unknown-linux-gnu`, kernel 6.8.0); the count is per-platform and this number is the
Linux one.

**Docker-gated lanes really ran:** `docker ps` succeeds on this box and 213 `runner::container::*`
test lines appear in the log, the same count G3 recorded.

**The binary measured *this* tree.** `w1-eight-iso` assigns a target base private to the worktree
(`/mnt/ramtarget/iso-g4`), and the log's `Compiling upstroke v0.1.0 (/srv/worktrees/g4)` line shows a
real compile rather than a served artefact. Verified rather than assumed: the lib test binary
`/mnt/ramtarget/iso-g4/slot1/debug/deps/upstroke-1f05c71869a3a005` contains the string
`/srv/worktrees/g4` seven times and contains neither `/srv/worktrees/pr9`, `/srv/worktrees/g3` nor
`/srv/tactus`. (The first attempt at that check used the box's `grep`, which is a shell function that
answered blank on a binary; the count above is `/usr/bin/grep -a -c`.)

**One box condition, recorded because it could have turned a gate red.** The shared slot pool at
`/mnt/ramtarget` was at 95% before the run, which the box's own history records as a cause of spurious
build failures. Three private target directories belonging to worktrees that no longer exist
(`iso-pr8`, `iso-pr239-mergeprobe`, `iso-pr239-probe`, 2.5 GB) were removed after checking each against
`git worktree list` and the process table; the pool was at 90% for the run. That is box hygiene, not a
repository change, and it is stated so the run's provenance is complete.

**Re-run at this report's own head.** The same ten gates were run again at `2e0f41da`, the head
that adds the whole of this report, the two finding files and the G3 addendum: `ALL 9 PASS at 2e0f41d`
from the wrapper plus `test-pr-ready-audit: ok`, 2026-09-09T14:25:58Z–14:27:29Z, `Compiling upstroke`
observed, 2433 lib tests passed. The final head of the branch differs from `2e0f41da` by this paragraph
and nothing else, and was gated the same way before the push. That is a check on this report, not on
the range: it confirms the report file itself breaks no gate, in particular `test-docs-consistency.sh`,
whose rooted-path check reads every backticked `src/…`-style path in a document.

**Step A verdict: the cumulative suite is green on the exact frozen range.** This is a necessary
condition of the pass rule and not a sufficient one; detection is established by mutation in §3.

---

## 2. Step B — what the tree claims: the packet's test names against the tree

The packet names tests as descriptive slugs in the four fault rows' `test` fields and in PR9's
`proof_tests`; the tree names them as prose. The mapping below was derived by searching the tree
(`grep -rn "fn <slug>"` over `src/`), not by trusting a name match. Of the 19 distinct slugs the four
rows name, **11 exist verbatim**, **7 exist under prose names**, and **1 does not exist**, for the
reason set out after the table.

| Row | Packet slug | In the tree as | File |
|---|---|---|---|
| T-REPAIR-DISPATCH | `repair_materialization_reproduced_after_kill` | verbatim (a real child killed before and after the materialization site) | `src/engine/topology/dispatch/tests.rs` |
| T-REPAIR-DISPATCH | `repair_materialization_synthetic_residue_recreated_after_forced_removal` | verbatim | `src/engine/topology/dispatch/tests.rs` |
| T-REPAIR-DISPATCH | `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` | verbatim | `src/engine/topology/dispatch/tests.rs` |
| T-REPAIR-DISPATCH | `repair_source_present_after_parked_resume` | prose: `an_over_limit_repair_spends_nothing_until_its_answer_activates_it` (the run parked on the admission question across the resume) and `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` (the dispatch from the protected source after that resume); the ref itself is read before and after the parked resume in measurement `G4C1` (§8) | `src/engine/topology/recover/tests.rs` |
| T-REPAIR-DISPATCH | `repair_materialization_objects_released_to_git_on_scrub` | verbatim | `src/engine/topology/dispatch/tests.rs` |
| T-ANSWER | `answer_ingested_after_kill_returns_pending` | prose: `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` (the production `EventLogAnswers` reader, `via: "event-log"`; the task goes `AwaitingInput` → `Pending` → dispatched) | `src/engine/topology/recover/tests.rs` |
| T-ANSWER | `partial_answer_ignored_and_never_pruned` | prose: `a_staged_partial_is_never_ingested_and_a_published_answer_survives_ingestion` (the reader; the partial byte-identical afterwards) plus measurement `G4H` (the loop: a resume with only a `.partial` hard-blocks and leaves it) | `src/rundir/tests.rs` |
| T-ANSWER | `answer_not_ingested_after_budget_stop_until_resume` | prose: `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` (the fold refuses `question_answered` in the stopped epoch and accepts it after `run_resumed`) plus measurement `G4H` (the loop: the published file is ingested by the next incarnation's first step) | `src/topology/fold/tests.rs` |
| T-ANSWER | `late_answer_before_halting_settlement_is_inert_and_retained` | prose, in two halves: the fold half is the same test (an answer after a halting settlement in the epoch is refused); the retained-file half is `a_staged_partial_is_never_ingested_and_a_published_answer_survives_ingestion` (R21: ingestion is a read, not a take). **No single test publishes a file immediately before a halting settlement**; the composition is reasoned from the two halves | as named |
| T-ANSWER | `late_answer_after_finalization_is_inert_and_reported_not_live` | **absent** — its subject does not exist at this range (see below) | — |
| T-ANSWER | `declined_repair_fails_lineage_and_releases_lease` | prose: `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` (loop) and `declining_a_repair_admission_fails_the_lineage_and_allows_the_run_to_end` (fold) | `src/engine/topology/recover/tests.rs`; `src/topology/fold/tests/questions.rs` |
| T-ANSWER | `binding_override_replay (ST-12)` | prose: `an_override_replaces_the_frozen_binding_for_every_later_attempt` (the fold's refusals) and `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` (the loop, whose end state is read back by replaying the on-disk log); the replay-equality half of ST-12 is measured in `G4C2` (§6) | `src/topology/fold/tests.rs`; `src/engine/topology/recover/tests.rs` |
| T-RETAINED | `retained_generation_closed_before_run_resumed` | verbatim | `src/engine/topology/settle/tests.rs` |
| T-RETAINED | `retained_generation_closed_when_worktree_missing (ST-11)` | verbatim for an ordinary generation; the repair half is `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` (resumed) and measurement `G4G` (missing worktree, live) | `src/engine/topology/settle/tests.rs`; `src/engine/topology/recover/tests.rs` |
| T-RETAINED | `retained_generation_closed_at_run_end (ST-17)` | verbatim, at the `close_retained` level; the run-end closure procedure that would call it is PR10's (see below) | `src/engine/topology/settle/tests.rs` |
| T-RETRY | `same_generation_retry_regates_cumulative_tree (ST-15)` | verbatim (ordinary); the repair half is `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` | `src/engine/topology/settle/tests.rs`; `src/engine/topology/recover/tests.rs` |
| T-RETRY | `retry_refused_after_resume` | verbatim, twice: the settle-level refusal and the recovery-level one | `src/engine/topology/settle/tests.rs`; `src/engine/topology/recover/tests.rs` |
| T-RETRY | `retry_refused_with_stale_incarnation` | verbatim | `src/engine/topology/settle/tests.rs` |
| T-RETRY | `retained_worktree_with_residue_closed_not_retried` | verbatim | `src/engine/topology/settle/tests.rs` |

**The absent slug is absent for the same reason G3 recorded for its two.** `late_answer_after_finalization_is_inert_and_reported_not_live`
needs a finalized run, and `grep -rn 'TopologyEventBody::RunFinished' src/` still finds no production
emitter: `run_finished`, the closure procedure and terminal finalization are `pr_sequence[PR10]`'s by
name, and this build's checkpoint refuses `Step::Closure` outright (`checkpoint` in
`src/engine/topology/select.rs`; observed refusing in measurements `G4B3`, `G4B4` and `G4C3`). A run
cannot be finalized here, so an answer after finalization has nothing to be inert against. The
`retained_generation_closed_at_run_end` slug exists but proves the settle-level function, not the
closure procedure that would call it, for the same reason. Both are named so a later gate knows they
are PR10's, not lost.

**PR9's eleven `proof_tests`**, each verified by running rather than by reading the record:

| Proof test | Held by (all passing at `74da2cbb`) |
|---|---|
| rejection kind tests | `an_override_replaces_the_frozen_binding_for_every_later_attempt` (E2: every single-field move of the binding, the tier either side of the floor, the pin, and a floorless ladder all refused; the exact five-field binding accepted); `a_closing_generation_admits_exactly_the_disposition_its_holding_implies` (`src/topology/fold/region.rs`); `a_rejection_already_folded_is_refused_when_it_arrives_again` |
| limit tests | `an_over_limit_repair_spends_nothing_until_its_answer_activates_it`; `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` (both answer deliveries) |
| two-lineage order test | `two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older` |
| already-present-source repair test | `an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly` (`src/engine/topology/attempt/tests.rs`); `a_repair_dispatch_records_what_its_materialization_observed` |
| source-protection test across Parked/BudgetExceeded resume | `a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume`; the Parked half is the over-limit pair above |
| materialization objects accounted by the worktree then released to Git on scrub; interrupted mid-cherry-pick recreated deterministically after forced removal | `repair_materialization_objects_released_to_git_on_scrub`; `repair_materialization_synthetic_residue_recreated_after_forced_removal`; `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`; `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` |
| ST-05 lease subset | `a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused` |
| ST-11 repair | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` |
| ST-12 | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`; `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`; `picking_the_last_of_two_offered_agents_binds_the_repair_to_it_rather_than_declining`; `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` |
| ST-15 repair | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` |
| kill tests per site in range incl. the residue class | `repair_materialization_reproduced_after_kill` (real kills before and after the site); `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` (three prefixes across the process boundary); the synthetic and sampled residue tests above; `kill_during_retry_attempt_closes_generation` (T-RETRY meeting T-ATTEMPT); `retained_generation_not_continued_after_kill` (T-RETAINED) |

**One coverage census the tree carries, stated for what it covers.** G3's O2 (now the filed finding
at `reviews/findings/P3_correctness_202609090301_fault-row-census-covers-one-row-and-is-named-every.md`)
still holds at this sha: `every_fault_row_name_is_a_test_in_the_tree` covers the T-CONTAINER row only,
so nothing mechanical holds the 19 slugs above to the tree. The obligation was discharged by hand in
this section.

---
## 3. Step C part 1 — detection, established by mutation

A green suite proves the tests pass, not that they still detect. Thirty-six mutations were applied to
the frozen tree, one at a time, by a driver that asserts the anchor text occurs exactly once in the
file, asserts the file's hash changed, runs a detection set, restores the file with `git checkout` and
asserts the hash is the original again before the next mutation. All 36 compiled; the driver's own
ledger records `changed`, `restored` and `clean_before` true for every one. Logs
(`mut-M<nn>.log`) and the ledgers (`mutation-report*.json`) are in the session scratchpad.

**The detection set, and the full-suite confirmation.** Each mutation first ran against the same 79
tests: the 46 committed tests §9 names (the two kill samplers excluded, so that a sampler's timing
could not masquerade as a kill), 20 of their neighbours in the same modules (the check-integration and
repair unit tests, the questions tests, the G3 shape table and the atomic-rejection test, the
retained-retry recovery tests), and the 13 temporary measurements that existed at that point. That set
runs in about 4 s; each mutation's cost was the rebuild (27 s each, 973 s for the catalogue). **Every
mutation that survived the 79 was then re-run against the full library suite** (2476 tests, about 60 s
each), because a survivor of a subset is only a survivor of that subset. Four of the seven were caught
there, by tests this reviewer had not put in the set; the table says which, and the three that survive
the full suite are the survivors.

| # | Mutation | File | Result | Killed by |
|---|---|---|---|---|
| M01 | lineage order inverted: a younger lineage member no longer waits behind an older overlapping lineage | `src/topology/queue.rs` | **caught**, 2 | `two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`, `an_ordinary_candidate_waits_for_any_lineage_and_a_member_only_for_older_ones` |
| M02 | an ordinary candidate overlapping a live lineage is no longer skipped (InsideLineage dropped) | `src/topology/queue.rs` | **caught**, 1 | `an_ordinary_candidate_waits_for_any_lineage_and_a_member_only_for_older_ones` |
| M03 | a lineage lease is never released (release of Lineage owner is a no-op) | `src/topology/leases.rs` | **caught**, 7 | `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`, `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`, `two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`, `a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused` … (+3) |
| M04 | fail_lineage no longer releases the members' holdings (lineage lease, candidate leases, queue positions) | `src/topology/fold/apply.rs` | **caught**, 5 | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`, `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`, `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy`, `an_embedded_question_blocks_a_retained_siblings_retry_until_answered_or_declined` … (+1) |
| M05 | a repair member's terminal Failed settlement no longer folds its lineage | `src/topology/fold/apply.rs` | **caught**, 1 | `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay` |
| M06 | a non-override attempt_started on an overridden task is accepted (BindingMismatch dropped) | `src/topology/fold/check_attempt.rs` | **caught**, 1 | `an_override_replaces_the_frozen_binding_for_every_later_attempt` |
| M07 | materialization_observed presence parity no longer checked at attempt_started | `src/topology/fold/check_attempt.rs` | **caught by the full suite only**, 1 | `an_attempt_runs_the_frozen_binding_or_the_validated_override` (not in the 79-test set) |
| M08 | an answered question stays open, so a second answer is not refused as already answered | `src/topology/fold/apply.rs` | **caught**, 9 | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`, `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step`, `g4_meas_c2_human_binding_activation_and_refusals` … (+5) |
| M09 | an option index outside the frozen option list is accepted | `src/topology/fold/check_end.rs` | **caught**, 1 | `an_answer_names_an_open_question_of_that_task_and_an_option_it_offered` |
| M10 | an answer is ingested after budget_exceeded in the same epoch | `src/topology/fold/check_end.rs` | **caught**, 1 | `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch` |
| M11 | a repair dispatch skips the ceiling check | `src/engine/topology/select.rs` | **caught**, 1 | `a_repair_origin_task_crosses_the_checkpoint_and_the_ceiling_binds_it_like_any_dispatch` |
| M12 | a same-generation retry of a repair records no materialization (Retained dropped) | `src/engine/topology/run.rs` | **caught**, 1 | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` |
| M13 | recovery step (e) closes no retained generation | `src/engine/topology/recover.rs` | **caught**, 3 | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again`, `a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next`, `retry_refused_after_resume` |
| M14 | a worktree that fails Worktree.Verify is reported recreated but reused as it stands | `src/engine/topology/dispatch.rs` | **caught**, 3 | `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`, `repair_materialization_synthetic_residue_recreated_after_forced_removal`, `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` |
| M15 | the materialization no longer restores the base tree before it picks | `src/workspace_manager.rs` | **caught**, 1 | `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` |
| M16 | a merge_rejected registering a Runnable repair past the frozen limit is accepted | `src/topology/fold/check_integration.rs` | **caught by the full suite only**, 2 | `a_lineage_past_its_repair_limit_registers_only_a_human_required_repair`, `a_lineage_that_has_consumed_its_allowance_registers_only_a_human_required_repair` (not in the 79-test set) |
| M17 | the builder never admits HumanRequired past the limit | `src/engine/topology/repair.rs` | **caught**, 9 | `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`, `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step`, `an_over_limit_repair_spends_nothing_until_its_answer_activates_it`, `g4_meas_b2_decline_without_halt` … (+5) |
| M18 | an empty tier intersection returns the root's ladder instead of a HumanBinding one | `src/engine/topology/repair.rs` | **caught**, 7 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`, `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`, `g4_meas_c2_human_binding_activation_and_refusals` … (+3) |
| M19 | a decline under a halting policy no longer records halted_at | `src/topology/fold/apply.rs` | **caught**, 3 | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`, `a_decline_fails_its_task_and_halts_only_when_its_recorded_policy_says_so`, `declined_parked_verification_fails_task_consumes_queue_position_releases_lease_and_halts_per_policy` |
| M21 | an absent or substituted source candidate is not refused at dispatch | `src/engine/topology/dispatch.rs` | **caught**, 1 | `a_repair_whose_source_candidate_is_missing_is_refused_before_any_append` |
| M22 | a conflict rejection deletes the rejected candidate's candidates ref (R11 violated at the Conflict arm) | `src/engine/topology/integrate.rs` | **SURVIVED** (79-test set and full suite: 2445 passed, 0 failed) | nothing — see §3.1 |
| M23 | a code rejection deletes the rejected candidate's candidates ref (R11 violated at the CodeRejected arm) | `src/engine/topology/integrate.rs` | **SURVIVED** (79-test set and full suite: 2445 passed, 0 failed) | nothing — see §3.1 |
| M24 | an open question no longer blocks work throughout its lineage | `src/topology/fold.rs` | **caught**, 2 | `a_lineage_question_blocks_related_work_and_decline_preserves_an_unrelated_transaction`, `an_embedded_question_blocks_a_retained_siblings_retry_until_answered_or_declined` |
| M25 | task_merged may release any lease (check_lease_release accepts everything) | `src/topology/fold/check_integration.rs` | **caught**, 2 | `a_publication_releases_the_lease_its_own_candidate_or_its_lineage_holds`, `a_merge_copies_the_authorization_exactly` |
| M26 | task_merged may record a satisfies closure other than the one the fold derives | `src/topology/fold/check_integration.rs` | **caught**, 1 | `a_merge_copies_the_authorization_exactly` |
| M27 | a new lineage's region omits the rejected candidate's own actual paths | `src/topology/fold/apply.rs` | **caught by the full suite only**, 1 | `a_conflict_opens_and_closes_its_own_transaction` (not in the 79-test set) |
| M28 | a repair dispatch naming no source candidate, or an ordinary one naming one, is accepted | `src/topology/fold/check_attempt.rs` | **caught by the full suite only**, 1 | `a_dispatch_takes_the_holding_its_origin_implies` (not in the 79-test set) |
| M29 | closed generations' worktrees and intents are never reclaimed on resume | `src/engine/topology/recover.rs` | **caught**, 3 | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again`, `a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next`, `retry_refused_after_resume` |
| M30 | a retry whose worktree fails verification starts anyway instead of closing the generation | `src/engine/topology/settle.rs` | **caught**, 2 | `retained_generation_closed_when_worktree_missing`, `retained_worktree_with_residue_closed_not_retried` |
| M31 | the loop retries a session another incarnation retained | `src/engine/topology/settle.rs` | **caught**, 1 | `retry_refused_after_resume` |
| M32 | the fold accepts a stale-incarnation retry | `src/topology/fold/check_attempt.rs` | **caught**, 1 | `a_retained_session_belongs_to_the_incarnation_that_retained_it` |
| M33 | answers are never polled before a step (ingestion only at the hard block) | `src/engine/topology/run.rs` | **caught**, 6 | `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`, `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue`, `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` … (+2) |
| M34 | an override no longer makes the ladder single-rung (an overridden empty ladder exhausts to Fail, not the human rung) | `src/engine/topology/run.rs` | **SURVIVED** (79-test set and full suite: 2445 passed, 0 failed) | nothing — see §3.1 |
| M35 | a one-off binding is not a pin (E2) | `src/topology/events.rs` | **caught**, 3 | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`, `picking_the_last_of_two_offered_agents_binds_the_repair_to_it_rather_than_declining`, `an_override_replaces_the_frozen_binding_for_every_later_attempt` |
| M36 | recovery step (g) reports every open generation verified without verifying or recreating | `src/engine/topology/recover.rs` | **caught**, 1 | `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` |
| M37 | the continuation of a repair generation does not materialize it | `src/engine/topology/dispatch.rs` | **caught**, 5 | `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces`, `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`, `repair_materialization_reproduced_after_kill`, `repair_materialization_synthetic_residue_recreated_after_forced_removal` … (+1) |

**Detection score: 36 mutations, 33 caught (29 by the detection set, 4 more only by the full suite),
3 survived.** Every refusal the twelve adversarial rows lean on has a live witness (§9's last
column). The three survivors sit under two claims — the R11 protection at the rejection arms, and the
override's single-rung ladder — and §3.1 says what each means.

Three of the kills are the pass rule's own clauses and deserve naming: **M03** (a lineage lease that is
never released) kills seven tests, among them the two-lineage test (the younger lineage never becomes
eligible), the decline test (the lease is asserted released) and the ST-05 lease test — so "no lease
double release" is guarded from both directions, the release counted once and its absence noticed;
**M01** (lineage order inverted) kills exactly the two tests that own the ordering claim and nothing
else, which is the right shape for a mutation of one rule; **M08** (an answered question kept open, so
a second answer is not refused) kills nine, because every activation path notices the question still
standing.

**The four caught only by the full suite** are recorded for the next reviewer, who will otherwise
build the same subset and reach the same false survivor: the fold's parity refusals on a repair's
`materialization_observed` (M07) and `source_candidate` (M28) are guarded by
`an_attempt_runs_the_frozen_binding_or_the_validated_override` and
`a_dispatch_takes_the_holding_its_origin_implies`; the fold's over-limit admission refusal (M16) by
`a_lineage_past_its_repair_limit_registers_only_a_human_required_repair` and
`a_lineage_that_has_consumed_its_allowance_registers_only_a_human_required_repair`; and the widening of
a new lineage by the rejected candidate's own paths (M27) by
`a_conflict_opens_and_closes_its_own_transaction`. All four are in `src/topology/fold/tests.rs`.

### 3.1 The three survivors, each read for what it means

| Survivor | What it removes | Why nothing caught it | Does the guarantee still hold at `74da2cbb`? | Filed as |
|---|---|---|---|---|
| **M22**, **M23** | each rejection arm of `integrate.rs` (conflict; code) additionally deletes the rejected candidate's candidates ref through `Ref.DeleteCandidatesRef` | **no test in the tree reads a candidates ref after a rejection produced by `integrate`**: the G3 shape table reads pins only (`grep -n 'candidates/' src/engine/topology/integrate/tests.rs` is empty), the atomic-rejection test asserts events and hook order, and every G4 recovery test plants its rejection directly rather than producing it. Full suite: 2445 passed, 0 failed, under either mutation. The two measurements that do go through `integrate` (`G4B5`, code; `G4B6`, conflict) were re-run under the mutations with their output kept: under M22, `G4B6` shows the ref gone and the repair's dispatch **refused** — `its authoritative ref … does not exist, and it is what keeps the candidate reachable` — and `G4B5` unaffected; under M23 the mirror image. So the defence-in-depth refusal works, and nothing asserts that it never has to | **yes**, structurally: at this sha there is no production caller of `Ref.DeleteCandidatesRef` at all (§8c), so the protection cannot be violated by this tree; and observed: §8a reads the ref present after a real conflict rejection and a real code rejection and the repair dispatching from it. What is missing is a witness that would notice a *future* arm deleting it — and PR10's finalization is where such an arm will be written | `G4-O1` (§11), P3; the G3 report's R11 sentence corrected by a dated addendum |
| **M34** | `ladder_policy`'s single-rung ladder for a task with a recorded override (without it, an overridden empty ladder has zero rungs and exhausts to `Fail` on the first failure) | no committed test drives an overridden repair to exhaustion; the packet names the consequence (*"exhaustion escalates to the human rung"*) but lists no proof test for it; the driven fixture cannot observe it, because its fixed-id question source refuses the exhaustion's park as a reused question (`G4C3`, §6.2). Full suite: 2445 passed, 0 failed | yes: `G4C3b` measures `next_step` with `rungs = 1` reaching `AskHuman` at the second failure and with `rungs = 0` reaching `Fail` at the first, and the `ladder_policy` arm is a two-line conditional read here; what is missing is a loop-level witness | `G4-O2`, P3 |

Neither survivor is a behaviour defect at this sha: in each case the guarantee was measured to hold
(§6, §8) and the survivor is a guard whose *rot* would not be noticed. That is the class this project
has recorded before — a guard with no witness — and each is filed so that the next reviewer does not
rerun the same mutation and reach the same dead end.

---

## 4. Required artifact #2 — lineage/lease trace outputs

These are the printed outputs of the temporary measurement tests described in §0, each of which
drives the production loop against a real repository and then **replays the on-disk log prefix by
prefix** through `TopologyFold::replay`, printing after every event the lineage lease table
(`L<root>@age<n>[<region>]`), every queue entry with the queue's own eligibility answer for it
(`CandidateQueue::ineligible`, or `ELIGIBLE`), the fold's own first-eligible candidate, the open
transaction, the recorded overrides, the open question count, every task's state, `halted_at` and the
budget stop. So each line is a fact about durable state, not about the process that produced it.

### 4.1 Two overlapping lineages resolve in creation order — `G4A`

Set-up (the committed two-lineage test's shape, reused verbatim): alpha and beta are both queued at
the base; alpha is rejected at sequence 0 by a conflict on `candidate.txt`+`shared.txt`, beta at
sequence 1 on `other.txt`+`shared.txt`; the *younger* lineage's repair (`k3`) has already run to a
candidate and queued it, while the older lineage's repair (`k2`) has not started. The loop is then
driven three steps.

```
G4A before-drive: lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } eligible=None next_sequence=Some(2)
G4A step1 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4A step2 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), merged_sha: CommitSha("d5b5cf08b29357b7aa8d6214c709e132e81de892") })
G4A step3 Ok(Integrated { key: TaskKey(3), sequence: SequenceId(3), merged_sha: CommitSha("380580f5ec2fa84ec5be896d883846c3a9601b95") })
G4A #09 merge_rejected s0 candidate=k0g0@837b33d3 head=35cb939c disposition=Conflict[candidate.txt+shared.txt] lease_effect=CreatesLineage { root: TaskKey(0), paths: Prefixes { paths: [GitPath("candidate.txt"), GitPath("shared.txt")] } } repair=k2 admission=Runnable lineage=Some(Lineage { root: TaskKey(0), parent: TaskKey(0), index: 0 }) || lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[other.txt]:ELIGIBLE | eligible=Some("k1g0") | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingMerge,2:Pending | halted=None | budget_stop=None
G4A #10 merge_rejected s1 candidate=k1g0@c212353e head=35cb939c disposition=Conflict[other.txt+shared.txt] lease_effect=CreatesLineage { root: TaskKey(1), paths: Prefixes { paths: [GitPath("other.txt"), GitPath("shared.txt")] } } repair=k3 admission=Runnable lineage=Some(Lineage { root: TaskKey(1), parent: TaskKey(1), index: 0 }) || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=empty | eligible=None | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:Pending,3:Pending | halted=None | budget_stop=None
G4A #14 task_candidate_created k3 g0 @d96fe604 || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } | eligible=None | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:Pending,3:AwaitingMerge | halted=None | budget_stop=None
G4A #16 task_dispatched k2 g0 base=35cb939c lease=InheritedLineage { root: TaskKey(0) } source=k0g0@837b33d3 || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } | eligible=None | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:Pending,3:AwaitingMerge | halted=None | budget_stop=None
G4A #17 attempt_started k2 g0 a1 rung0 binding=claude-code/claude-opus-5/High/tier Mid/pinned false resume=false mat=Some(Clean) || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } | eligible=None | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:Pending,3:AwaitingMerge | halted=None | budget_stop=None
G4A #18 candidate_prepared k2 g0 commit=d5b5cf08 paths=[candidate.txt] lease_effect=WidensLineage { root: TaskKey(0), paths: Prefixes { paths: [GitPath("candidate.txt")] } } || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } | eligible=None | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:AwaitingMerge,3:AwaitingMerge | halted=None | budget_stop=None
G4A #19 task_candidate_created k2 g0 @d5b5cf08 || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE | eligible=Some("k2g0") | tx=none | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:AwaitingMerge,3:AwaitingMerge | halted=None | budget_stop=None
G4A #20 merge_prepared s2 disp=Fast k2g0 expected=35cb939c proposed=d5b5cf08 candidate=d5b5cf08 pin=None satisfies=[0, 2] verification=none || lineages=L0@age0[candidate.txt+shared.txt] L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:BehindOlderLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE | eligible=None | tx=s2 prepared | overrides= | questions=0 | states=0:AwaitingRepair,1:AwaitingRepair,2:AwaitingMerge,3:AwaitingMerge | halted=None | budget_stop=None
G4A #21 task_merged s2 sha=d5b5cf08 satisfies=[0, 2] release=Lineage { root: TaskKey(0) } || lineages=L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:ELIGIBLE | eligible=Some("k3g0") | tx=none | overrides= | questions=0 | states=0:Merged,1:AwaitingRepair,2:Merged,3:AwaitingMerge | halted=None | budget_stop=None
G4A #22 merge_verification_started s3 k3g0 basis=StaleClean { prepared_ref: GitRef("refs/upstroke/runs/01KZTPR7E00000000000000001/prepared/3") } expected=d5b5cf08 proposed=380580f5 || lineages=L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:ELIGIBLE | eligible=None | tx=s3 verifying | overrides= | questions=0 | states=0:Merged,1:AwaitingRepair,2:Merged,3:AwaitingMerge | halted=None | budget_stop=None
G4A #23 merge_prepared s3 disp=StaleClean k3g0 expected=d5b5cf08 proposed=380580f5 candidate=d96fe604 pin=Some("refs/upstroke/runs/01KZTPR7E00000000000000001/prepared/3") satisfies=[1, 3] verification=Passed || lineages=L1@age1[other.txt+shared.txt] | queue=k3g0[other.txt+shared.txt]/L1:ELIGIBLE | eligible=None | tx=s3 prepared | overrides= | questions=0 | states=0:Merged,1:AwaitingRepair,2:Merged,3:AwaitingMerge | halted=None | budget_stop=None
G4A #24 task_merged s3 sha=380580f5 satisfies=[1, 3] release=Lineage { root: TaskKey(1) } || lineages=none | queue=empty | eligible=None | tx=none | overrides= | questions=0 | states=0:Merged,1:Merged,2:Merged,3:Merged | halted=None | budget_stop=None
G4A replay_twice_equal=true events=25 outcome=Ending(Complete)
G4A after: lineages=none queue=empty entitlements_held=0 invocations_balance=true
```

(Events #00–#08 are the two ordinary tasks' dispatch, attempt and candidate creation; #11–#13 are
the younger repair's planted dispatch, attempt and candidate; #15 is `run_resumed`. They are in the
session log and add nothing to the reading.)

What the trace shows, read off the lines: the lineage table is **totally ordered by creation**
(`L0@age0`, `L1@age1`); the younger lineage's queued candidate is ineligible with the reason
`BehindOlderLineage { root: 0 }` at every line from #14 to #20 while the older lineage has no
candidate; the loop's step 1 therefore **dispatches the older repair** rather than integrating the
queued younger candidate, and the older lineage publishes at s2 with `satisfies=[0, 2]` and
`release=Lineage { root: 0 }`; at #21 the lease table drops `L0`, the younger candidate becomes
`ELIGIBLE` in the same line, and it is re-verified stale onto the head the older lineage left (#22,
`StaleClean`, `expected=d5b5cf08`) and merged at s3 with `release=Lineage { root: 1 }`. **No deadlock:
the run reaches `Ending(Complete)` with an empty lease table and queue, and the replay of the on-disk
log is equal to itself twice.** Each lineage lease is released exactly once, at the publication whose
`satisfies` names its root.

### 4.2 An ordinary candidate overlapping a lineage waits, then re-verifies stale — `G4B`

Set-up: alpha queued at the base; beta's *ordinary* candidate queued on `shared.txt`; alpha rejected at
s0 by a conflict on `candidate.txt`+`shared.txt` (one automatic repair allowed, so the repair is
`Runnable`).

```
G4B before-drive: lineages=L0@age0[candidate.txt+shared.txt] queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } eligible=None
G4B step1 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4B step2 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(1), merged_sha: CommitSha("d5b5cf08b29357b7aa8d6214c709e132e81de892") })
G4B step3 Ok(Integrated { key: TaskKey(1), sequence: SequenceId(2), merged_sha: CommitSha("2cab9e12d11b9f7886e0f4f38b69458330f672c8") })
G4B #09 merge_rejected s0 ... repair=k2 admission=Runnable ... || lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } | eligible=None | ...
G4B #14 task_candidate_created k2 g0 @d5b5cf08 || lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } k2g0[candidate.txt]/L0:ELIGIBLE | eligible=Some("k2g0") | ...
G4B #16 task_merged s1 sha=d5b5cf08 satisfies=[0, 2] release=Lineage { root: TaskKey(0) } || lineages=none | queue=k1g0[shared.txt]:ELIGIBLE | eligible=Some("k1g0") | ...
G4B #17 merge_verification_started s2 k1g0 basis=StaleClean { prepared_ref: GitRef(".../prepared/2") } expected=d5b5cf08 proposed=2cab9e12 || ...
G4B #18 merge_prepared s2 disp=StaleClean k1g0 expected=d5b5cf08 proposed=2cab9e12 candidate=f36375ce pin=Some(".../prepared/2") satisfies=[1] verification=Passed || ...
G4B #19 task_merged s2 sha=2cab9e12 satisfies=[1] release=Candidate { key: TaskKey(1), generation: GenerationId(0) } || lineages=none | queue=empty | ... | states=0:Merged,1:Merged,2:Merged | ...
G4B replay_twice_equal=true events=20 outcome=Ending(Complete)
```

The ordinary candidate is `InsideLineage { root: 0 }` from the rejection until the lineage's
publication, is not knowingly moved ahead of it (the repair's candidate, queued *after* it, integrates
first), becomes eligible in the very line the lease is released, and is then re-verified **stale**
(the head moved) rather than published fast.

### 4.3 The other exit: a declined lineage frees the waiting candidate — `G4B2`

The same shape with no automatic repairs (`max_merge_repairs = 0`), so the rejection registers the
repair `HumanRequired{limit: 0}` with a question, and a decline is delivered:

```
G4B2-nohalt step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: true })
G4B2-nohalt step2 Ok(Integrated { key: TaskKey(1), sequence: SequenceId(1), merged_sha: CommitSha("f36375cedb1ff912fd602535f9e39eac8bc1bd98") })
G4B2-nohalt #09 merge_rejected s0 ... admission=HumanRequired{limit:0, q:q-park-fixed kind:Continue options:[...]} ... || lineages=L0@age0[candidate.txt+shared.txt] | queue=k1g0[shared.txt]:InsideLineage { root: TaskKey(0) } | eligible=None | ... | questions=1 | states=0:AwaitingRepair,1:AwaitingMerge,2:AwaitingInput | ...
G4B2-nohalt #11 question_answered k2 q=q-park-fixed answer=Declined { decline_halts_run: false } via=driven || lineages=none | queue=k1g0[shared.txt]:ELIGIBLE | eligible=Some("k1g0") | ... | questions=0 | states=0:Failed,1:AwaitingMerge,2:Failed | halted=None | ...
G4B2-nohalt #12 merge_prepared s1 disp=Fast k1g0 expected=35cb939c proposed=f36375ce candidate=f36375ce pin=None satisfies=[1] verification=none || ...
G4B2-nohalt #13 task_merged s1 sha=f36375ce satisfies=[1] release=Candidate { key: TaskKey(1), generation: GenerationId(0) } || lineages=none | queue=empty | ... | states=0:Failed,1:Merged,2:Failed | ...
G4B2-nohalt replay_twice_equal=true events=14 outcome=Ending(Complete)
```

The decline fails the root and the repair in one line, releases the lineage lease in the same line,
and the waiting candidate is eligible immediately; since the head never moved it publishes **fast**.

---

## 5. Required artifact #3 — the fault evidence table for the G4 sites

Read from the packet's four rows (`transaction_fault_matrix[T-REPAIR-DISPATCH, T-ANSWER, T-RETAINED,
T-RETRY]`), each row's `boundary`, `resume_action` and `refusal_condition` taken as the claim, and the
executed evidence beside it. "Real kill" means a child process was killed; "synthetic" means the
durable prefix was constructed byte-for-byte; "resumed" means the state was handed to a fresh
incarnation through the production recovery order.

| Row | Boundary reached by | Recovery / refusal observed | Test or measurement | Ran or reasoned |
|---|---|---|---|---|
| T-REPAIR-DISPATCH | `task_dispatched(repair)` durable, no worktree (synthetic) | `Worktree.Verify` fails `NotRegistered`; forced recreate at the recorded base; the continuation materializes once (`Clean`), one dispatch, one attempt; the worker sees the source's bytes; the lineage merges | `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` (`NoWorktree` prefix) | ran |
| T-REPAIR-DISPATCH | worktree at base, `MERGE_MSG.lock` held (synthetic: the pick killed inside its last write) | `Worktree.Verify` fails `Residue(MergeMsg)`; recreated; materialized once | same test, `HeldMessageLock` prefix; `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` (K3) | ran |
| T-REPAIR-DISPATCH | materialization completed and its state files cleared, process dead before `attempt_started` (synthetic) | `Worktree.Verify` passes (`Reuse::Verified`); recovery materializes nothing; the continuation restores the base's tree and picks once; the tree is the one-pick tree | same test, `CompletedMaterialization` prefix; `a_continuation_after_a_completed_pick_hands_the_worker_the_tree_one_pick_produces` (two resumes, index and working-tree bytes asserted) | ran |
| T-REPAIR-DISPATCH | a real child killed **before** and **after** the `Object.RepairMaterialize` site | the dispatch is durable; the reproduced materialization's tree equals a control's; the checkout equals the expected bytes | `repair_materialization_reproduced_after_kill` | ran (real kill) |
| T-REPAIR-DISPATCH | the Internal residue class, each of the site's four registered elements planted (synthetic: unreferenced object, temporary object file, `index.lock`, `CHERRY_PICK_HEAD`) | `classify_object_residue` = `Internal` for each; `Worktree.Verify` fails for the two git-dir elements and passes for the two object-store ones (the stated partition); exactly one pick reproduces the tree; both planted objects still exist afterwards (R27) | `repair_materialization_synthetic_residue_recreated_after_forced_removal` | ran |
| T-REPAIR-DISPATCH | the Internal residue class by **kill sampling**: real `cherry-pick --no-commit` children killed on a schedule derived from two probe picks | every sample classified; every worktree recovered to the one-pick tree; at least one kill landed while the pick was writing | `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`; five runs here: 40 kills in 48 spawns, kill classes `None`×27, `Internal`×8, `After`-without-`MERGE_MSG`×5, 8 completed picks as controls (§9 row 10 has the per-run histogram) | ran (real kills) |
| T-REPAIR-DISPATCH | after scrub | the merge blob the pick created is referenced by the repair index while the worktree exists (`fsck` does not list it unreachable) and unreachable once scrubbed, and still exists (`cat-file -e`); the source commit still exists | `repair_materialization_objects_released_to_git_on_scrub` | ran |
| T-REPAIR-DISPATCH | `refusal_condition`: source object missing | an absent object, an absent ref and a ref naming another commit are each refused before any append and before any intent is written | `a_repair_whose_source_candidate_is_missing_is_refused_before_any_append` | ran |
| T-ANSWER | a `.partial` staged, nothing published (synthetic) | the reader answers `None` and leaves the partial byte-identical; the loop hard-blocks and leaves it on disk | `a_staged_partial_is_never_ingested_and_a_published_answer_survives_ingestion`; `G4H` | ran |
| T-ANSWER | a complete answer file published while the engine is away | the next incarnation's **first** step ingests it (`Progress::Answered`, `via: "event-log"`), then dispatches the activated repair; the file survives ingestion (R21) | `an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step`; `G4H` | ran |
| T-ANSWER | `budget_exceeded` recorded in the epoch, then an answer | the fold refuses `question_answered` (`RunEnding{the budget stop}`) in that epoch and accepts it after `run_resumed`; the loop ingests the file at the next incarnation | `an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch`; `G4H` | ran |
| T-ANSWER | a halting settlement in the epoch, then an answer | refused `RunEnding{a halting settlement}` in that epoch; accepted after `run_resumed`; `defer_wait_elapsed` refused the same way | same fold test | ran |
| T-ANSWER | invalid option; unknown or already-answered question; second answer | refused before any append (`WrongQuestion`), live and on replay with equal errors | `an_answer_names_an_open_question_of_that_task_and_an_option_it_offered`; `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`; `G4C2` (second answer, live = replay) | ran |
| T-ANSWER | decline of a repair admission | the lineage fails, the lease is released, `halted_at` set exactly when `decline_halts_run` | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`; `G4B2`, `G4B3`, `G4B4` | ran |
| T-ANSWER | after finalization | **not reachable at this range**: no production `run_finished` emitter (§2) | — | reasoned |
| T-RETAINED | a retained repair generation seen by a fresh process (resumed) | recovery step (e) appends `generation_closed{ResumeDiscardsRetainedSession, LineageHeld}` before `run_resumed`; the worktree and intent are reclaimed; the next generation is a fresh dispatch materialized again from the same recorded source; a later resume closes nothing | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` | ran |
| T-RETAINED | a retained repair generation whose worktree is missing, live | `settle::retry` returns `Close{WorktreeMissing, LineageHeld}` without starting; reservations balance; the fold accepts that close and refuses `PredictedReleased` and `PredictedRetained` for it | `G4G`; the ordinary-generation shape in `retained_generation_closed_when_worktree_missing` and `retained_worktree_with_residue_closed_not_retried` | ran |
| T-RETAINED | a retained generation left by a killed process (real kill) | the dead process appended no second `attempt_started`; recovery closes it; a retry in a new process is refused | `retained_generation_not_continued_after_kill` | ran (real kill) |
| T-RETAINED | at run end | `close_retained` with `RunEnding{outcome}`; **the closure procedure that calls it is PR10's** | `retained_generation_closed_at_run_end` | ran (the function), reasoned (the procedure) |
| T-RETRY | the retaining incarnation retries a repair generation in place | one dispatch, two `attempt_started` (`a1` observed `Clean`, `a2` with `resume_session` observed `Retained`); `Object.RepairMaterialize` executed once; exactly the second worker invocation resumes a session | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` | ran |
| T-RETRY | a fresh process retries | refused at the settle layer before the worktree is verified, and the reservation is released | `retry_refused_after_resume` (settle and recover) | ran |
| T-RETRY | a forged retry with a stale incarnation, or a settlement retaining for a foreign epoch | refused by the fold | `retry_refused_with_stale_incarnation` | ran |
| T-RETRY | a kill during the retry attempt | settles interrupted and closes the generation | `kill_during_retry_attempt_closes_generation` | ran (real kill) |
| T-RETRY | wrong disposition on a lineage generation's settlement | `attempt_interrupted{PredictedReleased}` refused, `{LineageHeld}` accepted, on an in-flight repair attempt | `G4G`; `a_closing_generation_admits_exactly_the_disposition_its_holding_implies` | ran |

Every row above was executed at `74da2cbb` except the two marked reasoned, and both of those are
reasoned because their subject — a finalized run — does not exist at this range.

---

## 6. Required artifact #4 — limit and admission outputs, including ST-12

### 6.1 The frozen limit and the over-limit admission — `G4C1`

Fixture: two tasks, `max_merge_repairs = 0` frozen at `run_started`; alpha's stale verification at
beta's published head is rejected by review at sequence 1.

```
G4C1 run_started.limits.max_merge_repairs=0
G4C1 rejection: merge_rejected s1 candidate=k0g0@837b33d3 head=c212353e disposition=CodeRejected(Rejected) lease_effect=CreatesLineage { root: TaskKey(0), paths: Prefixes { paths: [GitPath("candidate.txt")] } } repair=k2 admission=HumanRequired{limit:0, q:q-park-fixed kind:Continue options:["retry this task (typed text un-parks it and is not passed to the agent)", "give up on this task (`skip`) — its dependents will be blocked"]} lineage=Some(Lineage { root: TaskKey(0), parent: TaskKey(0), index: 0 })
G4C1 repair ladder: tiers=[Mid] rungs=["Mid/claude-code/claude-opus-5/pinned false"] floor=Some(Mid) ceiling=Some(Mid) attempts_per=2 admission=Runnable
G4C1 states: repair=Some(AwaitingInput) alpha=Some(AwaitingRepair) questions=Some([("q-park-fixed", 2, Admission, None)]) outcome=Ending(Parked)
G4C1 parked-resume progress=[Ok(Blocked { questions: 1 }), Ok(Blocked { questions: 1 })] appended=["run_resumed"] spend_delta=0 runs=0 source_ref_after=Some("837b33d335d996236155096cba573bb0c4961280")
G4C1 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false })
G4C1 step2 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4C1 step3 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), merged_sha: CommitSha("46dea2c442bb7dffee750a21ea5e8eb6d2d50d76") })
G4C1 #15 question_answered k2 q=q-park-fixed answer=Answered { option_index: 0, binding_override: None } via=driven || ... | questions=0 | states=0:AwaitingRepair,1:Merged,2:Pending | ...
G4C1 #16 task_dispatched k2 g0 base=c212353e lease=InheritedLineage { root: TaskKey(0) } source=k0g0@837b33d3 || ...
G4C1 #17 attempt_started k2 g0 a1 rung0 binding=claude-code/claude-opus-5/High/tier Mid/pinned false resume=false mat=Some(Clean) || ...
G4C1 #21 task_merged s2 sha=46dea2c4 satisfies=[0, 2] release=Lineage { root: TaskKey(0) } || lineages=none | queue=empty | ... | states=0:Merged,1:Merged,2:Merged | ...
```

Read off the lines: the limit is frozen in `run_started` and the rejection's admission reports that
same limit; the repair's ladder is a complete runnable ladder (the Mid rung, floor and ceiling Mid)
that is *not* run because the admission is `HumanRequired`; the run derives `Parked`; two steps of a
resumed incarnation with no answer append **only `run_resumed`**, run no process and spend nothing;
the activating answer carries **no** binding override (a `HumanRequired` question authorizes none),
the repair dispatches into the lineage lease from the rejected candidate at the head current at
dispatch, runs under the frozen Mid rung, and the lineage publishes with `satisfies=[0, 2]`.

### 6.2 The empty intersection, the validated binding, and the four refusals — `G4C2` (ST-12)

Fixture: the root's frozen chain is the one Small rung, so the repair's Mid floor intersects it empty.

```
G4C2 root ladder: tiers=[Small] rungs=1 floor=None ceiling=Some(Small) attempts_per=2
G4C2 repair ladder: tiers=[] rungs=0 floor=Some(Mid) ceiling=None attempts_per=2 admission=HumanBinding { options: ["claude-code"] }
G4C2 admission=HumanBinding{options:["claude-code"], q:q-park-fixed kind:Unblock q.options:["claude-code"]}
G4C2 non-option: progress=[Err("question q-park-fixed asks which agent runs task 2, and `gpt-5` is none of the 1 it offers; a one-off binding activates only through an option the spawn froze, so nothing was appended")] appended=["run_resumed"] state=Some(AwaitingInput)
G4C2 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: false })
G4C2 step2 Ok(Settled { key: TaskKey(2), accepted: true, spent_attempt: true })
G4C2 step3 Ok(Integrated { key: TaskKey(2), sequence: SequenceId(2), merged_sha: CommitSha("1d1468b6140d0884b100f68c317ef64becc664b3") })
G4C2 #15 question_answered k2 q=q-park-fixed answer=Answered { option_index: 0, binding_override: Some(BindingOverride { key: TaskKey(2), question: QuestionId("q-park-fixed"), option_index: 0, agent: "claude-code", model: "claude-sonnet-4-5", effort: High }) } via=driven || ... | overrides=k2=claude-code/claude-sonnet-4-5/High | questions=0 | states=0:AwaitingRepair,1:Merged,2:Pending | ...
G4C2 #17 attempt_started k2 g0 a1 rung0 binding=claude-code/claude-sonnet-4-5/High/tier Mid/pinned true resume=false mat=Some(Clean) || ...
G4C2 override=Some(BindingOverride { key: TaskKey(2), question: QuestionId("q-park-fixed"), option_index: 0, agent: "claude-code", model: "claude-sonnet-4-5", effort: High }) rung_binding(0)=Some(RungBinding { tier: Mid, agent: "claude-code", model: "claude-sonnet-4-5", pinned: true, effort: High }) frozen_rung_binding(0)=None
G4C2-second-answer live=Err("`question_answered` names question `q-park-fixed`, which has already been answered; a question is answered once")
G4C2-second-answer replay=Err("`question_answered` names question `q-park-fixed`, which has already been answered; a question is answered once")
G4C2-second-answer live_and_replay_equal=true both_refused=true
G4C2 authorized attempt_started accepted=Ok(())
G4C2 non-override attempt_started (root's frozen rung Small/claude-code/claude-haiku-4-5): live=Err("attempt 1 of task 2 runs a binding the run never froze for it: a human named `claude-code`/`claude-sonnet-4-5` at effort `high`, which authorizes tier `mid` pinned `true` on this ladder's floor, and it ran `claude-code`/`claude-haiku-4-5` at effort `low`, tier `small` pinned `false`. Run-start exact bindings are execution identity.")
G4C2 non-override attempt_started replay=Err("... the same text ...") equal=true
G4C2 override binding with pinned=false: Err("attempt 1 of task 2 runs a binding the run never froze for it: ... it ran `claude-code`/`claude-sonnet-4-5` at effort `high`, tier `mid` pinned `false`. ...")
G4C2 replay_twice_equal=true events=22 outcome=Ending(Complete)
```

So: the empty intersection registers `HumanBinding` over the root's allowed agents with the floor
`Mid` and **no rung**; an answer naming no frozen option is refused before any append (only the
resume's own `run_resumed` was appended, the task still `AwaitingInput`); the validated answer carries
the five-field override derived once at ingest (the catalogue's lowest `claude-code` model at or above
the floor, the policy's Mid effort); the attempt runs under exactly that binding **pinned at the floor**
and the frozen half of the reader has nothing to say (`frozen_rung_binding(0) = None`); a second answer
is refused live and on replay with byte-equal errors; a non-override `attempt_started` — the root's own
Small rung, the thing the floor excluded — is refused live and on replay with byte-equal errors; and an
override binding with the pin cleared is refused too.

**The single-rung ladder, at the level it is decided.** `ladder_policy` in `src/engine/topology/run.rs`
gives a task with a recorded override a ladder of **one** rung and otherwise the frozen ladder's rung
count, which for an empty-intersection repair is **zero**. Measured against `next_step` directly
(`G4C3b`):

```
G4C3b no override (empty repair ladder, rungs=0): attempts_on_rung=1 -> Fail
G4C3b no override (empty repair ladder, rungs=0): attempts_on_rung=2 -> Fail
G4C3b override recorded (single-rung ladder, rungs=1): attempts_on_rung=1 -> RetrySameRung { resume: false }
G4C3b override recorded (single-rung ladder, rungs=1): attempts_on_rung=2 -> AskHuman(Unblock)
```

So an overridden repair that exhausts its `attempts_per` reaches the **human rung**, where an
un-overridden empty ladder would `Fail` on the first failure. The engine-level drive of this
(`G4C3`, a gate failing under the override) reached the point where the exhausted attempt's settlement
tried to park with a question — and the fixture's fixed-id question source refused that second question
as a reused identity, so the drive could not observe the park itself. That is a fixture limit, noted in
§11, not a finding against the code; whether any committed test observes the exhaustion end-to-end is
answered by mutation M34 in §3.

### 6.3 The decline, and what it halts — `G4B2`, `G4B3`, `G4B4`

```
G4B3 rejection admission=HumanRequired{limit:0, ...} repair=k2
G4B3 before: states alpha=Some(AwaitingRepair) beta=Some(Pending) beta_ready=false outcome=Ending(Parked)
G4B3 step1 Ok(Answered { key: TaskKey(2), question: QuestionId("q-park-fixed"), declined: true })
G4B3 #07 question_answered k2 q=q-park-fixed answer=Declined { decline_halts_run: true } via=driven || lineages=none | queue=empty | ... | questions=0 | states=0:Failed,1:Pending,2:Failed | halted=Some(2) | ...
G4B3 after: alpha=Some(Failed) repair=Some(Failed) beta=Some(Pending) beta_ready=false halted_at=Some(2) run_is_ending=true outcome=Ending(Halted) lineage_or_candidate_lease_held=false
G4B4 after: alpha=Some(Failed) repair=Some(Failed) beta=Some(Pending) beta_ready=false halted=None outcome=Ending(Complete) leases_held=false
```

With beta depending on alpha: under a halting policy the decline sets `halted_at` to the answered
task, the run derives `Halted`, and beta stays `Pending` and not ready; under a non-halting policy the
same decline leaves beta `Pending`, not ready, and the run derives `Complete` — which is the fold's
`blocked_tasks` view (a Pending task whose dependency failed is Blocked, and a run of Merged, Failed and
Blocked is complete). In every decline the lease table is empty afterwards.

---

## 7. Required artifact #5 — the payload freezing check

The claim: a `merge_rejected` freezes the complete repair in its one append, nothing is regenerated
after an answer, the limit it reports is the run's frozen one, and a repeat of the record is refused.

**The frozen payload, verbatim from the event** (`G4C1`; the `HumanBinding` shape in `G4C2` differs
only in the ladder and admission fields, both quoted in §6.2):

```
G4C1 frozen_spawn_json={"key":2,"entry":{"key":2,"display_id":"merge-fix-0000-alpha","origin":"merge_repair","spec":{"kind":"fix","title":"alpha","body":"alpha body\n\n## Merge repair\n\nCandidate 837b33d335d996236155096cba573bb0c4961280 (refs/upstroke/runs/01KZTPR7E00000000000000001/candidates/0/0) of task 0 generation 0 was rejected at integration sequence 1 against the integration head c212353e739412a0c8956110e9ba47f02beb2dfc.\n\nThe verification of the proposal on that head ended rejected (gates passed): the reviewer rejected the proposal\n","acceptance":["alpha passes","preserve the behaviour already merged onto the integration head; the rejected candidate's change must integrate without regressing it"],"path_hints":["src/alpha/*.rs","candidate.txt"],"suggested_tier":null,"min_tier":null,"artifacts_in":[],"artifacts_out":["alpha-out"]},"deps":[],"display_deps":[],"ladder":{"tiers":["mid"],"attempts_per":2,"rungs":[{"tier":"mid","agent":"claude-code","model":"claude-opus-5","pinned":false}],"floor":"mid","ceiling":"mid","effort":{"small":"low","mid":"high","frontier":"max","review":"medium"},"admission":"runnable"},"reviews":{"enabled":true,"alternative_available":false,"pass_timeout_secs":600,"primary":{"agent":"claude-code","model":"claude-opus-5"},"alternative":null,"second_opinion":null},"allowed_agents":["claude-code"],"lineage":{"root":0,"parent":0,"index":0}},"admission":{"admission":"human_required","limit":0,"question":{"id":"q-park-fixed","key":2,"kind":"continue","context":"this lineage has consumed its 0 automatic repair(s); a person must approve another attempt with the latest evidence, or decline the lineage","options":["retry this task (typed text un-parks it and is not passed to the agent)","give up on this task (`skip`) — its dependents will be blocked"]}}}
```

Every field `decisions.repairs.merge_rejected` lists is present in the one record: the spec embedding
the candidate and rejecting-head evidence, the original acceptance plus the preserve-merged-behaviour
requirement, the expanded path hints (the root's hints plus the candidate's actual changed path), the
resolved ladder with exact rung bindings (or `rungs: []` with `HumanBinding` options), the effort
policy, the reviews, the allowed agents, the lineage, and the admission with its complete question keyed
by the repair's `TaskKey`.

**Nothing is regenerated after the answer.** After the activation and the merge:

```
G4C1 freezing: registry_entry_equals_frozen_payload_entry=true dispatched_source_equals_rejected_candidate=Some(true) override=None source_ref_after_merge=Some("837b33d335d996236155096cba573bb0c4961280")
```

The registry entry the fold holds after the answer is **equal** to the `entry` the rejection froze
(`TaskEntry` equality over every field), and the dispatch's `source_candidate` is equal to the
rejection's `candidate`; the dispatcher reads both from the log (`rejected_source` and
`dispatched_source` in `src/engine/topology/run.rs`), never from anything rebuilt.

**The limit is the run's.** The fold refuses a rejection whose `HumanRequired` reports any limit but
`run_started.limits.max_merge_repairs` (`check_admission` in `src/topology/fold/check_attempt.rs`), a
`Runnable` admission past the limit, and a `HumanRequired` one below it (`check_merge_rejected` in
`src/topology/fold/check_integration.rs`); mutations M16 and M17 in §3 measure whether those refusals
still detect.

**A repeat of the record is refused, live and on replay, with equal errors:**

```
G4C1-duplicate-merge_rejected live=Err("`merge_rejected` names integration sequence 1, and the open transaction is none; an event applies to the transaction it belongs to or to none")
G4C1-duplicate-merge_rejected replay=Err("`merge_rejected` names integration sequence 1, and the open transaction is none; an event applies to the transaction it belongs to or to none")
G4C1-duplicate-merge_rejected live_and_replay_equal=true both_refused=true
```

and the committed fold test `a_rejection_already_folded_is_refused_when_it_arrives_again` adds the
other case — the same candidate at the *next* sequence — and asserts the registry and the lineage's
member count are unchanged by the refusal.

**Replay-twice** holds for every measured log in this report (`replay_twice_equal=true` on `G4A`,
`G4B`, `G4B2`, `G4B3`, `G4B4`, `G4B5`, `G4C1`, `G4C2`, `G4E`, `G4H`), each read from the on-disk bytes.

---

## 8. Required artifact #6 — repair-source protection evidence

R11 says the rejected candidate's candidates ref is *"protected while the run can resume, whatever the
candidate's state"*, pruned only by Complete finalization. The evidence, in three parts.

**(a) Observed across every resumable outcome in range.** The ref's target was read before and after
each transition, and it is the rejected commit at every reading:

```
G4C1 source: ref refs/upstroke/runs/01KZTPR7E00000000000000001/candidates/0/0 -> Some("837b33d3...") candidate_commit=837b33d3... object_exists=Ok(true) in_expected_refs=true
G4C1 parked-resume ... source_ref_after=Some("837b33d3...")            (Parked, resumed with the question still open)
G4C1 freezing: ... source_ref_after_merge=Some("837b33d3...")           (after the activated repair dispatched from it and merged)
G4E before: budget_stop=Some(0) outcome=Ending(BudgetExceeded) source_ref=Some("28ca9fd5...") expected_refs_has=true
G4E after: budget_stop=None source_ref=Some("28ca9fd5...") dispatched_source_eq=Some(true) alpha=Some(Merged) repair=Some(Merged)
G4B5 source_ref_after=Some("1e99be30...") rejection_candidate_eq_planted=Some(true) dispatch_source_eq=Some(true) object_exists=Ok(true)
```

`G4B5` is the one where the rejection is **produced by the production integration path** (a gate fails
during the verification of a stale candidate, `CodeRejected(GatesFailed)`) rather than planted, and the
repair then dispatches from the ref that rejection left; the committed
`a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume` holds the
budget-stop case with assertions.

**(b) Read as an entitlement, not merely left alone.** The ref is in `expected_refs` (`in_expected_refs=true`
above), the set recovery derives from the fold and hands to `refuse_unexpected_refs` before it touches
anything, so a resume that found it *missing* would be refusing on the run namespace, not adopting an
absence. And `refuse_absent_source` in `src/engine/topology/dispatch.rs` refuses a dispatch whose source
object is missing, whose ref is missing, or whose ref names another commit — before any append and
before any intent — which `a_repair_whose_source_candidate_is_missing_is_refused_before_any_append`
drives for all three shapes and mutation M21 measures.

**(c) Structurally, at this sha, the ref cannot be deleted by this build.** `grep -rn DeleteCandidatesRef src/`
outside test modules finds the `RefSite` variant, its row (`R11`), its adjacency (`After(RunFinished)`)
and its fault row (`T-FINALIZE`) in `src/topology/effects/sites.rs` — and **no production caller**. The
four production call sites of `delete_ref_expected_old` are `DeletePreparedPin` (twice: `prune_pin` in
`src/engine/topology/integrate.rs` and the orphan reclaim in `src/engine/topology/recover.rs`) and
`DeleteCandidatePin` (twice, in `src/engine/topology/candidate.rs`). Finalization is PR10's, so the only
site that may prune a candidates ref has no caller yet. This is stronger than a guard and weaker than a
proof for the future: it says the protection cannot be violated by *this* tree, and mutations M22 and
M23 in §3 say what would catch a rejection arm that deleted it.

**(d) The object behind the ref is the source, byte for byte.** Three committed tests read the
worker's checkout and assert it holds the protected source's content after the materialization —
`the worker is handed the protected source's bytes, whatever the kill left`
(`a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once`),
the fresh-generation and the same-session-retry tests — and the synthetic-residue test asserts the
recovered tree equals a control materialization's tree and that the planted orphan objects still exist
afterwards (R27: never deleted).

---
## 9. The twelve adversarial fault tests

Read from `.cumulative_review_gates.gates[G4].adversarial_fault_tests`, not from a summary. Each row
names what holds the claim, the result at `74da2cbb`, whether this gate **ran** it or **reasoned** it,
and the mutation from §3 that proves the named test still detects. All 48 committed tests named in
this section pass unmutated (one filtered run, 48 passed, 0 failed, 3.24 s), and the measurements
named `G4…` are the temporary ones of §0.

| # | Adversarial claim | Held by | Result | Ran / reasoned | Detection witness (§3) |
|---|---|---|---|---|---|
| 1 | two overlapping lineages resolve in creation order; no deadlock | `two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`; the trace `G4A` (§4.1): the younger candidate `BehindOlderLineage` at every prefix until the older publishes, then eligible, re-verified and published; both leases released once; `Ending(Complete)` | **pass** | ran | M01 (lineage order inverted) → 2 failures; M03 (lineage lease never released) → 7 |
| 2 | an overlapping non-lineage candidate is skipped until the lineage merges or fails, then re-verified stale | the queue rule `an_ordinary_candidate_waits_for_any_lineage_and_a_member_only_for_older_ones`; **constructed here** for the loop: `G4B` (waits `InsideLineage`, the lineage merges, the candidate re-verifies `StaleClean` on the moved head) and `G4B2` (the lineage is declined, the candidate publishes in the next step). No committed test drives the loop through this shape | **pass** | ran (constructed) | M02 (InsideLineage dropped) → 1; M04 (holdings not released on lineage failure) → 5 |
| 3 | over-limit rejection registers `AwaitingInput`; nothing spends until the answer | `an_over_limit_repair_spends_nothing_until_its_answer_activates_it`; `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` (polled and blocking deliveries); `G4C1` (§6.1: only `run_resumed` appended across two blocked steps, zero spend, zero processes) | **pass** | ran | M17 (never `HumanRequired`), M16 (a `Runnable` past the limit accepted by the fold), M33 (answers never polled) |
| 4 | empty pin/ceiling intersection registers `HumanBinding` options; a validated answer activates a single-rung ladder; a non-option and a second answer refused; a non-override `attempt_started` refused (ST-12) | `a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run`; `picking_the_last_of_two_offered_agents_binds_the_repair_to_it_rather_than_declining`; `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`; `an_override_replaces_the_frozen_binding_for_every_later_attempt`; `G4C2` (all four refusals, the second answer and the non-override start each refused live and on replay with equal errors); the single-rung consequence at the ladder in `G4C3b` (§6.2) | **pass** | ran; the single-rung ladder's exhaustion measured at the ladder level and reasoned through `ladder_policy` | M18 (no `HumanBinding` ladder), M06 (non-override start accepted), M08 (second answer accepted), M09 (out-of-range option accepted), M35 (the override not a pin) all caught; M34 (override no longer single-rung) **survived** — the loop-level witness is O2 |
| 5 | a declined admission fails the lineage; the lease is released; dependents `Blocked`; a decline under a halting policy sets `halted_at` | `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to`; `declining_a_repair_admission_fails_the_lineage_and_allows_the_run_to_end`; `a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay` (the member's own terminal failure, the other route to the same fold); `G4B3` (`halted_at=Some(2)`, `Ending(Halted)`, the dependent `Pending` and not ready) and `G4B4` (the same without a halt: `Ending(Complete)` with the dependent blocked, lease table empty) | **pass** | ran | M04 (holdings kept) → 5; M05 (member failure does not fold the lineage) → 1; M19 (no `halted_at` on a halting decline) |
| 6 | a repair with an already-present source behaves as an ordinary attempt; an empty diff fails honestly | `an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly` (`Materialization::Empty`, the attempt starts, the capture is the base's tree, the assessment is `EmptyDiff`); `a_repair_dispatch_records_what_its_materialization_observed` (`Clean`, `Empty`, `Conflict` each observed and recorded, the index state asserted for each) | **pass** | ran | positive-behaviour claim; the observation's presence parity is guarded by M07 (caught by the full suite) and its recording by M37 |
| 7 | a same-session retry on a repair generation records `LineageHeld` and does not repeat the re-materialization; a wrong disposition refused | `a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` (one `Object.RepairMaterialize` execution across two attempts; `a2` records `Retained` and resumes the session); `a_closing_generation_admits_exactly_the_disposition_its_holding_implies`; `G4G` (`attempt_interrupted{PredictedReleased}` refused and `{LineageHeld}` accepted on an in-flight repair attempt) | **pass** | ran | M12 (`Retained` not recorded); the disposition refusal is exercised by `check_lease_disposition`'s own table test and by G3's M-series on the same function |
| 8 | a `Retained` repair generation missing its worktree, or resumed: `generation_closed{LineageHeld}` then a fresh generation re-materialized from the protected source (ST-11) | `a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` (resumed: closed with `ResumeDiscardsRetainedSession`/`LineageHeld` before `run_resumed`, worktree and intent reclaimed, generation 1 dispatched from the same source and materialized once, a later resume closes nothing); `G4G` (missing worktree, live: `Close{WorktreeMissing, LineageHeld}`, reservations balanced, the fold refuses either predicted disposition) | **pass** | ran | M13 (recovery closes no retained generation), M29 (closed generations never reclaimed), M30 (a failed verify starts the retry anyway) |
| 9 | a parked or budget-stopped lineage redispatches after resume with its source candidate object present (R11) | `a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume`; the over-limit pair (Parked); `G4E` and `G4C1` (§8: the ref read before and after each resume and after the merge) | **pass** | ran | M21 (absent source not refused) caught; M22/M23 (a rejection arm that deleted the ref) **survived** — O1 |
| 10 | the interrupted-materialization residue class: a kill after materialization before `attempt_started` leaves index-referenced merge objects that a later scrub releases to Git; the residue (unreferenced objects plus `CHERRY_PICK_HEAD`/`index.lock`) constructed synthetically **and** sampled by killing the cherry-pick child, failing `Worktree.Verify`, so the worktree is recreated and the materialization reproduced deterministically | `repair_materialization_objects_released_to_git_on_scrub` (R9 → R27 measured by `fsck`); `repair_materialization_synthetic_residue_recreated_after_forced_removal` (all four registered elements; `Internal`; recreated for the two git-dir elements, verified for the two object-store ones — the tree's stated partition; one pick; tree and checkout equal a control's); `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` (real children killed; every sample classified and recovered; a mid-write kill required); `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states` (K2 and K3); `repair_materialization_reproduced_after_kill` (real kills before and after the site); `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` (across the process boundary) | **pass**, with one qualification stated below the table | ran (real kills and synthetic) | M14 (a failed verify not recreated), M15 (no base restore before the pick), M36 (recovery step (g) verifies nothing), M37 (the continuation does not materialize) |
| 11 | attempted overtaking refused live and on replay | `an_integration_starts_only_for_the_first_eligible_candidate`; `the_queue_is_ordered_by_creation_and_not_by_preparation`; `G4A` (a forged `merge_verification_started` and a forged fast `merge_prepared` for the younger lineage's candidate ahead of the older lineage: both refused `NotFirstEligible`, live error byte-equal to the replay error) | **pass** | ran | M01; G3's M13 on the same `NotFirstEligible` refusal is not re-run — the refusal is unchanged in range and `G4A` exercises it through the lineage rule |
| 12 | kills at T-REPAIR-DISPATCH, T-ANSWER, T-RETAINED and T-RETRY | §5, every row | **pass** | ran, except the two finalization rows, reasoned | as §5's rows cite |
| 13 | duplicate `merge_rejected` replay refused | `a_rejection_already_folded_is_refused_when_it_arrives_again` (same sequence, and the next sequence; registry and lineage count unchanged); `G4C1` (the same record appended again: refused live and on replay with equal errors, `WrongSequence`) | **pass** | ran | the refusal is `open_transaction`'s and G3's M12/M14 cover its neighbours; the duplicate case here is asserted by a positive test that would fail if the repeat were accepted (the registry count assertion) |

(The packet lists twelve; row 12 of its list bundles four kill sites, which §5 unpacks, and this table
numbers the duplicate-rejection claim 13 so that no row carries two claims.)

**The qualification on row 10.** The sampled half of that row is established by
`sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`, which is the
PR9 sampler with the two-probe warm budget, **not** the T-PROPOSAL sampler
(`sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`) that carries the known
cold-probe finding. Both were run here five times each, with their histograms printed by a temporary
`println!`:

| Sampler | Run | Spawns | Died by the kill | Kill classes (`None` / `Internal` / `After`) | Completed (controls) | Result |
|---|---|---|---|---|---|---|
| RepairMaterialize (PR9's) | 1 | 10 | 8 | 5 / 2 / 1 (the `After` before `MERGE_MSG`) | 2 | pass |
| RepairMaterialize | 2 | 9 | 8 | 5 / 1 / 2 | 1 | pass |
| RepairMaterialize | 3 | 10 | 8 | 6 / 2 / 0 | 2 | pass |
| RepairMaterialize | 4 | 10 | 8 | 5 / 2 / 1 | 2 | pass |
| RepairMaterialize | 5 | 9 | 8 | 6 / 1 / 1 | 1 | pass |
| ProposalCherryPick (G3's, the known flake) | 1 | 8 | 7 | 3 / 3 / 1, plus one completed pick | 1 | pass |
| ProposalCherryPick | 2 | 8 | 8 | 2 / 5 / 1 | 0 | pass |
| ProposalCherryPick | 3 | 8 | 8 | 5 / 3 / 0 | 0 | pass |
| ProposalCherryPick | 4 | 8 | 7 | 3 / 4 / 0, plus one completed pick | 1 | pass |
| ProposalCherryPick | 5 | 8 | 7 | 3 / 3 / 1, plus one completed pick | 1 | pass |

Every kill in every run was classified into one of the site's three classes and recovered, and
every RepairMaterialize run contained at least one kill that landed while the pick was writing
(`Internal`, or `After` with `MERGE_MSG` not yet written), which is that sampler's own floor. The
known finding `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` — all eight samples exiting cleanly when the
one cold probe misrepresents the runs — **did not fire on this box in five runs** (37 kills in 40
samples), and its filed observations are macOS runs. If it fires on a required leg of this gate's
own pull request, that is the known finding and not a new one, and the sampled half of row 10 stands
on the PR9 sampler, which has the warm-probe budget the finding asks for and did not misfire here.
The known finding is **not** a G4 failure: row 10's claim is about `Object.RepairMaterialize`, its
sampler passed, and the T-PROPOSAL sampler is G3's evidence.

**All thirteen rows pass.** Two rows (6 and 13) carry positive-behaviour claims without a refusal of
their own to mutate, and each is asserted by a test that fails if the behaviour stops.

---

## 10. The six standing questions

The gate process says a gate *"may claim only resources and invariant portions introduced in its
frozen range, naming the portion and the inventory rows."* Each answer opens by naming G4's portion
and closes with a verdict of **answered with evidence**, **answered in the G4 portion only**, or
**not answered**. Nothing is claimed for a portion belonging to G1, G2, G3 or a later gate.

### Q6 — sequential guarantees preserved

**G4's portion.** FIFO integration and no overtaking *as they apply to lineage candidates*; atomic
settlements for the lineage lifecycle; the identical canonical trace where scheduling must not matter
(two lineages); the derived run outcome over the states a lineage can leave a run in; and no answer
ingestion after a budget stop or a halting settlement in the same epoch. The remaining clauses —
exact-commit verification, exact-base fast publication, one writer, one Runner, `budget_exceeded`
before any budget-driven end — are G1–G3's and are not re-claimed.

| Guarantee | Evidence in range |
|---|---|
| **FIFO integration, no overtaking — lineage candidates** | a lineage candidate owns a queue position (pushed once at `task_candidate_created`, `G4A` #14, #19) and no candidate lease (`WidensLineage` grants none: `a_lineage_members_candidate_widens_its_lineage_and_takes_no_lease_of_its_own` in `src/topology/fold/apply.rs`; the trace's lease table shows only `L0`/`L1` throughout); eligibility is decided by the queue's own rule against the lease table — `InsideLineage` for an ordinary candidate overlapping any lineage, `BehindOlderLineage` for a member overlapping an older one — and `check_transaction_start` refuses any start that is not the first eligible entry, with the reason named (`G4A`: both forged starts refused, live = replay). M01, M02 |
| **atomic settlements** | `merge_rejected` carries the frozen repair, the lineage creation or widening and the parent's `AwaitingRepair` in one append (§7); `question_answered{Declined}` fails every unmerged member, releases every holding and closes the lineage's questions in one delta (`G4B2` #11: `lineages=none`, `states=0:Failed,…,2:Failed`, `questions=0` on the same line); a member's `attempt_finished{Failed}` does the same (`a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`); `generation_closed{LineageHeld}` closes without touching the lineage lease (`G4G`). M03, M04, M05 |
| **identical canonical trace where scheduling must not matter** | `G4A` is the packet's two-lineage case: the younger lineage's candidate was queued *first* and the loop still publishes in lineage order; the eligibility that decides it is a function of the replayed lease table at every prefix, so a different interleaving of the same events cannot reorder the publications. Schedule-independence under real concurrency is PR11's and is not claimed |
| **derived run outcome: halt > budget > parked > complete** | over the states in range: a halting decline derives `Halted` (`G4B3`), a budget stop derives `BudgetExceeded` and the resume clears it (`G4E`), an open admission question derives `Parked` (`G4C1`), a declined lineage with a blocked dependent derives `Complete` (`G4B4`), and a lineage whose member fails is total over the outcome function (`assert_ne!(derived_outcome, FoldError)` in the terminal-failure test). The ordering itself and `run_finished` are G1/PR10's |
| **no answer ingestion after a budget stop or halting settlement in the same epoch** | the fold refuses `question_answered` (`RunEnding`) after either in the epoch and accepts it after `run_resumed` (`an_answer_is_refused_after_a_halt_or_a_budget_stop_in_the_same_epoch`); the loop does not poll while `run_is_ending()` (`ingest_answers`) and ingests the published file at the next incarnation's first step (`G4H`). M10 |
| **untouched user checkout; exact-commit verification on immutable snapshots** | unchanged in range: every repair effect is confined to the `tasks/k<key>-g<gen>` slot under the execution root, and a lineage candidate is verified and published through the same `integrate` path G3 certified (`G4A` #22–#24, `G4B` #17–#19: a stale lineage candidate takes the staging path with a pin and a verification, exactly as an ordinary one) |

**Q6 verdict: answered with evidence for the G4 portion.**

### Q4 — aliasing

**G4's portion.** The lineage lease (R8), the lineage-candidate queue position (R6), the repair
generation worktree and intent (R9), the source candidate ref (R11), the admission question (R15), the
binding override (R16), and the repair-budget unit (R14).

| Aliasing hazard | Why it cannot happen | Witness |
|---|---|---|
| two lineages for one root | `LeaseTable::grant` for a `Lineage` owner replaces the existing entry's region rather than adding a second, and the age is assigned once (`src/topology/leases.rs`); a `CreatesLineage` rejection of a member is refused (`rejection_lineage_root`: a member's rejection must `WidensLineage`, an ordinary task's must create) | `a_rejection_creates_a_lineage_from_an_ordinary_candidate_and_widens_a_members`; `G4A` (two roots, two entries, two ages) |
| a repair holding a lease of its own beside the lineage's | `check_dispatched` refuses `Predicted` on a lineage member and `InheritedLineage` on an ordinary task; `check_lease_effect` refuses `ReplacesPredicted` on a member's candidate; `apply_dispatched` grants no `Generation` owner for an inherited lease | `an_inherited_lease_on_an_ordinary_task_is_refused_at_the_barrier_before_step_g`; the trace's lease table |
| two queue positions for one candidate, or a lineage candidate overtaking | one push per `task_candidate_created` after the generation closes (G3's M11, unchanged in range); the first-eligible rule with the lineage reasons | M01, M02; `G4A` |
| two repair generations of one task open at once | `check_dispatched` refuses while `task.open()` is `Some` and requires the dense next generation; `G4C3` shows generation 1 opening only after generation 0's settlement closed it (#17 → #18) | `keys_and_generations_are_dense_in_both_directions` |
| two overrides for one task, or an override for a task that asked for none | `overrides` is keyed by task; a second answer to the question is refused (`WrongQuestion: already answered`); an override on a question with no `binding`, or naming an agent other than the option's, is refused (`check_question_answered`) | `G4C2`; M06, M08, M09 |
| two open admission questions for one parked generation, or a reused question id | `check_new_question` refuses an id the log has already used (observed as the fixture artefact in `G4C3`: the second park with the fixed id was refused as reused) and a question keyed to another task; `check_question_can_park_lineage` refuses a park while the lineage has an open generation or transaction | `a_rejection_creates_or_widens_exactly_one_lineage_and_registers_its_repair`; §11 O3 |
| a repair worktree shared by two generations | the slot is `Slot::Task{key, generation}` (`task_slot`), so generation 1's worktree is a different path from generation 0's; recovery scrubs a closed generation's slot only when its intent is still present | ST-11 test: `!retained_worktree.exists()` after the close, generation 1 created fresh |
| materialization objects accounted twice | while the repair worktree exists its index references the merge blob (`fsck --unreachable` does not list it); once scrubbed it is unreachable and Git's (R27); no ref or pin ever names it | `repair_materialization_objects_released_to_git_on_scrub` |
| a fast publication naming an object other than the candidate | G3's portion, unchanged; a lineage candidate's fast publication proposes its own commit (`G4A` #20: `proposed=candidate=d5b5cf08`) | G3 §4 |

**Q4 verdict: answered with evidence for the G4 portion.**

### Q3 — stale, duplicate and out-of-order completions

**G4's portion.** A stale or foreign-incarnation retry; a duplicate rejection; a second answer; a
settlement or close naming a generation that is not the open one; a late `generation_closed` for an
in-flight generation; and whether the append-error protocol reaches the new sites.

- **A retry from another incarnation cannot continue a retained generation.** Refused at the settle
  layer before the worktree is verified (`retained()` in `src/engine/topology/settle.rs`, M31) and at
  the fold (`check_attempt_started`'s `StaleIncarnation`, M32); a settlement retaining for a foreign
  epoch is refused too (`retry_refused_with_stale_incarnation`). The retaining process's own
  `retained` map is process-local by design, so a fresh process has nothing to re-gate against and
  refuses (`retry_ready` in `src/engine/topology/run.rs`).
- **A duplicate `merge_rejected` cannot register a second repair.** `open_transaction` finds no
  transaction (code rejection) or `check_transaction_start` finds no eligible queue entry (conflict);
  the registry is unchanged (§7).
- **A second answer cannot mutate the task again.** The question is removed by the first answer's
  delta and a `question_answered` naming it is refused as already answered, live and on replay
  (`G4C2`, M08).
- **A close or settlement for the wrong generation or attempt is refused.** `open_generation`
  requires the named generation to be the open one; `in_flight` requires the named attempt to be the
  running one; `check_generation_closed` refuses a generation that is `InFlight` or `Promoting`
  (`only_an_idle_generation_is_closed`).
- **A returned append error cannot let a stale in-memory fold drive anything.** The protocol is
  `emit.rs`'s and is **unchanged in range** (`src/engine/topology/emit.rs` is not in the range's diff);
  the new sites append through it (`RunEmitter` in `src/engine/topology/run.rs`; recovery's `emit`),
  and PR9's own witness for the recovery-time case is
  `a_retained_generations_reclaim_the_closing_recovery_never_reached_is_finished_by_the_next` (an
  append error at the `Synced` point of the close on one resume, the reclaim finished by the next
  from durable state).

**Q3 verdict: answered with evidence for the G4 portion.** Slot release, cancel and provisional
conversion under concurrency are PR11's.

### Q2 — resource accounting

**G4's portion, by row.** The gate claims these nine and no others.

| Row | Resource | Class(es) across its lifecycle | Enforcement domain | State at `74da2cbb` |
|---|---|---|---|---|
| R8 | lineage lease | `resumably_open` while the lineage is active or parked; `released` at `task_merged` with the root in `satisfies`, or at lineage terminal failure (a member's `Failed` settlement, a decline) | logical_fold_broker | `G4A` #21/#24, `G4B2` #11, the terminal-failure test; released exactly once and a repeat refused (`a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused`); never touched by an attempt-level settlement or `generation_closed` (`G4G`, `G4C3` #17: `LineageHeld` leaves the table unchanged). M03, M04, M25 |
| R6 | lineage-candidate queue position | `resumably_open` while queued or skipped; `consumed` at `task_merged` or `merge_rejected` | logical_fold_broker | one entry per candidate, pushed at `task_candidate_created`, removed by the terminal (`G4A` #19 → #21); ineligible-but-held while behind an older lineage |
| R9 | repair generation worktree + intent, incl. the materialization objects its index references and any administrative residue | `resumably_open` while `OpenNoAttempt` (reused only after `Worktree.Verify`, else recreated with force) or `RetainedIdle`; `pruned` at close, with the intent, objects released to R27 | external_physical | verified/recreated at (g) and by the continuation (the three-prefix test, `G4G`); the closed generation's worktree and intent reclaimed by the resume's sweep and stay reclaimed (ST-11 test); objects referenced while it exists (`repair_materialization_objects_released_to_git_on_scrub`). M14, M29, M36 |
| R27 | materialization objects once unreferenced, or written by an interrupted pick | `released` to Git; never deleted, never adopted | external_physical | the scrub test (`cat-file -e` succeeds after the scrub); the synthetic-residue test (both planted objects still exist after every recovery) |
| R11 | source candidate ref | `resumably_open`, protected while the run can resume | external_physical | §8: present across Parked and BudgetExceeded resumes and after the merge; in `expected_refs`; no production `DeleteCandidatesRef` caller at this sha. M21 caught; M22, M23 survived (O1) |
| R15 | admission question | `resumably_open` while open; `consumed` at `question_answered` | logical_fold_broker | one per parked generation, keyed to the repair, complete (`is_complete`); removed by the answer's delta; a decline removes every question of the lineage |
| R16 | binding override | `persistent_output` fold state, set once | logical_fold_broker | `G4C2`: recorded at the answer, read by every later attempt (`rung_binding`), the frozen half `None`; still present after the merge (`overrides=k2=…` on #21). M06, M35 |
| R14 | repair budget unit | `consumed` once per automatic rejection | logical_fold_broker | `lineage_members(root)` counts registered members; `check_merge_rejected` refuses `Runnable` at or past `max_merge_repairs` and `HumanRequired` below it; `G4C1` with the limit 0 registers the first repair `HumanRequired{limit: 0}`. M16 (caught by the full suite), M17 |
| R24 | snapshots for repair attempts | pruned after the terminal; reclaimed as residue after a kill | external_physical | unchanged from G3's portion; a lineage candidate's verification runs in the same snapshot slots (`G4A` #22, `G4B` #17 — `StaleClean` verifications with pins, judged by the same path) |

**No overlap, and one row each.** The one object two rows could contend for is the merge blob a
materialization writes: R9 while the repair index references it, R27 once the worktree is scrubbed,
and the scrub test measures the hand-over with `fsck` at both instants.

**What Q2 asks that G4 cannot close.** The outcome equations over all 28 rows, the closure procedure
and terminal finalization at Complete/Halted are PR10's by the packet's own `scope`, exactly as G3
recorded; *"repair-source protection on resumable outcomes"* is the one clause G4 owns, and §8 answers
it. **Q2 verdict: answered with evidence for the nine rows G4 owns; the total-run-outcome equation is
PR10's and is not claimed.**

### Q5 — effect and append boundaries

**G4's portion.** The Object site `Object.RepairMaterialize` with its command-internal residue class,
and the Event appends the range adds (the new events go through the unchanged funnel). The Answer sites
(`Answer.StageWrite`, `Answer.PublishRename`, `Answer.Ingest`) predate the range and are exercised
here only as T-ANSWER's ingestion into a lineage; their hook-phase coverage is not re-claimed.

**Both hook phases of `Object.RepairMaterialize` were observed executed by the suite.** The
recovery tests count the `Before` phase at the harness (`count(REPAIR_MATERIALIZE, HookPhase::Before)`
— `0` after recovery step (g), `1` after the continuation, `1` across a same-session retry) and the
synthetic-residue test counts the `After` phase (`count_after(mark, MATERIALIZE, HookPhase::After) == 1`
per element), so both phases are recorded by the harness, not inferred. The site's frozen entry in
`effects/residue-classes.json` names the four elements (`unreferenced_object`, `temporary_object_file`,
`index_lock`, `cherry_pick_head`), row `r9`, the `object_internal` class and `sampling_n: 8`.

**The residue class — both halves the packet demands.** *Synthetic construction:* each of the four
registered elements is planted with real git, classified `Internal`, recovered by the tabled action, and
the recovered tree equals a control's (§5). *Real-command kill sampling:* the sampler's five runs in §9
(40 kills in 48 spawns, every one classified and recovered). **One boundary the tree itself states and
this gate confirms:** the two object-store elements (`UnreferencedObject`, `TemporaryObjectFile`) do
**not** fail `Worktree.Verify` — by design, since a verifier that consulted the object store would refuse
essentially every real repository — and the synthetic test asserts that partition rather than the
packet's blanket "`Worktree.Verify` fails" for every element. The packet's own row for the site also
names `CHERRY_PICK_HEAD`, which `cherry-pick --no-commit` never writes (PR9's measurement, R5); the
element is still constructed synthetically because PR3 froze the row, and the residue the command
really leaves (`MERGE_MSG`, `AUTO_MERGE`, the held `.lock` forms) is read by the verifier all the same
(`administrative_residue_at` in `src/workspace_manager/residue.rs`). This is PR9's recorded `PR9-N1`,
an approval owed on a frozen row, not a G4 finding.

**The whole-tree half — for what the range changed.** Cargo.toml, Cargo.lock (64 packages) and
`clippy.toml` are unchanged across the range; `cargo +1.85.0 check --locked` is green (gate 04), which
is what proves the lock is the one reviewed. `effects/wrappers.toml` gained twelve rows, every one
`effect-free` (the manifest and index readers, `poll`, `is_decline_option`), and the wrapper census
tests that hold that file to the code are in the green suite. The allow-placement scan, the
funnel-module allowlist and the frozen legacy allowlist are untouched. **The whole-inventory bijection
is still PR10's** and the tree still says so: `the_bijection_over_the_whole_claimed_inventory_fails_for_this_slice`
is unchanged in range (`src/topology/effects/tests.rs` is not in the diff).

**Q5 verdict: answered in the G4 portion.**

### Q1 — reconstruction from durable state

**G4's portion is four things**, and only these are claimed.

**(a) Every fold-derived state the range adds is reconstructed from the log alone, and the loop's live
decisions agree with the reconstruction.** Every `G4…` trace line is a replay of an on-disk prefix
through `TopologyFold::replay` — the lease table with its ages, the queue positions of lineage
candidates with their eligibility, the open admission questions with their `binding` options, the
binding override, `halted_at` from a decline, the epoch-scoped budget stop and its clearing at
`run_resumed` — and the loop's choices at each step (dispatching the older repair rather than
integrating the queued younger candidate; hard-blocking on the parked question; dispatching only after
the answer) are the choices the replayed state at that prefix admits. Replay is deterministic
(`replay_twice_equal=true` on every measured log), and the committed dispatch tests assert the live
incremental fold equals a replay of its own log (`run.replay_twice_equal()`).

**(b) A `RetainedIdle` repair generation with its `retained_session` and `retained_incarnation` is
reconstructed and disposed of by the resume.** Recovery step (e) closes it `LineageHeld` before
`run_resumed`, from the replayed class alone; the retaining process's tree is not needed and is not
consulted (ST-11 test; `G4G` for the live missing-worktree case).

**(c) A repair generation's source and base are reconstructed from the log for a continuation.**
`dispatched_source` reads the generation's own `task_dispatched`; `rejected_source` reads the
`merge_rejected` that registered the task; the continuation after a kill re-materializes from exactly
that candidate (the three-prefix test, `dispatched_source_equals_rejected_candidate=true` in `G4C1`
and `G4E`).

**(d) The recovery order is unchanged at its typestate.** The chain `RootDerived → LocksHeld →
RecordsVerified → BarrierHeld → ResumeCensused → RunnerRebuilt → PreflightCertified` is the one G3
certified; the range adds `reclaim_closed_generations` after step (e) and the repair arm of
`open_no_attempt`, both after the barrier. G3's O1 (the inert `StablePrefixBarrier::establish` token at
its only production call site, now filed) is unchanged and not re-adjudicated.

**What Q1 asks that G4 does not answer.** Coordinator-local holdings at process start, the reaper
contract, husk reclamation, the private root and the Runner rebuild are G1's and G2's; the unresolved
transaction and the stable-prefix barrier are G3's. **Q1 verdict: answered with evidence for the four
G4 portions; the remainder is explicitly out of this gate's range.**

---
## 11. Observations recorded, findings, and how the deferrals bear on the pass

Nothing in this section is a new critical or high finding. Each item says whether it was found by
reading, by measurement or by mutation.

### O1 — no test observes the candidates ref after a rejection produced by integration, and G3's R11 row said one did (mutation, M22 and M23; reading)

Both rejection arms of `integrate.rs` can be made to delete the rejected candidate's ref without any
test in the suite noticing, and the reason is that every rejection a G4 test dispatches a repair from
is planted by the test, while the tests that reject through `integrate` never read the ref. **The
protection holds at this sha** — structurally (no production caller of `Ref.DeleteCandidatesRef`,
§8c) and observed through the production path in `G4B5` (code rejection) and `G4B6` (conflict
rejection), where the repair then dispatches from the ref and the dispatch's `source_candidate` equals
the rejection's; under the mutations the same measurements show the dispatch refused, which is the
defence in depth doing its job with nobody watching.

The G3 report's Q2 table, R11 row, says *"The shape table's rejection arms assert the ref remains."*
That sentence is not true of the table at `9cfaf0c8` or at `74da2cbb`: the table reads no candidates
ref, and M22/M23 survive it and the full suite. The row's first sentence — *"`merge_rejected` deletes
no candidates ref: `Ref.DeleteCandidatesRef` is reached only at Complete finalization"* — is the true
one, and it is why the protection held then as now. Per the project's rule for merged records, G3's
report is not edited; a dated addendum is appended to it in this pull request, and this gate's own §8
rests on the structural fact and the measurements, not on the shape table. **Filed:**
`G4-O1-REJECTION-KEEPS-CANDIDATES-REF-UNWITNESSED`, P3, `correctness`, provenance pre-existing (the
shape table is G3's range; the dispatch that consumes the ref is PR9's). The repair is one assertion in
the shape table's two rejection arms — read the ref, expect the candidate — or `G4B5`/`G4B6`'s shape
adopted as a test, before PR10 writes the one site that can delete the ref.

### O2 — the overridden repair's single-rung ladder has no loop-level witness (mutation, M34; measurement)

With an override recorded, `ladder_policy` gives the repair one rung, so its exhaustion escalates to
the human rung instead of failing on the first attempt. `G4C3b` measures the rule at the ladder;
nothing drives the loop through it, and the driven fixture cannot: its `FixedIds` question source
reuses one question id, so the settlement that would park the exhausted repair is refused by the fold
as a reused question (`G4C3` step 3). That is a fixture limitation worth knowing about in its own right
— any loop-level test that needs a *second* question in one run must supply a fresh id source.
**Filed:** `G4-O2-OVERRIDE-SINGLE-RUNG-EXHAUSTION-UNWITNESSED`, P3, `correctness`, provenance
introduced-by-feature (PR9's `ladder_policy` arm).

### O3 — the frozen sha's post-merge macOS leg is red, on the known sampler (reading of CI)

The merge-group run that landed #249 (`34355870940`) is green on every leg. The `push` run on
`master` at the same sha (`34356671343`) is red on exactly one job, `test (macos-latest)`, on exactly
one test: `sampled_cherry_pick_child_kills_every_residue_classified_and_recovered`, panicking at the
assertion `killed_while_running >= 1` — the vacuity refusal that `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE`
(P2, filed on pull request #254, not yet in this tree) describes: one cold probe pick, all eight kills
scheduled from it, eight clean exits. 2423 of 2424 macOS tests passed. This is the known finding and
not a new one, and it is **not a G4 test**: it is G3's T-PROPOSAL sampler. G4's own sampler for
`Object.RepairMaterialize`, which has the two-probe budget the finding asks for, passed on every leg
of both runs and five times here (§9). The brief anticipated this exact event; it is recorded so that
nobody reads the red `upstroke-ci` check on `master` as a G4 failure or as a regression in the range.

### O4 — the lineage region really does absorb the rejected candidate's own paths (measurement, `G4B7`; mutation M27)

Recorded because a reader of `apply_merge_rejected` could take the first `widen_lineage` (by the
candidate's actual paths) for redundant with the second (by the contended paths). It is not: `G4B7`
rejects a two-file candidate by a conflict on **one** of its files, and the lineage's region is
`candidate.txt+extra.txt`, so an ordinary candidate on the other file waits `InsideLineage`; under M27
the region is `candidate.txt` alone and that candidate is `ELIGIBLE` — the overtaking INV-16 forbids,
reachable only through a conflict narrower than the candidate. The full suite catches M27
(`a_conflict_opens_and_closes_its_own_transaction`), so this is a positive observation with a witness,
not a finding.

### O5 — recovery step (g) reads a field it does not use (reading)

`open_no_attempt` in `src/engine/topology/recover.rs` resolves `dispatched_source` for every open
repair generation before step (g), and step (g) (`recreate_open_no_attempt` → `verify_or_recreate`)
never reads `OpenGeneration.source`; the continuation reads the source again itself
(`continue_open` in `src/engine/topology/run.rs`). The read is harmless — it can only refuse a resume
whose log the fold has already refused (a repair dispatch with no source, the M28 parity, which
`a_dispatch_takes_the_holding_its_origin_implies` guards) — and it is recorded as an observation, not
filed: one redundant log read in the recovery order.

### O6 — the driven fixture's fixed question id (reading, from `G4C3`)

`FixedIds` answers every `question_id()` with the same identity, so no driven test can observe a run
that raises a second question. It cost this gate the loop-level half of O2 and will cost the next
reviewer the same. Not filed separately; it is the second paragraph of `G4-O2`'s repair.

### The known deferrals inside this gate's range — reported, not reopened

| id | severity | what it is | does it bear on the pass rule? |
|---|---|---|---|
| `PR8-R2-SPEND-REPLAY` | P1 | a parked or deferred integration verification's review cost is charged live and forgotten on replay; unbounded overspend across restarts | **inherited from G3, unchanged in range.** A lineage candidate's verification goes through the same path, so the range adds occasions for the gap and no new mechanism; `Spend::replay` in `src/engine/topology/select.rs` is unchanged. Owner-deferred; does not fail this gate on its own, and the verdict rests on that deferral standing |
| `PR8-CRASH-002` | P1 | a coordinator killed inside a ref write leaves `integration.lock`; every resume refuses until an operator removes it | **inherited from G3, unchanged in range.** A lineage candidate is published through the same compare-and-swap; the range adds publications and no new ref write. Owner-deferred; the verdict rests on it |
| `PR249-ANSWER-TEXT-NOT-CARRIED` | P2 | a typed free-text answer reaches no agent in schema 4; the option index is all the wire carries | bears on T-ANSWER's *content*, not its recovery: every T-ANSWER row in §5 concerns which event is appended and when, and `G4C1`/`G4C2` show the index-only ingestion doing exactly what the vocabulary allows. Beyond reach (Class C); does not engage the pass rule |
| `PR249-KILL-SAMPLER-WINDOWS-WRAPPER` | P2 | PR5's four-command census sampler kills Git for Windows' launcher, not git | bears on Q5's kill-sampling record **for PR5's sites on Windows**, not for `Object.RepairMaterialize`, whose sampler spawns the real binary and whose mid-write floor binds on Windows (PR9's guest runs). Does not engage the pass rule |
| `PR249-MANIFEST-NORMALIZATION-ALIAS` | P3 | a normalization-form respelling of a conflicted path is not read as an alias | a capture-time boundary on macOS, inside a repair attempt; no lease, lease release, ordering or source-protection clause depends on it. Does not engage the pass rule |
| `PR249-REFUSED-MANIFEST-HANDOFF` | P3 | the worker-facing strings promise an in-place manifest correction the driver does not perform; the refused attempt closes its generation and the next is fresh | the fresh-generation route is the one this gate observed (`G4B6`: the unresolved conflict refused at capture, `AgentError`, `Closed{Retry, LineageHeld}`), and it keeps every G4 invariant — the generation closes `LineageHeld`, the lineage lease stays, the next dispatch re-materializes from the protected source. Does not engage the pass rule; the design and the strings disagreeing is the owner's call as the finding says |
| `PR80-MACOS-WORKSPACE-SAMPLER-COLD-PROBE-RECURRENCE` | P2 | the workspace-manager sampler's cold-probe schedule can sample nothing on macOS | the family O3 belongs to; not a G4 site. Does not engage the pass rule |
| `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE` | P2 (on #254) | the T-PROPOSAL sampler's cold probe; fired on this sha's post-merge macOS leg (O3) | G3's evidence, not G4's; **does not engage "every G4 test green"** because it is not a G4 test, and G4's sampled evidence for row 10 stands on the RepairMaterialize sampler. It does mean this gate's own pull request may see the same red on macOS; if so, that is this finding |

Every one was verified present in `reviews/findings/` at `74da2cbb` (the last is verified present on
#254's head `ece3544c`), read in full, and neither reopened, repaired nor narrowed.

### The state of the finding ledger

`reviews/findings/` holds 293 files at `74da2cbb`, one of which is `README.md` — **292 findings**: 14
at P1, 102 at P2, 175 at P3, 1 at P4. Dispositions, counted from the frontmatter: **289 `deferred`, 4
`accepted-risk`, 1 `fixed`**; `grep -l '^disposition: *open'` returns nothing. All 14 P1s are `deferred`
with a named guard; two (above) are G3's range, none is G4's. This gate adds two P3 findings (O1, O2),
each `deferred` with the repair named. No finding of this gate is critical or high.

---

## 12. Verdict against the pass rule

The rule, verbatim:

> pass iff every G4 test green, no lease double release, ordering proven, source protection proven,
> questions answered, no open critical/high finding

| Clause | Verdict | Where |
|---|---|---|
| every G4 test green | **met, with one leg-shaped qualification.** The 48 committed tests the twelve rows name pass on this box; the cumulative suite is green on the exact range (§1); the merge-group run that landed the range is green on all three platforms. The post-merge `push` run on the same sha is red on macOS on G3's T-PROPOSAL sampler, the known cold-probe finding (O3), which is not a G4 test; G4's own sampler is green everywhere | §1, §9, O3 |
| no lease double release | **met.** A lineage lease is released once, at the publication whose `satisfies` names its root or at the lineage's terminal failure, and a second `task_merged` is refused with the ledger unchanged (`a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused`); every trace in §4 shows one release per lineage and none by an attempt-level settlement or close; M03 (never released) and M25 (any release accepted) are caught | §4, §9 rows 1 and 5, §3 |
| ordering proven | **met.** Two overlapping lineages resolve in creation order in a real repository with the younger candidate queued first (`G4A`); an ordinary overlapping candidate waits until the lineage merges (`G4B`) or fails (`G4B2`); overtaking is refused live and on replay with byte-equal errors for both a verification start and a fast publication; M01 and M02 are caught by exactly the tests that own the rule | §4, §9 rows 1, 2, 11 |
| source protection proven | **met for this tree, with the witness gap stated.** The rejected candidate's ref is present, unchanged and in recovery's expected set across a parked resume, a budget-stopped resume, and after the repair's merge; a repair dispatches from it after a real conflict rejection and a real code rejection; a missing or substituted source is refused before any append (M21 caught); and no production caller of `Ref.DeleteCandidatesRef` exists at this sha. What is *not* proven is that a future rejection arm deleting the ref would be caught by a test (M22, M23 survived the full suite; O1) | §8, §3.1 |
| questions answered | **yes, each with its scope named.** Q6, Q4, Q3 and Q5 answered with evidence for G4's portion; Q2 for the nine rows G4 owns, with the total-run-outcome equation named as PR10's; Q1 for its four G4 portions, the rest named as G1–G3's | §10 |
| no open critical/high finding | **met** on the ledger's own terms: 14 P1s, all `deferred` with named guards, none `open`; the two findings this gate adds are P3, each a guard without a witness rather than a behaviour defect | §11 |

### Verdict: **G4 PASSES**, with the scope of the pass stated.

Every clause of the pass rule is met on the evidence above, with three qualifications that are part of
the verdict and not footnotes to it:

1. **Three mutations survive the full suite, and each is a missing witness rather than a broken
   guarantee.** For each the guarantee was measured to hold at `74da2cbb` (§3.1), so none of them
   fails a clause; but two of them (M22, M23) sit exactly under the "source protection proven"
   clause, which this gate therefore meets by the structural fact that this tree cannot delete the ref
   and by end-to-end measurement, not by a test that would catch a regression. A successor should
   treat O1 as the first thing to close, because the protection's only mechanical guard today is the
   absence of a caller that PR10 will add.
2. **The pass rests on the owner's two inherited deferrals.** `PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002`
   are P1s in G3's range that the range under review does not touch and does make more reachable (more
   verifications, more publications). The gate brief directs that neither fails a gate on its own, and
   that direction is the owner's ruling, not this reviewer's judgement. If the owner withdraws either
   deferral, this verdict does not survive it, exactly as G3's did not.
3. **The sampler family is a standing threat to the leg, not to the range.** The known cold-probe
   sampler reddened the frozen sha's own post-merge macOS leg (O3). Nothing in the range caused it and
   G4's sampled evidence does not depend on that sampler, but the verdict is stated knowing that the
   pull request carrying this report may show the same red, and that the correct reading of such a red
   is `RECOVER-CHERRY-PICK-SAMPLER-COLD-PROBE`, not a G4 failure.

**Consequence: PR10 and later are unblocked by G4**, subject to (2).

### What a successor should do first if this report is reopened

- **O1 first.** One assertion in each rejection arm of the G3 shape table (read the candidates ref,
  expect the candidate) turns the R11 protection from a structural accident of this build into a
  guarded invariant before PR10 adds the only site that can delete the ref.
- **O2 is one loop test** once the driven fixture can hand out a second question id.
- **Build the detection set from the full suite, not from a subset.** Four of this gate's seven
  first-round survivors were caught by fold tests this reviewer had not thought to include; the
  full-suite re-run cost eight minutes and changed the findings list from five to two. G3's protocol —
  the full suite per mutation — is the safer one when the catalogue is small.
- **The measurement module is worth keeping in some form.** Fifteen temporary tests produced every
  trace in §4, §6, §7 and §8 from a replay of the on-disk log; a committed helper that prints the
  lease table and the queue's own eligibility answer per prefix would make the next gate's trace
  artifact a one-line call.
