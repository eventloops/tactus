# PR9 — repair execution, retries, lineages: the working record

The record of the ninth slice of upstroke v0.2, kept on the branch so that a successor session
inherits what was decided and why. Written before the work and updated as each piece lands, so
that at any commit on this branch the record is true. It is **not** a design document:
`DESIGN.md` and the packet stay the authority, and a sentence here that disagrees with either is a
defect in this file.

**Branch:** `feature/pr9-repair-execution`, cut from master at `9cfaf0c8` — the exact tree G3
certified on 2026-09-08 (`reviews/2026-09-08-gate-G3.md` on branch `gate/g3`).

**Where this file lives, and why.** The owner ruled on 2026-09-08 (PR #248) that PR8's three
root-level working records do not belong on `master`, and the brief for this slice forbids any new
file at the repository root. `docs/internals/` cannot hold it either: `validate-internals-notes.sh`
(N2) requires every file there to mirror a Rust module. So the record sits beside the gate report's
convention in `reviews/`, dated. Where it belongs long-term is the owner's call, as the G3 report
says of itself.

## 0. Status

| Step | State |
|---|---|
| A read the packet, errata, G3 report, DESIGN §4/§21/§26, MAINTAINING, standards | **done** |
| B verify and extend the abandoned attempt's refusal inventory against the code | **done** — §4 |
| C record the readings taken on every packet ambiguity | **done** — §3 |
| D fold layer: errata E2 override binding, lineage terminal failure, the override-aware reader | **done** — `c7223367` |
| E workspace manager: materialization observed, unresolved-conflict read, pick state cleared, held lock forms read | **done** — `d23d6bdb`, `9eff29d5`, `0cebabec` (§11) |
| F engine: repair dispatch, continuation, retry, answer ingestion, recovery step (g) | **done** — `d23d6bdb` |
| G tests: the packet's gating and proof tests, the G4 adversarial list | **done** — §10 names each test; two defects found by the tests and fixed (§9) |
| H notes under `docs/internals/` brought back into agreement with the code | **done** — `848a36b3` |
| I ten gates green on this box; Windows guest for anything platform-shaped | in progress — Linux full suite green at `0cebabec` (2399 passed); the ten-gate run and CI's Windows/macOS legs follow |
| J draft pull request with a validated body | not started |

## 1. What PR9 is, from the packet

`decisions.pr_sequence[10]`, `.id == "PR9"`, title *repair execution, retries, lineages*,
`production_effect: none`, `cumulative_gate_after: G4`.

Scope, verbatim: repair dispatch (`InheritedLineage`) from protected source candidates,
materialization plan+observed (`Object.RepairMaterialize`; objects behind the repair index, R9;
interrupted materialization recreated after `Worktree.Verify` fails), `LineageHeld` dispositions
incl. same-generation retry without re-materialization, one-off binding activation, over-limit
activation, lineage terminal failure, lineage candidates through the queue with `satisfies`,
two-lineage order at runtime, `generation_closed` for repair generations; T-REPAIR-DISPATCH,
T-ANSWER, T-RETAINED, T-RETRY (repair).

Non-goals: the empty no-worker path; the production writer. Invariants introduced: INV-06 (repair
generations incl. retry), INV-11 (execution), INV-16 (complete lifecycle incl. terminal failure),
INV-19 (overrides). Preserved: every PR7/PR8 invariant and R11's protection of source candidates.

## 2. What the tree already had at `9cfaf0c8`, measured by reading

Most of the vocabulary and most of the primitives landed in PR3–PR8; what was missing was the
production path through them, plus the three refusals PR8 stood where that path goes.

Present: `LeaseGrant::InheritedLineage`, `TaskDispatched.source_candidate`, `Materialization`,
`AttemptStarted4.materialization_observed`, `LeaseDisposition::LineageHeld`, `BindingOverride`,
`SpawnAdmission::{HumanRequired, HumanBinding}`; `engine::topology::dispatch`'s
`DispatchKind::Repair`, `refuse_absent_source`, `materialize_repair`, `verify_or_recreate`,
`resume_open_no_attempt`; `workspace_manager::repair_materialize` (the funnelled
`Object.RepairMaterialize` site with its `ObjectInternal` residue class); the fold's
`check_dispatched` repair arms, `check_attempt_started`'s materialization-parity arm,
`check_question_answered` with override validation, `apply_answer` (override state, `fail_lineage`
on a decline), `satisfies_closure`, the queue's `BehindOlderLineage` ordering,
`repair::merge_rejected` with `repair_ladder` and `admission_for`; `settle::retry` already carries
a `materialization` field; `close_generation` already takes its disposition from the generation's
lease kind.

## 3. Readings taken on packet ambiguities

Each is a place where the packet, the errata and the shipped vocabulary together do not determine
the answer. The reading, the alternative, and why. Where the abandoned attempt
(`~/tactus-artifacts/pr9-opus-wip/pr9-plan-opus.md`) reached the same reading, that is noted, but
each was re-derived here from the packet and the code rather than adopted.

### R1 — tier and pin of a one-off binding (errata E2)

E2 amends `decisions.task_registry.binding_override`: the validation binds *"the complete resulting
rung binding: agent, model and effort from the override payload, and tier and pinned from the
frozen option selected by option_index"*. The shipped frozen option is a bare agent name
(`SpawnAdmission::HumanBinding { options: Vec<String> }`, filled from `allowed_agents`), so tier
and pin cannot come from the option literally.

**Reading.** `tier` is the repair ladder's frozen `floor` and `pinned` is `true`. Both are
derivable from frozen registry state alone, which replay stability requires: the fold re-derives
them on every replay of every log. `repair_ladder` sets `floor = max(root floor, mid)` on exactly
the empty-intersection path that raises a `HumanBinding`, so the floor is the tier the options were
offered *at*; "the frozen option" is read as "the frozen option set", whose tier is that floor.
`pinned: true` because a human naming one agent for one task is the definition of a pin, and it is
the safer value: a build that forgets to mark it is refused rather than admitted.

**Alternatives rejected.** Leaving tier and pin unbound (the shipped half-rule, which E2 exists to
close). Deriving the tier from the model catalogue, which is a compile-time constant and would make
the replay of an old log depend on the binary. Widening the frozen option to a rung — a Class C
change to `SpawnAdmission` (§6).

The abandoned attempt reached the same reading.

### R2 — where a one-off binding's `model` and `effort` come from

`BindingOverride` carries `{key, question, option_index, agent, model, effort}`; the answer source
yields an option *text*, an agent name. Nothing in the packet says who chooses the model.

**Reading.** The model is the named agent's catalogued model at the lowest tier at or above the
ladder's floor (`crate::catalog::CATALOG`, first entry at that tier in catalogue order), and the
effort is the entry's frozen effort policy at the floor. The derivation runs **once, live, at
ingest**, and what replay reads is the recorded payload; the fold never re-derives the model, so a
compile-time catalogue is safe here and would not be inside the fold. An agent with no catalogued
model at or above the floor refuses the activation before any append, naming the agent and the
floor.

**Alternatives rejected.** `run_started.chains` (frozen run identity, but the empty intersection
means the root's chain has nothing at or above the floor, and another task's chain is not the
right authority for this task). A new seam on `AttemptPlans` for the assembler to choose — a
reasonable future shape when PR12 wires production config, recorded here as a follow-up, not
built: the production `FrozenPlans` has no config today either.

### R3 — the ceiling applies to a repair dispatch

`select` returned `Step::RepairDispatch` *before* the ceiling check, because the checkpoint refused
the step anyway. `decisions.sequential_substrate.loop` states the branch as *"else if a ready task
exists: ceiling check, provisional dispatch reservation, dispatch"*, and a repair is a ready task.
**Reading:** a repair dispatch checks the ceiling exactly as an ordinary dispatch does, and a breach
appends `budget_exceeded` naming the repair's key. Nothing exempts repairs, and `repairs.limits`
bounds them by count, not cost.

### R4 — which settlement folds a lineage (`decisions.repairs.terminal_failure`)

*"a lineage member Failed folds every AwaitingRepair member of that lineage to
Failed{lineage_failed} and releases the lineage lease"*. **Reading:** the fold folds the lineage
when a task **that has a `lineage` in the registry** settles `attempt_finished{Closed{Failed}}` —
a repair, never an ordinary task. A lineage root is an ordinary task and is `AwaitingRepair` with no
open generation for as long as its lineage lives, so it cannot be the settling task; testing
membership rather than `lineage_root(key)` keeps an ordinary task's terminal failure exactly as PR7
left it. `Failed{lineage_failed}` is a derived-state annotation, not a wire field: `TaskState::Failed`
carries no payload, and adding one would be Class C.

### R5 — classifying the materialization (measured, not assumed)

`attempt_started.materialization_observed` is `Clean | Conflict | Empty | Retained`, and nothing
says how a `git cherry-pick --no-commit` maps onto the first three. Measured on this box (git 2.43.0,
`x86_64-unknown-linux-gnu`) on purpose-built repositories, 2026-09-08:

| case | exit | `ls-files --unmerged` | `diff --cached --quiet` | git-dir residue left |
|---|---|---|---|---|
| the change applies | 0 | empty | 1 (a staged change) | `AUTO_MERGE`, `MERGE_MSG`, `ORIG_HEAD` |
| the change is already present | 0 | empty | 0 (nothing staged) | `AUTO_MERGE`, `COMMIT_EDITMSG`, `MERGE_MSG`, `ORIG_HEAD` |
| the change conflicts | 1 | three stage entries | — | `AUTO_MERGE`, `COMMIT_EDITMSG`, `MERGE_MSG`, `ORIG_HEAD` |
| the commit is not an object | 128 | — | — | — |

Two facts from the table bind the code. First, **`CHERRY_PICK_HEAD` is not left behind by
`--no-commit` in any of the three cases** — `git rev-parse --verify CHERRY_PICK_HEAD` fails in all
three worktrees — which contradicts the pre-existing doc comment on `repair_materialize` ("`--no-commit`
deliberately leaves `CHERRY_PICK_HEAD` behind"). `MERGE_MSG` is, and it is one of the names
`administrative_residue_at` reads, so a *completed* materialization fails `Worktree.Verify` at its
base exactly as an interrupted one does. That is what makes R6 below consistent. Second, exit 0 with
an unchanged index is the already-present case, not an error.

**Reading.** Exit 0 with a staged change is `Clean`; exit 0 with none is `Empty`; a non-zero exit
**with unmerged index entries** is `Conflict` and is not an error — a conflicted cherry-pick is the
normal shape of repairing a `Conflict` rejection, and leaving the conflict in the worktree is what
gives the agent something to resolve. Any other non-zero exit stays the Git error it is today.
`Retained` is recorded by a same-generation retry, which re-materializes nothing.

### R6 — when a repair generation's worktree is re-materialized

`T-REPAIR-DISPATCH.resume_action`: Worktree.Verify at the recorded base; on failure remove with
force and recreate; re-run the cherry-pick deterministically; the observed kind is recorded when
`attempt_started` is appended. `recovery_order` (g): "recreate OpenNoAttempt worktrees at their
bases (through Worktree.Verify or forced recreate)". Neither says whether step (g) or the loop's
continuation performs the cherry-pick.

**Reading.** Recovery step (g) recreates the worktree **at its base only** — verify, or forced
recreate — for ordinary and repair generations alike. The materialization is reproduced by the
loop's continuation of the generation, immediately before the `attempt_started` that records what
it observed. Two reasons. The observation recorded must be the one made by the process that
appends it; an interrupted process's observation is never carried. And by R5 a completed
materialization fails `Worktree.Verify` (`MERGE_MSG`), so a step (g) that materialized would hand
the loop a worktree its own verify then recreates and materializes again — two cherry-picks per
resume for one observation. With this reading every resume of a repair generation performs exactly
one cherry-pick, on a worktree that verified clean at its base or was just recreated there, and the
three prefixes a kill can leave (before the pick, inside it, after it) converge on the same tree.

**Alternative rejected.** Materializing in step (g) as well (the abandoned attempt's shape).
Correct and deterministic, but it performs the effect twice and reports a healthy materialized
worktree as `Recreated { Residue(MergeMsg) }`.

### R7 — which candidate a repair is materialized from, and where that is read

`repairs.dispatch`: "task_dispatched(repair) records base and source candidate; ... git cherry-pick
--no-commit <candidate> from the protected candidates ref". `merge_rejected`: "the rejected
candidate's candidates ref remains as the protected repair source". DESIGN §26.4 says of a
semantic rejection that the engine "materializes the clean proposal against that current head".

**Reading.** The source is always the **rejected candidate** — the `candidate` the `merge_rejected`
that registered the repair names — because that is the object R11 protects: the proposal's pin is
pruned at the rejection terminal and the staging worktree reclaimed, so the proposal is Git's, not a
protected source. Cherry-picking the candidate onto the current head reproduces the proposal for a
code rejection and the conflict for a textual one, which is what §26.4 describes in both arms.

For a **fresh** dispatch the source is read from the `merge_rejected` event whose `repair.key` is
the task (the log is the durable pairing). For a **continuation** of an open generation — live, or
after a resume — the source is the `task_dispatched.source_candidate` of that generation, read from
the log. Both reads are Class A: `integrate::authorized_head` and `IntegrationRequest::from_log`
already read the log for exactly this kind of fact, and it avoids retaining a second copy in the
frozen fold (the abandoned attempt's Class B row 1, not taken here).

### R8 — the materialization kind on a same-generation retry

ST-15: "a repair generation retry records `LineageHeld` and does not re-materialize". **Reading:**
the retry records `Materialization::Retained` and runs no `Object.RepairMaterialize`; the fold keeps
checking only presence parity (a lineage member records a kind, an ordinary task records none) and
does not gain a refusal on the kind itself. The kind is the engine's observation, like `pool`, and
the packet's refusal list for this slice names no refusal for it.

### R9 — what "unresolved index entries fail capture before gates" can mean

`repairs.dispatch` and DESIGN §26.4: "the worker may edit but never commit; the engine refuses a
result with unresolved index entries and then runs the ordinary gates and reviews." Under
*agents edit files; the engine owns git*, an agent cannot resolve an index entry: only the engine's
own staging (`git add -A` at capture) does, and staging resolves every unmerged path
unconditionally — an untouched conflicted file is staged **with its markers inside**. Read
literally after staging the rule is vacuous; read literally before staging it fails every conflict
repair, because no agent may run `git add`.

**Reading.** Before the capture stages anything, the engine lists the index's unmerged paths and
reads each working-tree file: a path whose file still carries a conflict marker (a line beginning
`<<<<<<< ` or `>>>>>>> `) is unresolved; a path whose file is gone was resolved by deletion; a path
that does not decode is unresolved because it cannot be inspected. If any path is unresolved the
capture stages nothing, the attempt fails as the worker's failure (`AgentError`, worker origin,
naming the paths) before any gate or review runs, and the settlement is never `Retained`: an
unmerged index has no tree for a retained retry to verify against, so the next attempt is a fresh
generation that re-materializes the conflict. Measured (git 2.43): a file rewritten without markers
is resolved by the engine's own `git add -A`, and `write-tree` then succeeds.

**Limits stated.** `=======` alone is not read as a marker (a setext heading is one). A binary
conflict carries no markers and reads as resolved when untouched. A resolved file that legitimately
begins a line with `<<<<<<< ` reads as unresolved and costs one attempt.

### R10 — the answer-ingestion branch, and why it needs a non-blocking read

`loop`: "after recovery: ingest answers (never after budget_exceeded or a halting settlement in
this epoch) -> if an eligible integration exists ...". PR8 assigned this branch to PR9 by name
(`LoopBranch::IngestAnswers => Disposition::NotThisSlice { slice: "PR9", .. }`) and ingested only
verification-park answers, at the hard block. DESIGN §4 (6): "Questions never stop the runnable
frontier ... the run hard-blocks only when nothing remains runnable."

**Reading.** Every `step()` begins by polling the answer source for each open question, in id
order, while the run is not ending; the first answer or decline is ingested as `question_answered`
and the step returns `Progress::Answered`. The hard block keeps its blocking read. The poll must be
**non-blocking**: `TerminalAnswers::resolve` prompts and reads stdin, and `EventLogAnswers::resolve`
sleeps up to its budget — both are the hard block's "attached-terminal prompt or wait_on_block",
and neither may run while other work is runnable. So `AnswerSource` gains `poll`, defaulting to
`Unanswered`; `EventLogAnswers` answers it from the answer file without waiting. That is the whole
change to `interaction.rs`.

### R11 — this record's location

See the preamble. `reviews/`, dated, beside the gate report's convention; not the root, not
`docs/internals/`.

### R12 — the worker's prompt does not say what the materialization observed

`PlanRequest` carries `materialization_observed` and the assembler could tell the worker that its
worktree holds conflict markers. Not done here: the frozen repair spec body already names the
conflicting paths and the rejecting head, the packet does not ask for it, and the prompt builder is
shared with the released path. Recorded as a possible follow-up.

## 4. The PR8 checkpoint refusals this slice replaces, and the placeholders beside them

`decisions.sequential_substrate.checkpoint_refusals` let PR8 refuse *"repair execution and
repair-admission answers"* before any append. The abandoned attempt's inventory listed three
refusal sites; each was verified against `9cfaf0c8` by grep and by reading, and the inventory was
extended with the eleven places that were written for ordinary tasks only and would have let a
repair through wrongly once the refusals fell. Every one is replaced by an implementation; none is
deleted and forgotten.

**The three refusals (verified; the abandoned inventory had all three):**

| # | Site at `9cfaf0c8` | What it refused | What stands there now |
|---|---|---|---|
| 1 | `src/engine/topology/select.rs`, `checkpoint`, `Step::RepairDispatch` arm | dispatch of a Repair-origin task, before any append | `Admitted::RepairDispatch { key, generation, continuing }`; the loop dispatches or continues the repair and runs its first attempt |
| 2 | `src/engine/topology/run.rs`, `hard_block`, `origin != VerificationPark` arm | ingesting a repair-admission or attempt-park answer | every origin is ingested: an activation, a one-off binding (with the validated `binding_override`) or a decline |
| 3 | `src/engine/topology/recover.rs`, `open_no_attempt`, `InheritedLineage` arm | recovering a repair generation, because the source was not retained | the source is read from the generation's `task_dispatched` in the proven prefix; step (g) verifies or recreates at base and the loop re-materializes (R6, R7) |

**The eleven placeholders (extension of the inventory; each written for ordinary tasks only):**

| # | Site | Ordinary-only shape | Now |
|---|---|---|---|
| 4 | `select.rs`, `select` | `RepairDispatch` selected without the ceiling check | ceiling checked (R3) |
| 5 | `run.rs`, `LoopBranch::IngestAnswers` | `NotThisSlice { slice: "PR9" }` | `Performed` (R10) |
| 6 | `run.rs`, `continue_open` | `source: None`, `DispatchKind::Ordinary` | kind from the registry, source from the log |
| 7 | `run.rs`, `dispatch_request` | `DispatchKind::Ordinary` unconditionally | `Repair { root, source }` for a lineage entry |
| 8 | `run.rs`, `retry_ready` | `materialization: None` | `Some(Retained)` for a lineage member (R8) |
| 9 | `run.rs`, `attempt` | `materialization_observed: None` | what the dispatch or continuation observed |
| 10 | `run.rs`, `promote_candidate` | `CandidateLeaseEffect::ReplacesPredicted` unconditionally | `WidensLineage { root }` for a lineage member (the fold refuses the other) |
| 11 | `run.rs`, `attempt` / `retry_ready` / `implementer_binding` | `frozen_rung_binding` only | the override-aware `rung_binding` reader |
| 12 | `run.rs`, `ladder_policy` | `rungs` from the frozen ladder (0 for an empty-ladder repair, which `next_step` reads as `Fail`) | 1 when an override is recorded: "a single-rung ladder with the kind's attempts_per; exhaustion escalates to the human rung" |
| 13 | `src/topology/events.rs`, `RungBinding::matches_override` | agent, model and effort only | all five fields (E2; Class B row B1) |
| 14 | `src/topology/fold/apply.rs`, `apply_settlement`, `Failed` arm | closes the generation and fails the task | also folds the lineage for a member (Class B row B2, R4) |

## 5. Class B / Class C ledger

`src/topology/**` is frozen. Class A (no vocabulary change) is free; Class B (fold behaviour, no
wire change) needs a row here whose description matches the code; Class C (wire vocabulary) is an
approval owed and is not made in this slice.

**No Class C change is made.** §6 records the one place a Class C change would have been simpler.

| # | Change | Where | Description (verified against the code at each commit that touches it) |
|---|---|---|---|
| B1 | `attempt_started`'s override check binds all five fields of the rung binding | `src/topology/events.rs` (`RungBinding::from_override`, `matches_override`), `src/topology/fold/check_attempt.rs` | Errata E2. The check compared agent, model and effort and said nothing about tier or pin. It now builds the whole expected binding — agent, model and effort from the recorded override, tier from the repair ladder's frozen floor, `pinned: true` (R1) — and compares it whole; an override recorded on a ladder with no floor is a mismatch, because there is then no tier the binding runs at. Strictly narrows what is accepted; no wire form changes. |
| B2 | A repair task's terminal `Failed` settlement folds its lineage | `src/topology/fold/apply.rs` (`apply_settlement`) | `decisions.repairs.terminal_failure`. The fold folded a lineage on a recorded decline and not on a member's own terminal failure, so such a settlement left the lineage lease held and the root `AwaitingRepair` with nothing admissible — the `derived_outcome` arm the census asserts unreachable. It now runs the same `fail_lineage` the decline path runs, for a settling task whose registry entry has a `lineage` (R4), and records the halt afterwards as before. Ordinary tasks are untouched. |

Class A changes in the frozen layer, listed so nobody hunts for an undeclared one: the read-only
reader `TopologyFold::rung_binding` in `src/topology/fold/predicates.rs` (the override-aware
composition of `binding_override` and `frozen_rung_binding`, the same rule `check_attempt_started`
applies); the constructor `RungBinding::from_override`.

## 6. Where a Class C change would have been simpler, and was not made

R1: the errata's prose says tier and pin come "from the frozen option", and the shipped option is
an agent name. Widening `SpawnAdmission::HumanBinding`'s options to frozen rungs would make the
sentence literal. It is a wire change, in every log forever, and the ladder's frozen floor answers
the same question from state the fold already holds — so the change is not made and no approval
is owed for it.

## 7. Deferred findings this slice must not disturb

`PR8-R2-SPEND-REPLAY` and `PR8-CRASH-002` are deferred by owner ruling with standing findings, and
G3's pass rests on the first staying deferred. Neither is reopened, repaired or narrowed. Whether
this slice makes either newly reachable is checked in §9 as the work lands, not assumed.

## 8. Measurements

- git 2.43.0 `cherry-pick --no-commit`: the table in R5, including the absence of
  `CHERRY_PICK_HEAD` in every case and the presence of `MERGE_MSG` and `AUTO_MERGE`.
- Resolution by edit: after a conflicting `--no-commit` pick, rewriting the file without markers
  and running `git add -A` leaves zero unmerged entries and `write-tree` succeeds (R9).
- Kill points of `cherry-pick --no-commit` (git 2.43 source order, confirmed by the N=8 kill
  sampler `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered`):
  `index.lock` is taken at the start (`write_index_as_tree`) and again for the whole merge and
  working-tree write (`do_recursive_merge`), released by `write_locked_index`; `AUTO_MERGE` is
  written plainly while that lock is held; `MERGE_MSG` is written afterwards through its own
  `MERGE_MSG.lock`. So a kill leaves one of: nothing; `index.lock`; a merged index with no state
  file; a merged index with `MERGE_MSG.lock`; a merged index with `MERGE_MSG`. Sample 5 of the
  first sampler run was the fourth state.
- Re-running the pick onto an index that already holds it is a no-op merge that reports the same
  observation (`Clean`); onto an unmerged index it fails with "your index file is unmerged" and the
  unmerged entries are unchanged, which the funnel reports as `Conflict`
  (`a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`).
- The catalogue's lowest `claude-code` model at or above `Mid` is `claude-sonnet-4-5` (first of
  two `Mid` entries, `min_by_key` returning the first minimum); the policy's `Mid` effort in the
  recover fixture is `High`. Both are what the one-off binding test pins.

## 9. Findings raised

**Fixed in this slice (both found by this slice's own tests, both in the path it adds):**

- **PR9-F1 — a held `MERGE_MSG.lock` passed `Worktree.Verify`.** Found by the kill sampler on its
  first run: a pick killed inside `write_message` leaves `MERGE_MSG.lock`, a merged index and no
  `MERGE_MSG`; the verifier read only the committed name, reused the worktree, and the re-run pick
  died on `could not lock 'MERGE_MSG': File exists`. Fixed as the class in
  `src/workspace_manager/residue.rs`: every administrative state file is read in its committed
  form and in its held `.lock` form (`CHERRY_PICK_HEAD`, `MERGE_HEAD`, `MERGE_MSG`, `REVERT_HEAD`),
  in the verifier and in the residue classifier alike; `index.lock` was already the held form of
  `index`. Within the packet's quiescence rule ("no cherry-pick/merge/revert/sequencer/rebase
  state exists"): a held lock is that state, in progress. Pinned by
  `worktree_verify_answers_every_non_quiescence_by_name` and the K3 half of the late-kill test.
- **PR9-F2 — the materialization left `MERGE_MSG` for the life of the generation.** Found by the
  ST-15 (repair) test: the retry's `HoldsTree` verification read the `MERGE_MSG` the pick had
  left and closed the generation, so no repair generation could ever be retried in place, which
  the packet's own `ST-15` for repairs requires. Fixed in `WorkspaceManager::repair_materialize`:
  the funnel removes `MERGE_MSG` and `AUTO_MERGE` once the pick has ended, in all three outcomes.
  Consequence recorded in R6's terms: a *completed* materialization is now quiescent at its base,
  so recovery (g) reuses it (`Reuse::Verified`) and the continuation's re-run pick is a no-op
  reporting the same observation; a kill before the removal still fails the verify and is
  recreated. `a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once`
  drives all three prefixes.
- **Ordering of the unresolved-conflict verdict.** First written after `evaluate_outcome`, which
  reported a completed worker's empty diff (`EmptyDiff`) ahead of the conflict it left. Now the
  unresolved verdict pre-empts the diff-shaped verdicts but not the worker's own end (error exit,
  timeout, question). Pinned by `an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_resolved_one_is_staged`.

**Recorded, not fixed (each is outside this slice's authority or beyond the packet's rule):**

- **PR9-N1 — the frozen residue row for `Object.RepairMaterialize` names `CHERRY_PICK_HEAD`,**
  which `--no-commit` never leaves (R5, and the pre-existing workspace_manager residue grid
  already says so); what it leaves is `MERGE_MSG`, which the row does not name. The element is
  still constructed synthetically because PR3 froze the row. Amending the row is a change to
  `ObjectSite::residue_elements()` in `src/topology/effects.rs` and to `effects/residue-classes.json`
  — frozen; **approval owed**, not made.
- **PR9-N2 — `Quiescence::AtBase` does not compare the index with the base's tree.** The packet's
  rule reads HEAD, the index lock and the state files, and this slice keeps to it. The one
  interrupted state it therefore passes — a merged index with no state file — converges because
  the re-run pick is idempotent (measured, §8); a stricter rule would be a strengthening beyond
  the packet (`hardening-vs-defect`), so it is recorded here rather than made.
- **PR9-N3 — `ORIG_HEAD.lock` is not read** for the same reason `ORIG_HEAD` is not (ordinary
  commands write it); no engine funnel writes `ORIG_HEAD` in a task worktree, measured by the
  sampler's residue histogram.
- **PR9-N4 — the human guidance text of an admission question is not on the schema-4 wire**
  (`FrozenQuestion.context` carries a fixed sentence; the worker's prompt is not told what the
  materialization observed, R12). No change; the observation is in `attempt_started`.
- **PR9-N5 — `TaskRegistry::originals_with_agents` and `Damage.small_only`:** a Small-only chain is
  accepted and yields a `HumanBinding` repair; nothing in `registry.rs` refuses a chain whose every
  rung is below the repair floor at ingest, so the first repair of such a task always asks a
  person. That is the packet's rule (`repairs.human_binding`) and is recorded as the behaviour a
  plan author gets, not as a defect.

**The two deferred findings (§7):** neither `PR8-R2-SPEND-REPLAY` nor `PR8-CRASH-002` is made newly
reachable. The first concerns the replay of a parked review's cost, which this slice does not
touch (`Spend::replay` is unchanged and `a_runs_spend_is_the_same_live_as_on_replay` still holds);
the second concerns a crash window in candidate publication that repairs enter through the same
`promote_candidate` path as ordinary tasks, with `WidensLineage` the only difference, and the
window is neither widened nor moved.

## 10. Tests against the packet's `gating` and `proof_tests`

Every row names the test that holds it at the head of this branch. Where the test is narrower
than the packet's sentence, the row says how.

| Packet item | Test |
|---|---|
| rejection kind tests (invalid or duplicate override; non-override `attempt_started` on an overridden task; `LineageHeld`/predicted disposition mismatch) | `fold::tests::an_override_replaces_the_frozen_binding_for_every_later_attempt` (E2: exact five-field binding accepted; every single-field move, the tier either side of the floor and the pin refused; a floorless ladder refused naming the floor); `a_closing_generation_admits_exactly_the_disposition_its_holding_implies` (PR8, unchanged); `a_rejection_already_folded_is_refused_when_it_arrives_again` |
| limit tests (over-limit registers `AwaitingInput`; nothing spends until the answer; the answer activates) | `recover::tests::an_over_limit_repair_spends_nothing_until_its_answer_activates_it`, `a_repair_admission_answer_activates_the_repair_which_materializes_and_merges_through_the_queue` |
| ST-12 one-off binding authority | `recover::tests::a_one_off_binding_answer_activates_a_repair_no_frozen_rung_can_run` (the five-field override on the wire and the attempt run under it, pinned, at the floor), `a_binding_answer_naming_no_frozen_option_is_refused_before_any_append`, `declining_a_repairs_admission_fails_its_lineage_and_halts_the_run_only_when_asked_to` (both `halts_run` values); the fold half in the row above |
| ST-11 repair | `recover::tests::a_fresh_incarnation_closes_a_retained_repair_generation_lineage_held_and_the_next_materializes_again` |
| ST-15 repair | `recover::tests::a_repairs_same_session_retry_records_retained_and_is_not_materialized_again` (one `Object.RepairMaterialize` execution across both attempts, counted at the hook harness) |
| two-lineage order test; lineage candidates through the queue with `satisfies` | `recover::tests::two_lineages_publish_in_lineage_order_and_the_younger_candidate_waits_behind_the_older`. **Narrower than the sentence:** the rejections are `Conflict` dispositions on the record and the repairs' picks apply cleanly; the fake worker resolves nothing, so the conflict-resolving half is the attempt-level `an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_resolved_one_is_staged`, where the resolution is written by the test. The lineage-order claim itself — the younger candidate queued first and ineligible, publication in lineage order with `satisfies` and the lineage release — is held whole. |
| source-protection test across Parked/BudgetExceeded resume | `recover::tests::a_rejected_candidates_ref_survives_a_budget_stop_and_the_repair_dispatches_after_the_resume` (BudgetExceeded); the Parked half is the over-limit test, whose run is parked on the question across the resume |
| T-ANSWER | `recover::tests::an_answer_published_into_the_run_directory_is_ingested_by_the_next_incarnations_first_step` (the production `EventLogAnswers` reader, `via: "event-log"`) |
| already-present-source repair | `attempt::tests::an_already_present_source_proceeds_as_an_ordinary_attempt_whose_empty_diff_fails_honestly`; the dispatch-level `Empty` observation in `dispatch::tests::a_repair_dispatch_records_what_its_materialization_observed` |
| kills incl. mid-materialization, synthetic and sampled | `dispatch::tests::repair_materialization_synthetic_residue_recreated_after_forced_removal`, `sampled_repair_materialization_child_kills_every_residue_classified_and_recovered` (N=8), `a_materialization_killed_after_its_index_write_converges_from_both_of_its_states`, `repair_materialization_objects_released_to_git_on_scrub`; the existing `repair_materialization_reproduced_after_kill` |
| T-REPAIR-DISPATCH across the process boundary | `recover::tests::a_repair_dispatch_interrupted_before_its_attempt_is_recreated_at_its_base_and_materialized_once` (no worktree; held `MERGE_MSG.lock`; completed pick) |
| T-RETAINED / T-RETRY (repair) | the ST-11 and ST-15 rows; an interrupted retry's `LineageHeld` settlement is `fold::tests::a_closing_generation_admits_exactly_the_disposition_its_holding_implies` plus the loop's `settle_interrupted` through `closing_disposition` (PR8) |
| ST-05 lease subset | `fold::tests::a_lineage_lease_is_released_exactly_once_and_a_second_release_is_refused` |
| unresolved index entries fail capture before gates (R9) | `attempt::tests::an_unresolved_conflict_fails_the_capture_before_any_gate_and_a_resolved_one_is_staged`, `deleting_a_conflicted_file_resolves_it` |
| lineage terminal failure | `fold::tests::a_repair_members_terminal_failure_folds_its_lineage_live_and_on_replay`, `an_ordinary_tasks_terminal_failure_leaves_a_live_lineage_exactly_as_it_was` |
| the checkpoint split | `select::tests::every_step_variant_is_admitted_or_refused_and_the_split_is_seven_two`, `a_repair_origin_task_crosses_the_checkpoint_and_the_ceiling_binds_it_like_any_dispatch` |

## 11. The funnel layer's changes (`src/workspace_manager.rs`, `src/workspace_manager/residue.rs`)

Not frozen, but the layer G3 certified, so listed:

- `repair_materialize` returns `Materialized::{Clean, Conflict, Empty}` (R5) and, once the pick
  has ended, removes `MERGE_MSG` and `AUTO_MERGE` (`clear_pick_state`; PR9-F2). Its doc's claim
  that `CHERRY_PICK_HEAD` is left was corrected against the measurement.
- New reads: `staged_against_head`, `unmerged_records` (shared with `proposal_state`),
  `unresolved_conflicts` (unmerged paths whose working-tree file carries a line beginning
  `<<<<<<< ` or `>>>>>>> `; a missing file is resolved by deletion), `carries_conflict_marker`.
  `effects/wrappers.toml` lists `unresolved_conflicts` as effect-free.
- `administrative_residue_at` and `observed_residue_elements` read each state file's held
  `.lock` form as that state's residue (PR9-F1); `state_file_present` is the one rule.
- `quiescence()` is otherwise unchanged: HEAD, the index lock, the state files (PR9-N2).
